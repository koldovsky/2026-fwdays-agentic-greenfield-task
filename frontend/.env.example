# Copy to frontend/.env for local development.
# Leave empty to use the Vite dev proxy (recommended locally).
# For production builds, set the absolute backend URL, e.g. https://api.example.com
VITE_API_URL=
