# Project Current State

## [2026-07-04T12:05:00+03:00]

**Остання дія:** Повністю реалізовано та верифіковано можливість `dashboard-analytics` (усі 28 завдань завершено).

**Статус:**
*   **Працює:**
    *   Встановлено та налаштовано бібліотеку `recharts` для побудови графіків.
    *   Створено API маршрути: генерації API-ключа `/api/dashboard/api-key`, збереження та зчитування зашифрованих/маскованих доступів CRM/телефонії `/api/dashboard/credentials`, отримання та агрегації аналітичних метрик `/api/analytics` у межах 14-місячного ліміту з урахуванням Київського часу.
    *   Розроблено клієнтський інтерфейс дашборду `DashboardClient.tsx` з миттєвим перемиканням табів (Аналітика, Інтеграція, Інструкції, Оплата).
    *   Реалізовано вкладку "Аналітика" з селектором періоду, сумарними картками метрик, динамічним лінійним графіком і круговими діаграмами розподілу конверсій на Recharts (без hydration mismatches), а також таблицею останніх 100 конверсій з пагінацією та фільтрацією на стороні сервера.
    *   Реалізовано вкладку "Інтеграція" з формами доступів (з валідацією посилань HTTPS), генератором API-ключа та Apps Script коду з підстановкою поточного домену.
    *   Реалізовано вкладку "Інструкції" з акордеоном для GA4, Google Ads, GCP (з сервісним акаунтом `auto@acontrol.pro` та ролями) та Binotel.
    *   Проведено перевірку типів (`npx tsc --noEmit`), лінтинг (`npm run lint`), а також повну збірку проекту (`npm run build`) — усі перевірки пройшли успішно з 0 помилок.
*   **В процесі:** Очікування команди на архівування виконаної зміни `dashboard-analytics` (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити синхронізацію дельта-специфікації та архівування зміни за допомогою команди `/opsx:archive`.

## [2026-07-04T11:53:00+03:00]

**Остання дія:** Виправлено всі зауваження (F1–F7 та Gap pass) повторного незалежного огляду специфікації `dashboard-analytics` у файлах `spec.md`, `design.md` та `tasks.md`.

**Статус:**
*   **Працює:**
    *   Специфікацію `spec.md` повністю оновлено: додано розділ для вкладки "Оплата" (FR-SUB-01, FR-SUB-06, FR-SUB-07) з детальними сценаріями перегляду підписки, оформлення checkout, паузи та скасування підписки.
    *   Змінено логіку фільтрації таблиці конверсій з клієнтської на серверну для точності логування.
    *   Додано обов'язкове використання протоколу `https://` для CRM та телефонії, покращено обробку помилок шифрування/валідації, врегульовано поведінку оновлення замаскованих паролів та обмежено аналітичні дати майбутнім часом.
    *   Додано глобальні обмеження щодо локалізації та тону бренду (NFR-I18N-01, BC-BRAND-01).
    *   Документи `design.md` та `tasks.md` синхронізовано з оновленою специфікацією (додано задачі реалізації Billing-вкладки та серверної фільтрації).
    *   Усі планувальні артефакти повністю готові та погоджені.
*   **В процесі:** Очікування запуску реалізації можливості `dashboard-analytics`.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `dashboard-analytics` за допомогою команди `/opsx-apply`.

## [2026-07-04T11:45:00+03:00]

**Остання дія:** Усунено всі зауваження (F1–F4 та Gap pass) повторного незалежного огляду специфікації `dashboard-analytics` у файлі `spec.md`.

**Статус:**
*   **Працює:**
    *   Специфікацію `spec.md` повністю оновлено та виправлено: усунуто суперечність із виведенням згенерованого API-ключа (тепер використовується плейсхолдер), додано шифрування та перевірку наявності для `telephonyPassword`, уточнено часовий пояс (`Europe/Kyiv`) для групування конверсій на лінійному графіку, впроваджено правила нормалізації джерел (`ad_source`) та каналів (`channel`) для кругових діаграм, а також прописано сценарії для порожніх станів (empty state) віджетів та таблиці.
    *   Усі планувальні артефакти повністю готові та погоджені.
*   **В процесі:** Очікування запуску реалізації можливості `dashboard-analytics`.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `dashboard-analytics` (за допомогою команди `/opsx-apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-DASH-01` — `FR-DASH-08`, `NFR-PERF-02`, `NFR-SEC-01`.

## [2026-07-04T11:40:00+03:00]

**Остання дія:** Виправлено всі зауваження (F1–F5) незалежного огляду специфікації `dashboard-analytics` у файлі `spec.md`.

**Статус:**
*   **Працює:**
    *   Специфікацію `spec.md` повністю оновлено та виправлено: виправлено назву індексу на `idx_conversions_user_id_conversion_time`, додано перевірку автентифікації/сесій у сценарії, виключено зайве дешифрування чутливих даних для перевірки наявності, додано перевірку меж дат (`endDate >= startDate`), додано динамічне групування для лінійного графіка (по днях/тижнях/місяцях), а також уточнено обмеження клієнтської фільтрації до 100 записів.
    *   Усі планувальні артефакти готові та погоджені.
*   **В процесі:** Очікування запуску реалізації можливості `dashboard-analytics`.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `dashboard-analytics` (за допомогою команди `/opsx-apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-DASH-01` — `FR-DASH-08`, `NFR-PERF-02`.

## [2026-07-04T11:35:00+03:00]

**Остання дія:** Проведено незалежний огляд (review) специфікації `dashboard-analytics` в каталозі `openspec/changes/dashboard-analytics` та сформовано звіт.

**Статус:**
*   **Працює:**
    *   Створено та перевірено планувальні артефакти для `dashboard-analytics`.
    *   Проведено аудит специфікації, виявлено 5 знахідок (3 середнього рівня ризику `Warning` та 2 пропозиції `Suggestion`).
*   **В процесі:** Очікування виправлення знахідок специфікації та затвердження плану реалізації.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Виправити зауваження F1–F5 у специфікації `spec.md` відповідно до звіту огляду.
2. Затвердити план та запустити реалізацію можливості `dashboard-analytics`.

**Примітки:**
*   Пов'язані вимоги: `FR-DASH-01` — `FR-DASH-08`, `NFR-PERF-02`.

## [2026-07-04T11:30:00+03:00]

**Остання дія:** Ініційовано зміну `dashboard-analytics` та створено планувальні артефакти: `proposal.md`, `specs/dashboard-analytics/spec.md`, `design.md` та `tasks.md`.

**Статус:**
*   **Працює:**
    *   Створено та повністю валідовано планувальні артефакти OpenSpec для можливості `dashboard-analytics` (4/4 артефактів готово).
    *   Специфікація містить 9 вимог з відповідними тестовими сценаріями: форми доступів CRM та телефонії з AES-256-GCM шифруванням, віджет інструкцій для GA4/GAds/GCloud/Binotel, генерація API-ключа та Apps Script, селектор дат аналітики, картки метрик, лінійний графік динаміки та кругові діаграми розподілу на Recharts, а також лог конверсій з пагінацією та фільтрацією.
    *   Архітектурний дизайн описує використання клієнтських табів для покращення UX, динамічний імпорт графіків Recharts для обходу hydration mismatch в Next.js, оптимізований єдиний запит агрегації та маскування чутливих полів доступу.
    *   Перелік завдань містить 23 завдання у 8 групах.
*   **В процесі:** Очікування погодження плану та запуску реалізації.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `dashboard-analytics` за допомогою команди `/opsx-apply`.

**Примітки:**
*   Пов'язані вимоги: `FR-DASH-01` — `FR-DASH-08`, `NFR-PERF-02`, `TC-STACK-02`, `TC-STACK-04`.
*   Залежності: `core-database` (схема та підключення), `telegram-auth` (сесії), `ingestion-api` (конверсії).

## [2026-07-03T20:26:00+03:00]

**Остання дія:** Синхронізовано дельта-специфікацію `ingestion-api` до головної специфікації `openspec/specs/ingestion-api/spec.md` та заархівувано зміну в `openspec/changes/archive/2026-07-03-ingestion-api`.

**Статус:**
*   **Працює:**
    *   Синхронізовано специфікацію `ingestion-api` (створено файл [spec.md](file:///d:/home/Documents/Developer/2026-fwdays-agentic-greenfield-task/openspec/specs/ingestion-api/spec.md)).
    *   Зміну `ingestion-api` заархівовано в [openspec/changes/archive/2026-07-03-ingestion-api](file:///d:/home/Documents/Developer/2026-fwdays-agentic-greenfield-task/openspec/changes/archive/2026-07-03-ingestion-api).
    *   Всі артефакти та завдання з розробки `ingestion-api` повністю завершено (100% готовності).
    *   Проект успішно збирається (`npm run build`) та проходить перевірку лінтера (`npm run lint`).
*   **В процесі:** Підготовка до планування та реалізації наступної можливості.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Ініціювати та спланувати наступну можливість (наприклад, `dashboard-analytics`).

**Примітки:**
*   Архів успішно створено, зміни додано в git.

## [2026-07-03T20:20:00+03:00]

**Остання дія:** Повністю реалізовано та протестовано можливість `ingestion-api` (усі 18 завдань завершено).

**Статус:**
*   **Працює:**
    *   Додано функції автентифікації API-ключів та перевірки підписки у `lib/auth.ts`.
    *   Створено валідатор конверсій `lib/conversions.ts` з підтримкою типів TypeScript та валідацією часових міток.
    *   Реалізовано API-ендпоінт `POST /api/conversions` у `app/api/conversions/route.ts` із підтримкою batch insert, валідації JSON, автентифікації та перевірки статусу підписки.
    *   Створено API-ендпоінт очищення застарілих конверсій `GET /api/conversions/cleanup` у `app/api/conversions/cleanup/route.ts` з використанням batch delete по `LIMIT 10000`.
    *   Додано `vercel.json` з конфігурацією щоденного Cron Job.
    *   Додано `CRON_SECRET` у `.env` та `.env.example`.
    *   Проект успішно проходить лінтинг (`npm run lint`), компіляцію (`tsc --noEmit`) та збірку (`npm run build`).
*   **В процесі:** Очікування команди на архівування виконаної зміни `ingestion-api` (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Синхронізувати дельта-специфікацію та заархівувати зміну (команда `/opsx:archive`).
2. Перейти до етапу 5: dashboard-analytics (візуалізація отриманих даних та налаштування інтеграцій користувачем).

**Примітки:**
*   Пов'язані вимоги: `FR-API-01` — `FR-API-06`, `NFR-PERF-01`.

## [2026-07-03T19:57:00+03:00]

**Остання дія:** Виправлено всі знахідки рецензії специфікації `ingestion-api` (F1–F5 та Gap pass) у файлі `spec.md`.

**Статус:**
*   **Працює:**
    *   **F1** — Уточнено глосарій "Активна підписка": пояснено різницю між полем `status = 'active'` у БД та бізнес-концепцією активності; додано попередження, що перевірка лише `status` є недостатньою.
    *   **F2** — Додано сценарій "Захист Cron-ендпоінта — невалідний токен" (`Authorization` присутній, але значення не збігається з `CRON_SECRET` → `401`).
    *   **F3** — Додано вимогу `Content-Type: application/json` та обмеження розміру тіла 4 МБ до FR-API-01.
    *   **F4** — Додано сценарій для `conversionTime` у далекому минулому (старішому за 14 місяців): API приймає запис, але він буде видалений Cron-задачею; поведінка задумана.
    *   **F5** — Уточнено семантику `conversionName` у таблиці схеми та сценаріях: рядок вважається непорожнім лише після `trim` (рядки з лише пробільними символами — невалідні).
    *   **Gap pass** — Розширено NFR-PERF-01: додано цільовий показник ≤ 500 мс на p95 для пакетів 101–500 записів.
    *   **Gap pass** — Додано примітку про ідемпотентність Cron-задачі при примусовому завершенні Vercel (часткове видалення прийнятне, наступний запуск продовжить).
*   **В процесі:** Очікування команди на запуск реалізації `ingestion-api` (команда `/opsx-apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `ingestion-api` (команда `/opsx-apply`).
2. Після реалізації — верифікація (`tsc --noEmit`, `npm run lint`, `npm run build`) та ручне тестування за чеклістом розділу 7 `tasks.md`.

**Примітки:**
*   Пов'язані вимоги: `FR-API-01`, `FR-API-02`, `FR-API-03`, `FR-API-04`, `FR-API-05`, `NFR-PERF-01`.
*   Специфікація готова до реалізації — всі знахідки огляду закриті.

## [2026-07-03T19:34:00+03:00]

**Остання дія:** Виправлено всі зауваження незалежного огляду специфікації `ingestion-api` (F1–F5 та Gap pass).

**Статус:**
*   **Працює:**
    *   `spec.md`: додано розділ **Glossary** (конверсія, активна підписка, API-ключ, пакет конверсій, 14 місяців).
    *   `spec.md`: уточнено **JSON-контракт API** — додано таблицю всіх 11 полів із типами та обов'язковістю, пояснено що camelCase є канонічним (F1).
    *   `spec.md`: виправлено **семантику "14 місяців"** — замінено розмиту фразу на точний SQL-вираз `conversionTime < NOW() - INTERVAL '14 months'` в усіх сценаріях; додано `LIMIT 10000` у вимогу очищення (F3, Gap).
    *   `spec.md`: додано **транзакційну семантику пакету** — явний сценарій "все або нічого" при помилках валідації (F5).
    *   `spec.md`: додано **сценарій відсутньої змінної `CRON_SECRET`** — система повертає `500` та логує помилку конфігурації (Gap).
    *   `tasks.md`: виправлено завдання 5.1 — уточнено порядок перевірок Cron (спочатку `CRON_SECRET`, потім `Authorization`), додано `LIMIT 10000` та точний SQL-критерій.
    *   `tasks.md`: додано **Розділ 7 "Ручне приймальне тестування" (Definition of Done)** — 8 curl-сценаріїв (F4).
    *   `proposal.md`: виправлено **невідповідність шляху файлу** — `scripts/cleanup-conversions.ts` → `app/api/conversions/cleanup/route.ts`, додано `vercel.json` (Gap Warning).
*   **В процесі:** Очікування команди на запуск реалізації `ingestion-api` (команда `/opsx-apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `ingestion-api` (команда `/opsx-apply`).
2. Після реалізації — верифікація (`tsc --noEmit`, `npm run lint`, `npm run build`) та ручне тестування за чеклістом розділу 7 tasks.md.

**Примітки:**
*   Пов'язані вимоги: `FR-API-01`, `FR-API-02`, `FR-API-03`, `FR-API-04`, `FR-API-05`, `NFR-PERF-01`.
*   Усі знайдені під час `/review-task` проблеми (F1–F5 + 3 Gap) закриті.

## [2026-07-03T19:25:00+03:00]

**Остання дія:** Ініційовано зміну `ingestion-api` та створено всі 4 планувальні артефакти: `proposal.md`, `specs/ingestion-api/spec.md`, `design.md` та `tasks.md`.

**Статус:**
*   **Працює:**
    *   Створено та повністю валідовано планувальні артефакти OpenSpec для можливості `ingestion-api` (4/4 артефактів готово).
    *   Специфікація містить 7 вимог із 17 тестовими сценаріями: автентифікація за API-ключем, перевірка підписки (5 сценаріїв на різні статуси), валідація схеми конверсій, продуктивність (batch insert, p95 ≤ 200 мс), очищення застарілих даних (14 місяців), обробка помилок API.
    *   Дизайн-документ фіксує 5 технічних рішень: SHA-256 автентифікація, batch INSERT через Drizzle, ручна валідація без Zod, Vercel Cron Jobs для очищення, перевірка статусу підписки та періоду.
    *   Перелік завдань містить 10 задач у 6 групах, готових до реалізації.
*   **В процесі:** Очікування рішення про запуск реалізації можливості `ingestion-api` (команда `/opsx-apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `ingestion-api` (команда `/opsx-apply`).
2. Після реалізації — верифікація (`tsc --noEmit`, `npm run lint`, `npm run build`).

**Примітки:**
*   Пов'язані вимоги: `FR-API-01`, `FR-API-02`, `FR-API-03`, `FR-API-04`, `FR-API-05`, `NFR-PERF-01`.
*   Залежності: `core-database` (схеми таблиць), `billing-monobank` (підписки).

## [2026-07-03T19:05:00+03:00]

**Остання дія:** Проаналізовано та успішно виправлено всі 19 проблем лінтингу (помилок та попереджень) у проекті.

**Статус:**
*   **Працює:**
    *   Усі розроблені API Route Handlers та клієнтські компоненти очищено від помилок ESLint (`prefer-const`, `@typescript-eslint/no-explicit-any`, `@typescript-eslint/no-unused-vars`).
    *   Повторний запуск `npm run lint` завершується успішно без жодних зауважень.
    *   Проект успішно проходить компіляцію TypeScript та збирання.
*   **В процесі:** Очікування команди на архівування зміни (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити архівування завершеної зміни за допомогою команди `/opsx:archive`.
2. Провести тестування в пісочниці (Sandbox) з реальним токеном Monobank Acquiring.

**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `FR-NOTIF-01` — `FR-NOTIF-06`, `TC-STACK-05`.

## [2026-07-03T18:50:00+03:00]

**Остання дія:** Повністю реалізовано та протестовано інтеграцію з Monobank Acquiring API та керування підписками (усі 16 завдань завершено).

**Статус:**
*   **Працює:**
    *   Схему бази даних `subscriptions` оновлено новими статусами та полем `autoRenew`, згенеровано та успішно перевірено міграції Drizzle (`0002_ordinary_nextwave.sql` та `0003_familiar_mastermind.sql`).
    *   Реалізовано HTTP-клієнт Monobank Acquiring (`lib/monobank.ts`) із підтримкою створення рахунків, оплат за токеном, видалення карт та перевіркою підпису вебхуків `X-Sign` (з ротацією ключів).
    *   Створено API-ендпоінти: `/api/billing/invoice` (виписка рахунку), `/api/billing/webhook` (вебхук оплат), `/api/billing/pause` (пауза), `/api/billing/cancel` (скасування), `/api/billing/resume` (поновлення), та `/api/billing/cron` (фоновий воркер).
    *   Написано скрипт фонового планувальника (`scripts/billing-cron.ts`) для проведення автосписань, управління ретраями та переходом життєвих циклів підписки.
    *   Реалізовано Telegram-сповіщення адміністратора та користувачів (`lib/notifications.ts`).
    *   Розроблено клієнтський інтерфейс білінгу (`app/dashboard/BillingManager.tsx`) та інтегровано його в особистий кабінет (`app/dashboard/page.tsx`).
    *   Проект успішно збирається та проходить компіляцію TypeScript (`tsc --noEmit`).
*   **В процесі:** Очікування команди на архівування зміни (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити архівування завершеної зміни за допомогою команди `/opsx:archive`.
2. Провести тестування в пісочниці (Sandbox) з реальним токеном Monobank Acquiring.

**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `FR-NOTIF-01` — `FR-NOTIF-06`, `TC-STACK-05`.

## [2026-07-03T18:31:00+03:00]

**Остання дія:** Виправлено всі виявлені помилки та неточності в специфікації можливості `billing-monobank` (`spec.md`), дизайні (`design.md`) та списку завдань (`tasks.md`).

**Статус:**
*   **Працює:**
    *   Специфікація `spec.md` повністю оновлена: виправлено параметри рекурентних платежів гаманця Monobank, додано збереження `invoiceId` при поновленні та автосписанні підписки для асинхронного 3DS матчингу, інтегровано Telegram-сповіщення адміністратора про автоплатежі в кроні.
    *   Проектну документацію (`design.md` та `tasks.md`) приведено до відповідності з виправленою специфікацією.
*   **В процесі:** Очікування погодження виправленої специфікації та команди на запуск реалізації можливості `billing-monobank` (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `billing-monobank` (команда `/opsx:apply`).
2. Створити та згенерувати міграції бази даних.

**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `FR-NOTIF-02`, `TC-STACK-05`.

## [2026-07-03T18:26:00+03:00]

**Остання дія:** Проведено незалежний огляд специфікації можливості `billing-monobank` (файл `spec.md`).

**Статус:**
*   **Працює:**
    *   Проведено перевірку специфікації `spec.md` на відповідність вимогам API Monobank та вимогам у `requirements.md`.
    *   Сформовано звіт про огляд специфікації з вердиктом `REQUEST_CHANGES` та оцінкою ризику `7`.
*   **В процесі:** Очікування на усунення критичних та другорядних зауважень у специфікації `spec.md` (невідповідність API рекурентних списань, відсутнє сповіщення адміну, проблема з 3DS при поновленні).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Оновити специфікацію `spec.md` відповідно до зауважень огляду.
2. Затвердити планувальні артефакти та запустити реалізацію можливості `billing-monobank` (команда `/opsx:apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `FR-NOTIF-02`.

## [2026-07-03T18:15:00+03:00]

**Остання дія:** Виправлено всі зауваження та помилки в специфікації можливості `billing-monobank` (у файлах `spec.md`, `design.md` та `tasks.md`) на основі вимог та документації API Monobank.

**Статус:**
*   **Працює:**
    *   Специфікація `spec.md` повністю оновлена: додано динамічний розрахунок UAH-еквіваленту та кешування курсу валют для обходу rate limits, універсальний сценарій купівлі для обох тарифів, перевірка ідемпотентності вебхуку, чітке налаштування Telegram-сповіщень користувачів і адміністратора (із зазначенням джерел даних та спокійного тону), а також опис життєвого циклу поновлення підписки.
    *   Архітектурний дизайн `design.md` оновлено новими рішеннями (кешування курсів, стратегія поновлення) та оновлено ризики.
    *   Список завдань `tasks.md` доповнено відповідними етапами реалізації.
    *   Всі артефакти успішно змінено та перевірено за допомогою `git diff`.
*   **В процесі:** Очікування рішення про початок кодування можливості `billing-monobank` (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `billing-monobank` (команда `/opsx:apply`).
2. Додати статус `created` до `subscriptionStatusEnum` у схемі та підготувати міграцію.

**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `FR-NOTIF-02` — `FR-NOTIF-06`, `BC-BRAND-01`, `TC-STACK-05`.

## [2026-07-03T17:57:00+03:00]

**Остання дія:** Виправлено всі зауваження та помилки в специфікації можливості `billing-monobank` (`spec.md`).

**Статус:**
*   **Працює:**
    *   Оновлено специфікацію `spec.md` для `billing-monobank` — виправлено критичні невідповідності з Monobank API (`X-Sign` заголовок, зчитування токенів з `walletData`), узгоджено назви полів та статусів бази даних (`lastInvoiceId`, `tariffPlan`, статус `created`), уточнено логіку переходу статусів при паузі та скасуванні, додано вимогу проміжних сповіщень про невдалі списання та підтримано ротацію ключів.
    *   Додано розділи "Verification & Testing Plan" та "Definition of Done".
    *   Усі виявлені під час огляду зауваження повністю усунено в специфікації.
*   **В процесі:** Очікування рішення про початок реалізації можливості `billing-monobank` (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Затвердити оновлені планувальні артефакти та розпочати кодування можливості `billing-monobank` (команда `/opsx:apply`).
2. Додати статус `created` до `subscriptionStatusEnum` у схемі [subscriptions.ts](file:///d:/home/Documents/Developer/2026-fwdays-agentic-greenfield-task/db/schema/subscriptions.ts) та згенерувати відповідну міграцію.


**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `TC-STACK-05`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T17:01:00+03:00]


**Остання дія:** Ініційовано зміну `billing-monobank` та створено планувальні артефакти: `proposal.md`, `specs/billing-monobank/spec.md`, `design.md` та `tasks.md`.

**Статус:**
*   **Працює:**
    *   Створено та повністю валідовано планувальні артефакти OpenSpec для можливості `billing-monobank` (4/4 артефактів готово).
    *   Специфікація містить детальні вимоги та сценарії для двох тарифних планів, токенізації карток через `saveCard: true`, обробки вебхуків еквайрингу, автоматичних списань (Cron), повторних спроб списання при збоях та кнопок паузи/скасування з видаленням токена.
    *   Дизайн-документ фіксує використання вбудованого модуля `crypto` для перевірки сигнатур ECDSA, архітектуру фонового воркера та стратегію ідемпотентності вебхуків.
*   **В процесі:** Очікування рішення про запуск огляду специфікації або початку кодування.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Провести незалежний огляд специфікації можливості `billing-monobank` (команда `/opsx:review` або `review-task`).
2. Запустити реалізацію можливості `billing-monobank` (команда `/opsx:apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-SUB-01` — `FR-SUB-07`, `TC-STACK-05`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T16:51:00+03:00]

**Остання дія:** Синхронізовано дельта-специфікацію `spec.md` можливості `telegram-auth` до головної специфікації `openspec/specs/telegram-auth/spec.md` та заархівовано зміну в `openspec/changes/archive/2026-07-03-telegram-auth`.

**Статус:**
*   **Працює:**
    *   Успішно перенесено (синхронізовано) опис вимог та сценаріїв до головної специфікації `openspec/specs/telegram-auth/spec.md`.
    *   Зміну `telegram-auth` переміщено до архіву `openspec/changes/archive/2026-07-03-telegram-auth`.
    *   Всі артефакти та завдання завершено (100% готовності).
*   **В процесі:** Перехід до наступного етапу — планування та реалізація billing-monobank (керування підписками та еквайринг monobank).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Ініціювати та спланувати наступну можливість (billing-monobank).

**Примітки:**
*   Архів успішно створено.

## [2026-07-03T16:38:00+03:00]

**Остання дія:** Повністю реалізовано та протестовано можливість `telegram-auth` (реєстрація та автентифікація користувачів через Telegram-бот).

**Статус:**
*   **Працює:**
    *   Створено схеми `pending_registrations` та `otp_codes` у Drizzle ORM, згенеровано міграції.
    *   Реалізовано логіку Telegram-бота (`lib/telegram-bot.ts`) зі збором даних, валідацією, нормалізацією URL та підтримкою команди `/cancel`.
    *   Створено API Route Handlers для вебхуку Telegram, токенів реєстрації, Magic Link, OTP кодів, перевірки polling та logout.
    *   Впроваджено rate limiting та захист від User Enumeration на етапі входу.
    *   Створено сторінки реєстрації, входу та кабінету-плейсхолдера з дизайном "Precision Hub" та шрифтами.
    *   Всі лінтинг та TypeScript перевірки проходять успішно (0 помилок).
*   **В процесі:** Очікування команди на архівацію виконаної зміни `telegram-auth` (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Синхронізувати дельта-специфікацію та заархівувати зміну (команда `/opsx:archive`).
2. Перейти до етапу 3: billing-monobank (керування підписками та еквайринг monobank).

**Примітки:**
*   Повністю покриває вимоги: `FR-AUTH-01`, `FR-AUTH-02`, `FR-AUTH-03`, `FR-AUTH-04`, `FR-AUTH-05`, `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T16:26:00+03:00]

**Остання дія:** Створено детальний план реалізації (`implementation_plan.md`) для можливості `telegram-auth` та передано його на розгляд користувачеві.

**Статус:**
*   **Працює:**
    *   Специфікація `spec.md` повністю оновлена.
*   **В процесі:** Очікування відгуку та погодження плану реалізації від користувача для початку кодування можливості `telegram-auth`.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Отримати погодження плану реалізації від користувача.
2. Запустити кодування та міграції бази даних.

**Примітки:**
*   Пов'язані вимоги: `FR-AUTH-01` — `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T16:22:00+03:00]

**Остання дія:** Успішно оновлено та виправлено специфікацію `telegram-auth` (`spec.md`) на основі зауважень повторного незалежного огляду.

**Статус:**
*   **Працює:**
    *   Специфікація `spec.md` повністю оновлена: додано вимоги до схем нових таблиць (`pending_registrations` та `otp_codes`), вимоги щодо анулювання попередніх токенів при нових запитах Magic Link/OTP, обробку помилок при збоях доставки повідомлень через Telegram Bot API, ліміти частоти перевірки OTP та опис механізму очищення бази даних.
    *   Генерацію та перегляд API-ключа перенесено у веб-кабінет (після першого входу).
    *   Визначено термін дії сесії та сесійного токена (30 днів), а також термін дії сесійної куки.
    *   Встановлено пільговий період 30 секунд для видалення реєстраційного токена.
*   **В процесі:** Очікування рішення про запуск реалізації можливості `telegram-auth` (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `telegram-auth` (команда `/opsx:apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-AUTH-01` — `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T16:15:00+03:00]

**Остання дія:** Виправлено всі зауваження другого незалежного огляду специфікації `telegram-auth` (у специфікації `spec.md`, дизайні `design.md` та переліку завдань `tasks.md`) відповідно до уточнень.

**Статус:**
*   **Працює:**
    *   Специфікація, дизайн-рішення та перелік завдань повністю оновлені, синхронізовані та виправлені.
    *   Генерацію та перегляд API-ключа перенесено у веб-кабінет (після першого входу), повністю прибравши цей крок з логіки реєстрації в боті.
    *   Визначено термін дії сесії та сесійного токена (30 днів), а також термін дії сесійної куки.
    *   Додано обмеження частоти запитів (rate limiting) на ініціацію входу (генерацію OTP та запит Magic Link) для захисту від флуду.
    *   Таблицю `magic_links` додано до механізму регулярного очищення бази даних від застарілих записів.
    *   Встановлено 30-секундний пільговий період для видалення реєстраційного токена з бази даних для уникнення збоїв редіректу при проблемах з мережею.
*   **В процесі:** Очікування рішення про запуск реалізації можливості `telegram-auth` (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `telegram-auth` (команда `/opsx:apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-AUTH-01` — `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T15:59:00+03:00]

**Остання дія:** Виправлено всі зауваження незалежного огляду специфікації `telegram-auth` (у специфікації `spec.md`, дизайні `design.md` та переліку завдань `tasks.md`).

**Статус:**
*   **Працює:**
    *   Специфікація, дизайн-рішення та перелік завдань повністю оновлені та синхронізовані.
    *   Додано ліміт спроб введення OTP-кодів (3 спроби) та вимогу про обмеження частоти запитів (rate limiting).
    *   Впроваджено захист від перебору користувачів (User Enumeration) на формі входу та анулювання реєстраційного токена відразу після створення сесії.
    *   Визначено поведінку бота при помилках валідації вхідних даних та підтримку команди скасування `/cancel`.
    *   Додано перевірку секретного токена вебхуку `X-Telegram-Bot-Api-Secret-Token` проти `TELEGRAM_BOT_WEBHOOK_SECRET` та щоденне чищення застарілих записів у БД.
*   **В процесі:** Очікування рішення про запуск реалізації можливості `telegram-auth` (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `telegram-auth` (команда `/opsx:apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-AUTH-01` — `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T15:52:00+03:00]

**Остання дія:** Проведено незалежний огляд специфікації можливості `telegram-auth` за допомогою скіла `review-task` та сформовано звіт.

**Статус:**
*   **Працює:**
    *   Сформовано детальний звіт про огляд специфікації `spec.md` для `telegram-auth` з вердиктом `REQUEST_CHANGES` (ризик 7).
*   **В процесі:** Очікування на виправлення критичних та другорядних зауважень у специфікації та дизайні (OTP brute-force ліміти, валідація вхідних даних ботом, запобігання User Enumeration).
*   **Заблоковано:** Перехід до фази кодування відкладено до оновлення специфікації.

**Наступні кроки:**
1. Виправити зауваження в специфікації `spec.md` (F1–F5 та Gap pass).
2. Затвердити оновлені планувальні артефакти та запустити кодування.

**Примітки:**
*   Пов'язані вимоги: `FR-AUTH-01` — `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T15:43:00+03:00]

**Остання дія:** Ініційовано зміну `telegram-auth` та створено планувальні артефакти: `proposal.md`, `specs/telegram-auth/spec.md`, `design.md` та `tasks.md`.

**Статус:**
*   **Працює:**
    *   Створено та повністю валідовано планувальні артефакти OpenSpec для можливості `telegram-auth` (4/4 артефактів готово).
    *   Специфікація містить детальні вимоги та сценарії для реєстрації на сайті, Telegram-бота, Magic Link та OTP-кодів, редіректів та мовних обмежень.
    *   Дизайн-документ фіксує використання вебхуків Next.js, таблиць `pending_registrations` та `otp_codes` у БД, опитування (short polling) та захист вебхуків.
*   **В процесі:** Очікування рішення про початок кодування.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію можливості `telegram-auth` (команда `/opsx:apply`).

**Примітки:**
*   Пов'язані вимоги: `FR-AUTH-01`, `FR-AUTH-02`, `FR-AUTH-03`, `FR-AUTH-04`, `FR-AUTH-05`, `FR-AUTH-06`, `TC-STACK-06`, `BC-BRAND-01`, `NFR-I18N-01`.

## [2026-07-03T15:35:00+03:00]

**Остання дія:** Успішно синхронізовано специфікації можливості `core-database` у головні специфікації та заархівовано зміну в `openspec/changes/archive/2026-07-03-core-database`.

**Статус:**
*   **Працює:**
    *   Синхронізовано дельта-специфікацію `spec.md` можливості `core-database` до головної специфікації `openspec/specs/core-database/spec.md`.
    *   Зміну `core-database` успішно заархівовано в `openspec/changes/archive/2026-07-03-core-database`.
    *   Всі артефакти та завдання завершено (100% готовності).
*   **В процесі:** Підготовка до планування та реалізації наступної можливості (Telegram-автентифікація).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Ініціювати та спланувати наступну можливість (наприклад, Telegram-автентифікація).

**Примітки:**
*   Архів успішно створено.

## [2026-07-03T13:51:00+03:00]


**Остання дія:** Проаналізовано помилки та попередження `npm run lint`, успішно виправлено конфігурацію ESLint та очищено попередження в коді.

**Статус:**
*   **Працює:**
    *   Додано ігнорування папки `types/` у конфігурацію ESLint (`eslint.config.mjs`), оскільки файли там генеруються автоматично і не повинні проходити лінтинг (вони також виключені у `tsconfig.json`).
    *   Прибрано невикористану змінну `error` у блоках `catch` файлу `lib/crypto.ts`.
    *   Повторний запуск `npm run lint` виконується повністю успішно без жодних помилок чи попереджень.
*   **В процесі:** Очікування на архівацію зміни `core-database` (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити архівацію виконаної зміни `core-database` (команда `/opsx:archive`).
2. Перейти до планування/реалізації наступної можливості (наприклад, Telegram-автентифікації).

**Примітки:**
*   Усунено 31 проблему лінтингу (25 помилок у згенерованих файлах та 6 попереджень у коді).

## [2026-07-03T12:49:00+03:00]

**Остання дія:** Успішно реалізовано всі 14 завдань для можливості `core-database` та перевірено працездатність коду.

**Статус:**
*   **Працює:**
    *   Встановлено `drizzle-orm`, `@neondatabase/serverless`, `pg` та відповідні dev-залежності.
    *   Створено конфігураційний файл `drizzle.config.ts`.
    *   Додано змінні `DATABASE_URL` та `ENCRYPTION_KEY` у `.env.example` та `.env`.
    *   Реалізовано менеджер підключень `db/index.ts` з підтримкою Serverless.
    *   Створено Drizzle-схеми для таблиць `users`, `magic_links`, `sessions`, `subscriptions` та `conversions` із усіма зв'язками, індексами та політикою каскадного видалення.
    *   Розроблено та успішно протестовано модуль симетричного шифрування AES-256-GCM у `lib/crypto.ts`.
    *   Згенеровано міграції через `drizzle-kit generate`.
    *   Збірка проекту (`npm run build`) проходить успішно, без помилок типізації.
*   **В процесі:** Очікування на архівацію зміни `core-database` (команда `/opsx:archive`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити архівацію виконаної зміни `core-database` (команда `/opsx:archive`).
2. Перейти до планування/реалізації наступної можливості (наприклад, Telegram-автентифікації).

**Примітки:**
*   Зміни торкнулися конфігурації TypeScript (`tsconfig.json`), куди додано виключення для застарілих типів Next.js з `types/`.
*   Повністю реалізовано та верифіковано вимоги: `FR-API-02`, `FR-DASH-03`, `FR-AUTH-06`, `NFR-SEC-01`, `TC-STACK-03`, `FR-SUB-01`, `FR-SUB-03`.

## [2026-07-03T11:49:00+03:00]

**Остання дія:** Виправлено всі знайдені прогалини в специфікації можливості `core-database` (файл `spec.md`) та оновлено список завдань у `tasks.md`.

**Статус:**
*   **Працює:** Специфікація та список завдань успішно доопрацьовані. Всі зауваження (F1–F5) усунені: впроваджено каскадне видалення, уточнено правила валідації довжини ключа шифрування, додано попередження щодо серіалізації BigInt, деталізовано обробку помилок дешифрування та додано поля суми/валюти підписок.
*   **В процесі:** Очікування рішення про старт фази кодування.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію бази даних та ORM (команда `/opsx:apply`).

**Примітки:**
*   Торкається вимог: `FR-API-02`, `FR-DASH-03`, `FR-AUTH-06`, `NFR-SEC-01`, `TC-STACK-03`, `FR-SUB-01`, `FR-SUB-03`.

## [2026-07-03T11:41:00+03:00]

**Остання дія:** Проведено повторний незалежний огляд специфікації можливості `core-database` за допомогою скіла `review-task` та сформовано звіт з рекомендаціями.

**Статус:**
*   **Працює:** Створено детальний аналітичний звіт про огляд специфікації з вердиктом `APPROVE_WITH_CHANGES` та оцінкою ризику `4`.
*   **В процесі:** Очікування на застосування рекомендацій до специфікації `spec.md` (додавання політики каскадного видалення, усунення неоднозначності валідації hex-ключа та вирішення серіалізації BigInt).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Оновити специфікацію `spec.md` згідно з рекомендаціями огляду (F1, F2, F3, F4, F5).
2. Після фіналізації специфікації розпочати реалізацію бази даних та ORM (команда `/opsx:apply`).

**Примітки:**
*   Торкається вимог: `FR-API-02`, `FR-DASH-03`, `FR-AUTH-06`, `NFR-SEC-01`, `TC-STACK-03`.

## [2026-07-03T11:15:00+03:00]

**Остання дія:** Проведено повторний огляд (review) специфікації можливості `core-database` за допомогою скіла `review-task`.

**Статус:**
*   **Працює:** Створено звіт про огляд специфікації з вердиктом `APPROVE_WITH_CHANGES`.
*   **В процесі:** Очікування на виправлення зауважень або на рішення про початок кодування.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Отримати погодження розробника на запропоновані покращення специфікації (додавання глосарію, уточнення форматів шифрування та хешування API-ключів, часових зон).
2. Застосувати зміни до специфікації `spec.md` або почати фазу кодування `core-database`.

**Примітки:**
*   Торкається вимог: `FR-API-02`, `FR-DASH-03`, `FR-AUTH-06`, `NFR-SEC-01`, `TC-STACK-03`.

## [2026-07-03T10:58:00+03:00]

**Остання дія:** Оновлено специфікацію `spec.md` — явно визначено поля CRM та телефонії (`crm_*`, `telephony_*`), а також `telegram_username` як `nullable` (необов'язкові).

**Статус:**
*   **Працює:** Специфікація повністю відображає необов'язковість налаштувань інтеграцій при реєстрації користувача.
*   **В процесі:** Очікування рішення про старт фази кодування (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Отримати остаточне погодження специфікацій та почати реалізацію можливості `core-database`.

**Примітки:**
*   Торкається вимог: `FR-AUTH-03`, `FR-DASH-01`.

## [2026-07-03T10:52:00+03:00]

**Остання дія:** Виправлено виявлені помилки та прогалини в специфікації (`spec.md`), дизайні (`design.md`) та списку завдань (`tasks.md`) можливості `core-database`.

**Статус:**
*   **Працює:** Оновлені специфікації та дизайн-документи, які тепер включають: поле `api_key_hash` для користувачів, розділення сутностей Magic Link та сесій (додано `magic_links`), поля ретраїв списань та `last_invoice_id` у підписках, `bigint` тип для Telegram ID, унікальність email та індекси для конверсій.
*   **В процесі:** Очікування повторного огляду специфікації або рішення про старт фази кодування (команда `/opsx:apply`).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити повторну перевірку специфікації для отримання статусу `APPROVE`.
2. Почати кодування схеми бази даних та сервісу шифрування.

**Примітки:**
*   Торкається вимог: `FR-API-02`, `FR-DASH-03`, `FR-SUB-05`, `FR-AUTH-06`, `NFR-PERF-02`.

## [2026-07-03T10:35:00+03:00]

**Остання дія:** Проведено незалежний огляд (review) специфікації можливості `core-database` за допомогою скіла `review-task`.

**Статус:**
*   **Працює:** Створено детальний звіт про огляд специфікації з вердиктом `REQUEST_CHANGES`.
*   **В процесі:** Очікується виправлення виявлених критичних зауважень у специфікації та архітектурному дизайні перед початком реалізації.
*   **Заблоковано:** Початок реалізації (coding phase) для `core-database` відкладено до оновлення та затвердження специфікації.

**Наступні кроки:**
1. Виправити критичні прогалини в специфікації (додати поле API-ключа користувача у `users` та поля для відстеження спроб повторних списань у `subscriptions`).
2. Внести покращення щодо безпеки сесій (розділення сесій та Magic Link) та типів даних (BigInt для `telegram_id`).
3. Додати опис необхідних індексів для таблиці `conversions`.
4. Запустити повторну перевірку специфікації.

**Примітки:**
*   Торкається вимог: `FR-API-02`, `FR-DASH-03`, `FR-SUB-05`, `FR-AUTH-06`, `NFR-PERF-02`.

## [2026-07-02T20:41:00+03:00]

**Остання дія:** Ініційовано OpenSpec-зміну `core-database` та створено планувальні артефакти (`proposal.md`, `design.md`, `specs/core-database/spec.md`, `tasks.md`).

**Статус:**
*   **Працює:** Планувальні артефакти для можливості `core-database` готові та повністю валідовані у системі OpenSpec.
*   **В процесі:** Очікування запуску процесу реалізації (implementation phase).
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Запустити реалізацію зміни `core-database` за допомогою команди `/opsx:apply` (або дати команду агенту розпочати кодування).
2. Створити та протестувати схеми баз даних (користувачі, сесії, підписки, конверсії).
3. Впровадити модуль симетричного шифрування AES-256-GCM для CRM-допусків та ключів телефонії.

**Примітки:**
*   Торкається вимог: `TC-STACK-03`, `NFR-SEC-01`, `NFR-COST-01`.

## [2026-07-02T20:15:00+03:00]

**Остання дія:** Розділено вимоги (`requirements.md`) на можливості OpenSpec (capabilities) та створено документ плану реалізації (`docs/capabilities.md`).

**Статус:**
*   **Працює:** Документ [capabilities.md](file:///d:/home/Documents/Developer/2026-fwdays-agentic-greenfield-task/docs/capabilities.md) успішно створений та містить детальний опис 7 можливостей, зв'язок з FR/NFR вимогами та пріоритезований порядок розробки.
*   **В процесі:** Підготовка до старту розробки першої можливості `core-database` за допомогою OpenSpec.
*   **Заблоковано:** Немає.

**Наступні кроки:**
1. Створити та узгодити першу OpenSpec-специфікацію для можливості `core-database` (налаштування БД, ORM Drizzle, шифрування та схеми даних).
2. Запустити перший робочий цикл OpenSpec для ініціалізації бази даних.

**Примітки:**
*   Пов'язані вимоги: `TC-STACK-03`, `NFR-SEC-01`, `NFR-COST-01`.
*   Розподіл виконано відповідно до логічних залежностей та пріоритетів MVP.
