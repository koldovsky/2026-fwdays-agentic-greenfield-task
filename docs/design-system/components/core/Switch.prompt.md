A calm pill toggle (e.g. the night-theme switch). Slides, never bounces.

```jsx
<Switch checked={dark} onChange={setDark} label="Нічна тема" />
```

Props: `checked`, `onChange(next)`, `label` (also the accessible name), `disabled`.
