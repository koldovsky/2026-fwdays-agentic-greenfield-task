The signature Bookshelf 1–10 score. Ten pips fill with a red→teal ramp.

```jsx
<Rating value={9} readOnly />
<Rating value={score} onChange={setScore} size="lg" />
```

Read-only by default; pass `onChange` to make it interactive. `showNumber` toggles the mono "/10".
