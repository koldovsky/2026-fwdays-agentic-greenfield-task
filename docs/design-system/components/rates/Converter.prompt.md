UAH ⇄ foreign converter at the official rate. Two tabular-mono fields with the rate shown between them; the swap control flips direction. Locale-aware input (accepts `100,50` and trailing zeros).

```jsx
<Converter code="USD" rate={41.85} defaultAmount="100" />
```

Props: `code`, `rate` (UAH per 1 unit), `defaultAmount`.
