A calm roundel identifying a currency. Shows the ISO code in mono by default; pass `flag` (an emoji) for a flag — the single sanctioned emoji in the system.

```jsx
<CurrencyAvatar code="USD" />
<CurrencyAvatar flag="🇺🇸" code="USD" size="lg" />
```

Props: `code`, `flag`, `size` (`sm`/`md`/`lg`).
