Outlined metadata chip — methodology (`360°`), channel (`web`), audience hints.

```jsx
<Chip>360°</Chip>
<Chip mono>telegram</Chip>
```

Always a 1px `--line-strong` outline on white, muted text, 11px, 6px radius. Never filled with color — that role belongs to `StatusBadge`.
