Native select, styled to match `Input`.

```jsx
<Select label="Relation" defaultValue="peer"
  options={["manager","peer","report","self","buddy"]} />
```

Accepts plain string options or `{value,label}` objects. Same border/radius/fill rules as `Input`.
