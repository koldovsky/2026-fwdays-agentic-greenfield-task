Compact toggle (34×20) — e.g. the "Required" flag in the template editor.

```jsx
<Switch checked={required} onChange={setRequired} />
```

Green `--green` when on, `--line-strong` when off; 14px white knob slides 120ms. No labels built in — pair with an external `<label>`.
