Multiline input — open-text answers in the respondent form, question text in the editor.

```jsx
<Textarea placeholder="Your answer…" rows={4} />
```

Same border/radius/font as `Input`; 12px padding, vertical-resize only.
