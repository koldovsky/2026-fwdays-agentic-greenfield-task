Thin progress/coverage bar — response progress, respondent form progress, cycle coverage.

```jsx
<ProgressBar value={5/6} />
```

4px tall, `--line` track, `--green` fill, 2px radius, animates width over 250ms. The only bar in the system; scores use `ScaleDots`.
