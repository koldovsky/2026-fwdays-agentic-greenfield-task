# Slice 004 (metrics) — review evidence

**Result: pass** (engineering-DONE; CodeRabbit pending the owner's PR, as for slices 001–003).

Slice 004 ran the full `/run-slice` loop to its 3-iteration cap and escalated with 3 open
`[BLOCKING]` performance findings (see `docs/agent-runs/016-metrics-run.md`). The owner then
authorized a targeted iteration-4 fix pass for exactly those 3 findings; this file records that
pass's independent review, on top of the 5 review passes the loop already ran.

## Iteration-4 fix (commit `2a7533f`) — the 3 escalated findings

Maker: `capability-implementer` (isolated context). Checkers: `code-reviewer` ‖ `security-reviewer`
(independent, parallel, fresh isolated read-only contexts). Judge: the orchestrator (did not author
the code). Maker ≠ checker ≠ judge held.

- **`code-reviewer` → PASS (0 BLOCKING, 1 optional MINOR).** Verified all three fixes are
  value-preserving: the memoized `full_totals` is the full-history split (not a pre-windowed
  subset), so `compute_baselines(..., yesterday, daily_totals=full_totals)` still reads day
  `today-30` correctly (§3.6); `active_days`-from-dict-keys is exact (`active_days ≡
  set(daily_net_minutes(...).keys())`); switching pre-bucketing is byte-identical (same per-day
  order, `compute_day_switch_load` sorts internally); shared per-category split identical; empty /
  midnight-split / DST / zero-week-category edge cases hold. MINOR: the new memoization test asserts
  call-counts, not value-equality — non-blocking, value-correctness is covered by the 20 unchanged
  `test_metrics_snapshot.py` assertions.
- **`security-reviewer` → PASS (0 BLOCKING, 1 pre-existing cross-slice MINOR).** `check-secrets`
  clean; the pause-count cap is at the single load-time choke point covering both `snapshot` and
  `heatmap`; skip-not-clip leaks/corrupts nothing; `1000` is defensible; per-user isolation intact
  (shared dicts are per-call locals, `None` defaults avoid aliasing, no module-level cache); no raw
  session rows in output. MINOR: `selectin` eager-loads a session's pause rows before the count cap
  can skip it — a weaker memory/IO vector, pre-existing and cross-slice (fix belongs in slice-003's
  session schema / a SQL-level exclusion), which this diff strictly improves.

## Orchestrator's independent verification (real output, not the maker's claim)

- Diff: `schemas/stats.py` **unchanged** (wire shape preserved for slice 005); acceptance suites
  `test_metrics_snapshot.py` / `test_stats_api.py` untouched; `test_metrics_extra.py` +194 / −0
  (purely additive, no `skip`/`xfail`/`assert True`/bare `raises` introduced).
- `python scripts/gate-slice --ratchet` → **146 passed**, coverage **97.47% ≥ floor 82%**; `ruff`
  clean; `mypy` clean (59 files).
- Finding-2 structural repro: `daily_net_minutes` call count **3** (was ~21) with the top-level
  snapshot keys + 4 baselines + `all_time_min` intact.

## Accepted non-blocking follow-ups

- (sec MINOR) bound pause-count at session creation (slice-003 schema) or exclude over-cap sessions
  at the SQL level — recorded in `docs/followups/metrics-snapshot-extension.md`.
- (code MINOR) optional value-equality assertion for the memoization test.
- The 1 pre-existing M2 spillover-day denominator MINOR needs an owner ruling, not a fix.
