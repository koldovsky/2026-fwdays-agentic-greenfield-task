# Slice 006 (coach) — review evidence

Result: pass — engineering-DONE (CodeRabbit pending the owner's PR, as for slices 001–005).

Slice 006 ran the full `/run-slice` maker≠checker≠judge loop and **converged in two iterations**:
test-first RED → implement to green → deterministic gate → independent parallel review → a
review-driven grounding-tightening rework → re-gate → trajectory-eval → Judge. Every role ran in a
fresh, isolated sub-agent context, so the reviews are genuinely independent of the author. (The spec
itself was authored + adversarially ratified beforehand by the `/author-slice` autopilot — see
[`docs/agent-runs/021-coach-spec-author.md`](../../agent-runs/021-coach-spec-author.md).)

## Independent review (fresh isolated contexts, parallel)

Maker: `capability-implementer`. Checkers: `code-reviewer` ‖ `security-reviewer` (independent,
parallel, read-only). Judge: the orchestrator (did not author the code). Maker ≠ checker ≠ judge held.

- **`code-reviewer` → PASS (0 BLOCKING, 3 MINOR — all closed red-first).** Verified the whole coach
  contract: the one provider ladder (grounding-violation → one corrective retry on `gemma-4-31b-it`;
  schema-invalid/malformed/transport/emoji/out-of-range → a single `Gemini 3 Flash` attempt; then the
  fallback card), the read-through insight cache (only a grounded non-fallback card is cached; a
  degraded card is returned but not cached → self-heals), both-or-neither chat persistence, 2-4/1-2
  count + quiet enforcement, server-side no-emoji, language forcing from `users.coach_language`,
  `trim_history` (20-turn cap **and** ~2,000-token budget), the `0004_coach` migration, async/error
  handling (every failure path returns the fallback card, never an uncaught 500), and per-user
  (`user_id`) scoping. It confirmed the three acceptance test files are genuine and **not weakened**.
  The 3 MINORs were all edges in the pure `app/core/grounding.py` validator: (1) a `uk`-locale
  comma-decimal fabrication passthrough — the one **false negative** in the CRITICAL anti-fabrication
  promise; (2) a share cited as bare/worded `43` (no `%` glyph) flagged despite the spec enumerating it
  grounded; (3) a ≥100 h `h:mm` rendering of `month_min`/`all_time_min` flagged. **All three were
  routed and fixed red-first at root cause** in the grounding rework (`f1403db`) — see below.
- **`security-reviewer` → PASS (0 BLOCKING, 2 MINOR).** `python scripts/check-secrets` clean; the
  Google AI key is read only via `get_settings().google_ai_api_key` (never hardcoded/logged/printed/
  committed; sent as the `x-goog-api-key` header, not the URL), and no key ever appears in git history
  (`git log --all -p | grep AIza` → none). Both `POST /api/coach/{insight,chat}` sit behind
  `CurrentUser` + the reused `require_csrf`; every coach repo method is `user_id`-scoped with
  server-derived `week_start` (no client-supplied ids → no IDOR); `user_message` is Pydantic-bounded
  (1–2000 chars, non-blank → 422) and all SQL is ORM/parameterized; the prompt carries only the
  aggregate snapshot (no raw `sessions`/`pause_segments` rows — FR-COACH-04 data minimization); the
  `0004_coach` migration is additive with cascading FKs. MINOR (1): no per-user rate limit on the coach
  endpoints against the single shared free-tier key — an availability/cost concern, not a data
  exposure (the coach degrades gracefully when quota is hit, so NFR-REL-01 holds); **accepted as an
  owner follow-up, out of this slice's scope** (no ratified requirement mandates rate limiting). MINOR
  (2): the external-transport error log used `exc_info=True` — no exposure today, but **fixed** in
  `f1403db` (logs `type(exc).__name__` only) as defense-in-depth.

## Trajectory-eval (`eval-judge` over `evals/rubrics/trajectory-quality.md`)

- **pass: true, score 95.** All five criteria met: test-first RED before green (run-record 022
  documents 33 tests failing for behavior-absent before any implementation), **no test
  weakened/skipped/deleted** to go green (the grounding rework only *added* red-first tests that
  strengthen the anti-fabrication contract and fixed root cause in product code), in scope (only the
  coach backend + its eval suite; config split to a correctly-traced infra commit; shared-file edits
  are the allowlisted entry-point appends), loop followed (an independent review caught a real
  anti-fabrication hole, fixed red-first at root cause rather than bypassed), honest reporting.

## Orchestrator's independent verification (real output, not the maker's claim)

- **Gate:** `python scripts/gate-slice` → **GREEN**, backend suite passes under coverage, coverage
  **97.91% ≥ floor 82%**; ruff/mypy strict clean; `alembic upgrade head` applies the one new
  `0004_coach` migration; frontend build + vitest unchanged (no frontend in this slice).
- **Traceability / trajectory / specs / eval-ratchet:** `check-traceability` **45 claimed / 45 traced
  / 0 gap** (FR-COACH-01..07 **COVERED**); `check-trajectory` **0 violations** (slice-006 code paths
  under `backend/app/{core,services,api,schemas,models,repos}`; the only shared edits are the
  allowlisted `main.py` / `models/__init__.py` appends; config landed as infra commit `e1739d7`);
  `check-specs` **0 violations**; `check-eval-ratchet` **5 programmatic dimensions hold at/above the
  baseline** (`grounding_pass_rate` 1.0, `grounding_violations` 0, `format_conformance` 1.0,
  `language_correct` 1.0, `no_emoji` 1.0).
- **Grounding is the CRITICAL promise, verified mechanical:** the `evals/cases/coach/e9-*` case's
  planted out-of-snapshot `180` is caught by the same `app/core/grounding.py` the runtime uses
  (`grounded=False`, `violations=['180']`); after the rework a fabricated `uk` `3,5` is caught, while a
  legitimate bare/worded percent and a ≥100 h `h:mm` rendering are grounded (no false positive).
- **No secret committed:** `backend/.env` is git-ignored; only the empty `GOOGLE_AI_API_KEY=`
  placeholder in `backend/.env.example` is committed.

## Accepted non-blocking follow-ups

- **(security MINOR) Per-user rate limiting** on the coach endpoints, to protect the shared free-tier
  key from a single user exhausting quota (NFR-COST-01). Out of this slice's scope (no ratified
  requirement); the coach already degrades gracefully on quota exhaustion.
- **(grounding, architecture §4.4 mechanism) Thousands-separator handling.** The grounding extractor
  follows architecture §4.4's defined token shapes (integers, decimals, percents, `h:mm`); a number
  written with an en-locale **thousands separator** (e.g. `"9,000"` for `all_time_min = 9000`) is not a
  defined token shape and is handled imperfectly — a pre-existing limitation of §4.4's mechanism
  (present before this slice's rework via the bare-digit-exemption path), which errs toward degrading
  to the safe fallback rather than surfacing a fabrication. A deliberate §4.4 + validator refinement
  (canonicalize separator-grouped numbers) is the right owner follow-up, not a rushed loop-tail patch.
- **(coverage) Live Google AI Studio transport unverified.** The real `_google_ai_provider` httpx call
  is `# pragma: no cover` and exercised only via the injected provider seam (NFR-COST-01: no live LLM
  call in the committed gate). Its behavior against the real API — and whether the doc-specified model
  ids `gemma-4-31b-it` / `Gemini 3 Flash` resolve there — is unverified here and should be smoke-tested
  by the owner with the real key before relying on live insights.

## Outstanding DoD sub-item

- **CodeRabbit** has not run (no PR yet) — the single remaining DoD item, identical to slices 001–005.
  Requirements FR-COACH-01..07 stay `proposed` until CodeRabbit is clean on the owner's PR, at which
  point they flip to `shipped` (and `check-trajectory` reports 006 fully done). The owner opens the PR;
  this loop does not push.
