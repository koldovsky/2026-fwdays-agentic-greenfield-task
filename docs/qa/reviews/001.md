# Slice 001 — review evidence (email + password authentication)

Result: pass
Blocker: none
Date: 2026-07-10 (Europe/Kyiv)

Independent review-and-close pass for slice 001. Maker != checker != judge held:
the reviewers and the Judge ran in fresh, isolated contexts and did not author the code.
This file is the clean review evidence `scripts/check-trajectory` requires before a
slice may be marked done.

## Traceability (deterministic gate)

- `scripts/check-traceability` — 8 claimed, 8 traced, 0 gap; matrix fresh (`--check-fresh` exit 0).
- All previously-GAP requirements now COVERED by `@trace` tests in
  `backend/tests/test_auth.py`: FR-AUTH-01, FR-AUTH-02, FR-AUTH-03, FR-AUTH-06,
  FR-AUTH-07, NFR-SEC-01, NFR-SEC-02, NFR-SEC-03. See `docs/qa/traceability.md`.

## Gate battery

- `scripts/gate-slice` — GREEN (exit 0): `scripts/verify.*` full battery
  (ruff, mypy, `alembic upgrade head`, pytest with `RUN_DB_TESTS=1` → 11 passed against
  real Postgres, frontend `npm run build`) plus the coverage ratchet at 82.97% >= floor 82.

## Independent Checker (maker != checker)

- **code-reviewer** — PASS-with-minors, 0 BLOCKING. Confirmed every acceptance check incl.
  failure/edge paths (duplicate → 409, wrong password → 401, missing/invalid/expired
  session → 401, CSRF-missing → 403, per-user isolation FR-AUTH-07), migration SQL matches
  the models (citext, IDENTITY, opaque token unique+indexed, ON DELETE CASCADE), and that
  the tests genuinely exercise the behavior (not vacuous/weakened).
- **security-reviewer** — PASS-with-minors, 0 BLOCKING. `scripts/check-secrets` clean (only
  `*.env.example` tracked). Verified bcrypt cost 12 via `passlib[bcrypt]` + per-hash salt;
  opaque 256-bit session token; cookie `HttpOnly; SameSite=None; Path=/` with env-driven
  `Secure`; server-side session with expiry enforced on read; new token per login (no
  fixation); logout deletes the row and clears the cookie; CSRF double-submit with
  constant-time compare on the mutating route; per-user-scoped repositories.

### MINOR follow-ups (accepted, non-blocking, defense-in-depth)

- bcrypt hash/verify run on the event loop — consider `run_in_threadpool`.
- password length bound is 72 characters vs bcrypt's 72-byte limit — validate by UTF-8 bytes.
- hardcoded dev `session_secret` default — fail fast outside development before it is consumed.
- `POST /register` and `/login` are CSRF-exempt by design (they bootstrap the token;
  architecture Sec.8.2) — documentation-only note.

## Trajectory eval (LLM judge — `evals/rubrics/trajectory-quality.md`)

- Verdict: pass = true, score 91.
- `no-test-weakened` (CRITICAL) — MET; no test weakened/skipped/deleted/narrowed (the only
  change to the tests this pass was adding `@trace` docstrings).
- `test-first-red-before-green` (CRITICAL) — **not assessable, grandfathered**: slice 001
  predates the test-first factory/harness and was recorded retroactively, so there is no
  RED-before-green git trail by construction. Not graded FAIL, not held against the verdict.
- `in-scope`, `loop-followed`, `honest-reporting` — MET.

## Judge — Definition of Done

- Verdict: **DONE (engineering-DONE)**. Spec acceptance checks MET (8/8 automated tests +
  the password-hash security check), verify/gate-slice green, independent Checker (maker !=
  checker) passed, trailers + `@trace` present, no secrets, conventions/docs updated.
- **Outstanding DoD sub-item:** CodeRabbit review, which runs on a pull request and could not
  run in this no-PR review-and-close pass. It is gated on opening the PR, not an engineering
  failure.
