# ~/.aws/credentials — a profile that exists only here (values never read)
[default]
aws_access_key_id = AKIAFAKE
aws_secret_access_key = fake

[ci-only]
aws_access_key_id = AKIAFAKE2
aws_secret_access_key = fake2
