On/off toggle for settings (dark mode, private device sync, finished-reading).

```jsx
<Switch label="Dark mode" defaultChecked />
<Switch label="Show ruled lines" checked={ruled} onChange={setRuled} />
```
