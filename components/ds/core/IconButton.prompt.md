Icon-only square button for toolbars and card actions.

```jsx
<IconButton label="Edit note"><Pencil size={16} /></IconButton>
<IconButton label="Bookmark" variant="solid"><Bookmark size={16} /></IconButton>
```

Variants `ghost | solid | outline`, sizes `sm | md | lg`. `active` highlights a toggled ghost. Always pass `label`.
