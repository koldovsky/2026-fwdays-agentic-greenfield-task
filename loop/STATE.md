# Loop STATE

Append-only spine for the spec-loop. Each gate transition adds a row, stamped Europe/Kyiv
(scripts/kyiv-now.sh). The loop reads this on startup to resume. Do not rewrite history — append.

## Legend
- stage: assess | plan | gate1-implement | gate2-verify | gate3-docs | archive | blocked
- resolution: done | no (failed, will retry) | in-progress | blocked (budget exhausted → triage)
- retry budget: 3 attempts per spec

## Run config (this run)
- Branch: **main** (user directive: commit each spec directly to main, no feature branch).
- Identity: remember-reach.
- Order: the roadmap DAG (`doc/plans/web-app-roadmap.md`), changes 1→11, strictly sequential.
- Dependency policy (user directive): on an audit/dep problem — (1) try an upgrade; (2) else find an
  alternative; (3) else security-check it; (4) only if a security check shows it's non-critical for our
  use, add an `audit-ci` ignore-until-fix entry. Never silence without that ladder.
- e2e video: recorded at the END as one consolidated demo (user directive) — per-spec Gate 2 still
  captures its own video/screenshots as evidence.
- Komga: `test/komga/` Docker stack available; `pnpm komga:up && pnpm komga:provision` before the
  Komga-dependent gates (changes 3, 4, 9, 10).
- Baseline (`loop/artifacts/_baseline`): static suite GREEN; audit clean (0 vulns); fallow maintainability
  89.1 with 13 pre-existing scaffold dead-code findings (delta-judge against this, don't block on it).

## Backlog summary
| # | spec | current stage | attempt | resolution |
|---|------|---------------|---------|------------|
| 1 | add-app-shell | archive | 1 | **done** (4f66425) |
| 2 | add-library-browse | archive | 1 | **done** (cec7b45) |
| 3 | add-connector-komga | archive | 2 | **done** (001c4bc) |
| 4 | add-source-flow | archive | 1 | **done** (1ce52fc) |
| 5 | add-book-detail | done | 2 | done (c49cbcd) |
| 6 | add-format-epub | done | 1 | **done** (b48ed4b) |
| 7 | add-reader-navigation | archive | 4→done | **done** (archived after 7b) — reader shipped ff8fec9 + secured by 7a; delta validation fixed (SHALL-before-blockquote), synced → `specs/reader-navigation` (9 reqs), 13 spec-vs-code spot-checks PASS. Commit below. |
| 8 | add-reading-preferences | done | 1 | **done** (8a99761) — themes/typeface/size/layout/spacing panel; fontFamily allowlist+clamp closed; live-apply across the isolation bridge; readium-css vendoring deferred (architect-ruled in-pattern). |
| 9 | add-offline-and-sync | done | 2 | **done** (988f304) — offline OPFS + per-connector furthest-wins sync; core server-free; Gate-2 caught+fixed a credential-launder HIGH (origin-gated download) + an OPFS cycle. |
| 10 | add-extensions-and-capability-install | archive | 2 | **done** (04c946f) — extensions/capability-install: dispatch+sniff-tiers+event-bus, screens 06/07, connector.opds (origin-gated), format.pdf (install-on-demand, in-app canvas). Gate-2 caught OPDS credential-laundering HIGH + fallow regression → fixed; re-verify interrupted by session-limit then PASS 3/3. |
| 11 | add-visual-polish-e2e | archive | 1 | **done** (79cf915) — capstone acceptance gate: visual-regression 2-tier harness + a11y/responsive gates + BottomNav + offline-happy-path e2e (connector-komga 406 + :5174 CORS fixed to prove it E2E). Gate-2 3/3; the Komga-EPUB mid-book-progress limitation surfaced + escalated (triage). |
| 7a | add-reader-origin-isolation | done | 1 | **done** (ff8fec9) — separate-origin reader; closed the credential HIGH; M1/L1 hardened; shipped the ch7 reader |
| 7b | harden-format-navigator-contract | archive | 1 | **done** (cd2079f) — neutral contract reshape; 3/3 gates green attempt 1; core/contracts now a DOM-free conformance target; `open(PublicationSource)` matches ch7 spec. |

## Log
### _setup
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 01:56:10 EEST | assess | baseline GREEN | static suite passes; audit clean; fallow 89.1 (13 pre-existing dead-code). 11 pending changes, video runner present. | in-progress |
| 2026-06-29 01:58 EEST | assess | branch ready | scaffold committed as baseline `3bec051` on main (user: commit to main, no feature branch). | in-progress |

### add-app-shell
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 02:11 EEST | plan | plan ready | 16 tasks across tokens/fonts/primitives/shell+routing/PWA/verify; deps to add: @fontsource/newsreader,@fontsource/literata,@vue/test-utils; e2e: shell→library→settings→full-bleed reader | in-progress |
| 2026-06-29 02:44 EEST | gate1-implement | tasks 1.1–5.1 [x] | maker(opus): @theme parchment tokens+self-hosted fonts, 11 primitives, AppShell+NavSidebar+routing(fullBleed switch), PWA manifest+sw.ts Range-bypass(ADR-005)+shouldBypassSW, 9 test files/41 cases. self-check green typecheck/lint/format/test/build. 5.2/5.3/5.4 left for checker. | in-progress |
| 2026-06-29 03:07 EEST | gate2-verify | PASS | checker(sonnet): static suite GREEN; +21 independent tests (62 total); e2e 6/6 + video; openspec validate --strict ✓; tasks 5.2/5.4 [x]. security(opus)=PASS(max Low); architecture(opus)=PASS(fit/dup clean). Orchestrator applied advisory hermeticity fix (extracted pwa-manifest.ts; unit test no longer reads dist/) + re-ran suite GREEN. 5.3 [x]. | in-progress |
| 2026-06-29 03:14 EEST | gate3-docs | PASS | documenter(sonnet): synced delta specs → openspec/specs/{app-shell,design-system}/spec.md; verified spec-vs-code; added README run section. No code change. | in-progress |
| 2026-06-29 03:16 EEST | archive | archived | `openspec archive add-app-shell --skip-specs` (Gate 3 already synced) → changes/archive/2026-06-29-add-app-shell. | done |
| 2026-06-29 03:16 EEST | commit | 4f66425 | one-spec commit on main: code+tests+synced specs+README+loop artifacts. | done |

### add-library-browse
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 03:27 EEST | plan | plan ready | fixture-first: core-domain-model (LibraryBrowseEntry/ProgressSnapshot/serialization, platform-neutral) + FixtureConnector (9 books vs doc/web/01) + LibraryView (keep-reading/recently/search/grid-list) + Pinia shallowRef store; e2e: library happy path | in-progress |
| 2026-06-29 03:57 EEST | gate1-implement | tasks 1.1–7.4 [x] (−7.3) | maker(opus): core-domain-model (platform-neutral LibraryBrowseEntry/ProgressSnapshot/serialization + Locator↔snapshot), FixtureConnector+LocalProgressStrategy (9 books), LibraryView guard + LibraryBrowse content via Pinia Colada useQuery, shallowRef connector store, KeepReading/RecentlyCover cards. self-check green: typecheck/lint/format/test 89/89/build/e2e 4/4/validate strict. change-1 checker preserved. 7.3 → checker. | in-progress |
| 2026-06-29 04:00 EEST | gate2-verify | INTERRUPTED | first verifier(sonnet) hit a session limit mid-run (did static suite + independent checker test + e2e, but no verdict/copy/reviews). Resumed by orchestrator. | no |
| 2026-06-29 04:45 EEST | gate2-verify | PASS | RESUMED: re-ran static suite → caught lint FAIL (checker test in src/core/ imported plugins/app, violating no-restricted-paths) → root-cause fix = moved test to src/app/ → GREEN (124/124). fallow config fix (ignore loop/** generated JS) → MI 89.1→92.5, 1 moderate advisory (LibraryBrowse.vue 21.8). e2e 4/4 (exit0)+video. validate strict ✓. security(opus)=PASS(max Info); architecture(opus)=PASS(platform-neutrality grep-clean). 7.3 [x]. | in-progress |
| 2026-06-29 04:48 EEST | gate3-docs | PASS | documenter(sonnet): synced delta specs → openspec/specs/{core-domain-model,library-browse}/spec.md; verified spec-vs-code; README updated. No code change. | in-progress |
| 2026-06-29 04:52 EEST | archive | archived | `openspec archive add-library-browse --skip-specs` → changes/archive/2026-06-29-add-library-browse. | done |
| 2026-06-29 04:52 EEST | commit | cec7b45 | one-spec commit on main. Loop housekeeping: gitignore _playwright scratch + fallow ignore loop/**. | done |

### add-connector-komga
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 05:04 EEST | plan | plan ready | HostBridge (fetch seam) + PermissionEnforcingHttpClient + PluginRegistry (installed/available/resolve, semver, dynamic-import-install) + KomgaConnector (browse/probe/content/thumbnail vs live Komga) + minimal _opds-core; unit + skipIf integration tests. Komga UP at :25600 (reader creds, Test EPUBs seeded). | in-progress |
| 2026-06-29 05:36 EEST | gate1-implement | tasks 1.1–5.4 [x] (−5.3) | maker(opus): HostBridge+PermissionEnforcingHttpClient(platform/web)+InMemoryKVStore; PluginRegistry rewrite (lazy load+cache, persisted enabled set, 3-state resolve, semver gate)+catalog in src/app; KomgaConnector(browse/probe/content+client-slice/thumbnail/search/page) vs LIVE Komga; minimal _opds-core (pure). Real-server findings: /file ignores Range→client-slice on 200; ?search title-only→test "Pens"; PSE 500s on reflowable. self-check green: typecheck/lint/format/build/validate; test 188 (23 files), integration 7/7 LIVE + skip-not-pass verified. core stays fetch/DOM-free (injected factories). main.ts NOT wired to registry yet (ch4). | in-progress |
| 2026-06-29 05:57 EEST | gate2-verify | FAIL (attempt 1) | checker(sonnet) + security(opus)=PASS(max Low) + architecture(opus)=PASS ALL converged on ONE surgical defect: `src/platform/web/index.ts:103` storage-key separator is a raw NUL byte (0x00), not a space → `git diff HEAD` = "Binary files differ" (unreviewable; defeats diff-based maker≠checker), and prettier/eslint pass a NUL in a template literal (hard gate blind to it). Everything else GREEN: static 211 tests, integration 7/7 live + skip-not-pass, 23 independent checker tests, fallow improved, validate ✓. → Gate 1 attempt 2 (one-line fix; also closes sec M1/L2 delimiter injectivity). | no |
| 2026-06-29 05:58 EEST | gate1-implement (attempt 2) | fix applied | maker(sonnet): replaced the raw NUL with documented `KEY_SEPARATOR='\x1f'` (Unit Separator — textual escape, collision-safe ⇒ injective namespacing). No other change. self-check green 211 tests. | in-progress |
| 2026-06-29 06:02 EEST | gate2-verify (attempt 2) | PASS | RE-VERIFY: `git diff` now textual (165+/7-); repo-wide scan = zero raw control bytes; static suite GREEN (24 files/211 tests, 0 skipped); integration live + skip-not-pass (carried); fallow improved; security/arch PASS (the 1-line fix closed their delimiter-injectivity note). 5.3 [x]. | in-progress |
| 2026-06-29 06:06 EEST | gate3-docs | PASS | documenter(sonnet): synced delta specs → openspec/specs/{host-bridge,plugin-registry,connector-komga}/spec.md; verified spec-vs-code; README updated; test/komga/README already accurate. No code change. | in-progress |
| 2026-06-29 06:08 EEST | archive | archived | `openspec archive add-connector-komga --skip-specs` → changes/archive/2026-06-29-add-connector-komga. | done |
| 2026-06-29 06:08 EEST | commit | 001c4bc | one-spec commit on main (2 attempts: NUL-byte FAIL → fix). | done |

### add-source-flow
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 06:09 EEST | plan | plan ready | server-prober (platform-neutral, probe via HostBridge) + komga/opds probe fns + AddSourceModal (screen 05 `<dialog>`: stepper/detect/caps/sign-in) + sourcesStore (creds in SEPARATE localStorage key, never in syncable SourceRecord) + wire registry into main.ts; unit+integration+e2e vs live Komga. | in-progress |
| 2026-06-29 07:10 EEST | gate1-implement | tasks 1.1–6.4 [x] (−6.3) | maker(opus): platform-neutral server-prober (confidence+specificity ranking) + komga/opds probe fns; AddSourceModal native `<dialog>` (screen 05) + AddSourceView + store-driven NavSidebar; sourcesStore credential boundary (creds in edda.creds.<id>, NOT in syncable SourceRecord; sourceId minted once); registry wired into main.ts (OPDS enabled fallback, fixture fallback). Honest additive: KOMGA_CAPABILITIES.protocols→['opds2','komga-rest'] (chip derives from caps; updated 2 ch3 assertions). self-check green: typecheck/lint/format/build/validate; test 252/30 files, integration 3/3 LIVE, e2e 2/2 vs live Komga (Connect→Library shows Pensées). Maker recreated komga container w/ CORS (host concern; data survived). 6.3 → checker. | in-progress |
| 2026-06-29 07:23 EEST | gate2-verify | PASS | checker(sonnet): static GREEN (286 tests/31 files; integration 3/3 LIVE; no control bytes; all 13 diffs textual); fallow improved (0 dead files); 34/34 independent tests incl. 5 credential-boundary (no secret in SourceRecord; creds in distinct key) + 5 prober-neutrality; e2e 2/2 + video vs live Komga (probe→connect→Pensées) matching doc/web/05; validate ✓. security(opus)=PASS(no High); architecture(opus)=PASS incl. explicit OK on the KOMGA_CAPABILITIES additive edit (truthful, no spec delta). 6.3 [x]. | in-progress |
| 2026-06-29 07:26 EEST | gate3-docs | PASS | documenter(sonnet) synced delta specs → openspec/specs/{add-source,server-prober}/spec.md (cut off before README); orchestrator finished README "Add-a-source" entry + gate3 notes + spec-vs-code spot-check (corroborated by Gate-2). No code change. | in-progress |
| 2026-06-29 07:28 EEST | archive | archived | `openspec archive add-source-flow --skip-specs` → changes/archive/2026-06-29-add-source-flow. | done |
| 2026-06-29 07:28 EEST | commit | 1ce52fc | one-spec commit on main. | done |

### add-book-detail
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 07:38 EEST | plan | plan ready | screen 02: additive BookMeta + Connector.getBook(ref) (data from connector, NOT EPUB parse=ch6); fixture+Komga getBook; BookDetailView (header/cover/actions/progress/synced-per-format/chapters placeholder); Library card→RouterLink; reader CTA→placeholder route (ch7). unit+integration(Komga)+e2e. | in-progress |
| 2026-06-29 10:14 EEST | gate1-implement | tasks 1.1–7.4 [x] (−6.2,−7.3) | maker(opus): additive BookMeta + optional `Connector.getBook?` (kept change-2/3 test doubles untouched); model gains `LocatorLocations.minutesLeft`, Locator/Snapshot `lastReadAt/lastReadDevice` (ISO, not Date); fixture+Komga getBook (Komga shape curl-verified, no progress/breadcrumb call=ch9); BookDetailView + 5 book-detail components; LibraryBrowse cards→RouterLink; mediaType decoded from query. self-check green: typecheck/lint/format/test 301/301(+live Komga 8)/build/e2e 3/3/validate strict; control bytes clean. **Maker touched prior checker test** `gate2-checker-add-source.test.ts:344` (`as unknown as` cast, claims pre-existing HEAD typecheck fail) → Gate 2 must independently confirm. 6.2 deferred (needs real TOC=ch6); 7.3=checker. | in-progress |
| 2026-06-29 10:30 EEST | gate2-verify | FAIL (arch blocker) | 2 of 3 PASS, 1 blocking. **verifier(sonnet)=PASS**: verify-gate exit 0, static 310/310 (+ own checker test `src/app/book-detail-gate2-checker.test.ts` 9/9: BookMeta JSON-neutral, getBook from connector NOT EPUB-parse, progress no-bleed across mediaType, SYNCED verbatim), live Komga integ ran (not skipped), e2e 3/3 + video, fallow 91.6 (flat). **Cast claim VERIFIED genuine**: `ConnectorCapabilities.protocols` union never had `'kavita-native'` (git show 1ce52fc), uncast literal predates this spec, contracts diff doesn't touch the type → cast is safe unblocking, maker didn't mask its own break. **security(opus)=PASS** (max Low: coverColor :style allowlist; Info: bookId path seg). **architecture(opus)=FAIL**: `Connector.getBook?` must be REQUIRED per DESIGN §5.1 — sole caller treats absence as hard error; repo idiom is required-method+no-op (cf progressStrategy→localOnly); BookRef always has title so every connector (incl OpdsConnector) can return minimal `{title,authors:[]}`; the `?`-gap is fictional. Remedy ~3 edits: drop `?`; OpdsConnector.getBook minimal (PROD code); 1-line getBook stub in registry.test.ts + gate2-checker-komga.test.ts. → attempt 2. | no |
| 2026-06-29 10:37 EEST | gate1-implement (attempt 2) | arch remedy applied | maker(sonnet): surgical fix for the lone gate2 blocker. `Connector.getBook?`→`getBook` REQUIRED (contracts); `OpdsConnector.getBook` added (minimal honest `{title:ref.title,authors:[]}`); 1-line getBook stub in the two inline Connector doubles (registry.test.ts fakeConnector, gate2-checker-komga.test.ts:208); BookDetailView reconciled (dropped now-dead `!active.getBook` branch, calls `active.getBook(ref)` directly, keeps failed-query error state). Did NOT touch the verified-genuine `as unknown as` cast. verify-gate exit 0 (typecheck/lint/format/test 310/build); no suppressions; control bytes clean; openspec valid. Orchestrator: also gitignored `.playwright-mcp/` (maker prettierignored it; was untracked → would pollute the commit). → re-verify (architecture + independent static green). | in-progress |
| 2026-06-29 10:43 EEST | gate2-verify (attempt 2) | PASS | re-verifier(sonnet, ≠ fix-maker), proportionate: re-ran the FAILED dimension + independent static green; carried security + functional/e2e PASS from attempt 1 (isolated contract fix doesn't touch render/credential surface or view runtime). verify-gate exit 0, 310 tests/33 files, live Komga integ ran (getBook on Pensées), checker test 9/9. Blocker RESOLVED: `getBook` required (contracts:82, no `?`), all 5 implementors satisfy it (Komga/Fixture unchanged, OpdsConnector honest-minimal opds/index.ts:116, 2 doubles stubbed), typecheck = compiler proof of total coverage. Caller error state intact (BookDetailView `if(!active) reject` + v-else EmptyState; only the dead `!active.getBook` branch removed). Hygiene: no new suppressions, cast unchanged, control bytes clean, .playwright-mcp gitignored, openspec valid, core no new web imports. → Gate 3. | in-progress |
| 2026-06-29 10:51 EEST | gate3-docs | PASS | documenter(sonnet): synced delta → NEW `openspec/specs/book-detail/spec.md` (7 reqs: header/two-col/identity/progress-card/per-format/actions/chapters-placeholder); UPDATED `core-domain-model` (+BookMeta req w/ getBook REQUIRED, Locator minutesLeft/lastReadAt/lastReadDevice scenarios) + `connector-komga` (+Fetch book-detail metadata req). 8/8 spec-vs-code spot-checks PASS (no contradiction). README "Book detail screen" entry added. openspec validate --specs 10/10, --changes 7/7, format:check PASS. No code change. | in-progress |
| 2026-06-29 10:51 EEST | archive | archived | `openspec archive add-book-detail --skip-specs --yes` (Gate 3 already synced) → changes/archive/2026-06-29-add-book-detail. 20/22 tasks [x]; 2 deferred-by-design: 6.2 (real TOC ▸/row-activation → ch6) + 7.3 (independent review — performed by the Gate-2 verifier, not box-ticked pre-archive). | done |
| 2026-06-29 10:51 EEST | commit | c49cbcd | one-spec commit on main (author remember-reach). code+tests+synced specs+archived change+README+loop artifacts; .playwright-mcp/ gitignored. Spec 5/11 DONE (2 attempts; arch gate caught getBook optionality). | done |

### add-reader-navigation
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 12:55 EEST | plan | plan ready; **PAUSED for user decision** | planner(sonnet, read-only): 8-step plan in `loop/artifacts/.../plan/plan.md`. Wires EPUB into the live registry (app-layer: plugins-catalog/app-registry, reuse EPUB_MANIFEST) + `WebFormatHandler` type-guard (platform/web, NOT instanceof) + `useNavigator` composable (pub/nav as plain closure vars, markRaw, destroy/unsubscribe on unmount, cancelled-flag guard) + ReaderView/5 reader components matching doc/web/03 + no-op SyncEngine/prefs stubs + e2e. **BLOCKING FINDING: iframe-isolation is a SPEC GAP.** ch7 first mounts untrusted EPUB HTML/JS in foliate's `allow-same-origin allow-scripts` iframe on the app origin where `edda.creds.<id>` lives → crafted EPUB can exfiltrate creds via `window.parent.localStorage` → ch6 Medium escalates to **HIGH**; no spec doc specifies CSP/sandbox/separate-origin. Gate-2 security cannot pass no-HIGH bar as-specified → surfaced to user (AskUserQuestion) for the isolation strategy before Gate 1. | in-progress |
| 2026-06-29 13:01 EEST | decision | **user chose Option A (harden in-app) + ADR** | User directive: mitigate the iframe credential-exfil risk in-app (no separate-origin re-spec, no accept-and-defer). Orchestrator verified vendored foliate: it sets `sandbox='allow-same-origin allow-scripts'` (paginator.js:244, fixed-layout.js:86; allow-scripts is only foliate's WebKit event-bug workaround #218086) and **injects NO `<script>` into the content frame** — loads the spine doc via `iframe.src` (same-origin blob URL) + manipulates via parent-side `contentDocument`. ⇒ Refined mechanism: **inject strict CSP (`script-src 'none'; connect-src 'none'`) into each spine document** (kills EPUB-embedded JS + egress = the exfil vector) rather than stripping `allow-scripts` (would break Safari/iOS events). foliate rendering/pagination survives (no foliate scripts run in-frame). Implementer adds this as an in-scope task + an ADR in stack.md; Gate-2 security must confirm it closes the HIGH; e2e confirms real-browser rendering survives. → Gate 1. | in-progress |
| 2026-06-29 13:56 EEST | gate1-implement | tasks 1.1–6.3,S.1–S.3,7.1/7.2/7.4 [x] (−7.3) | maker(opus): real ReaderView + 5 reader components (TopBar/Spread/Chevrons/PositionBar/TocDrawer) matching doc/web/03; `useNavigator` (pub/nav as plain closure vars + markRaw, cancelled-guard on async open, destroy+unsubscribe once, locator→`enqueue` never `setProgress`); `WebFormatHandler`+`isWebFormatHandler` type-guard (platform/web, NOT instanceof); EPUB registered via ch6 EPUB_MANIFEST/loadEpubFormat enabled-at-boot (plugins-catalog/app-registry); noopSyncEngine (ch9) + readerPrefs stub (ch8); fixture connector serves an epub. **Security (ADR-012, Option A): injected per-document CSP** (`default-src 'none'; script-src 'none'; connect-src 'none'; base-uri/form-action 'none'; img/style/font/media allowed`) as first `<head>` child at parse time in shared `loadText` (book-loader.ts) before foliate's blob URL; **load-bearing fix**: render routes `view.open(book)` w/ pre-built book (not `view.open(file)` which rebuilt foliate's loaders & bypassed the CSP); allow-scripts kept. **Proven both ways (e2e)**: benign EPUB paginates; XSS-probe EPUB (`<script>`+`<img onerror>` writing `parent.localStorage`) renders but localStorage untouched. Delta spec +requirement/2 scenarios; ADR-012 in stack.md. **Maker touched prior tests** (`src/app/gate2-checker.test.ts` +102, `e2e/app-shell.spec.ts`) claiming placeholder-reader assertions obsolete (ch7 replaces the placeholder) → Gate 2 MUST confirm legitimate, not masking a regression. self-check GREEN: typecheck/lint/format/test 362/build(foliate code-split)/reader e2e 2/2/full e2e 19/19/validate strict. NOTE: re-running full e2e regenerated 3 prior-specs' committed screenshots (hardcoded SHOTS paths, ch6 antipattern) → orchestrator reverts at commit. 7.3→checker. | no |
| 2026-06-29 14:10 EEST | gate2-verify | **FAIL (attempt 1)** — security residual HIGH | 2/3 PASS, 1 blocking HIGH. **verifier(sonnet,≠maker)=PASS**: static exit 0 (41 files/369 tests; +7 independent checker tests gate2-reader-checker.test.ts); **prior-test edits LEGITIMATE** (git-diffed vs b48ed4b: old asserted placeholder *echoed raw props*; new asserts real reader via stub connector — strengthened, not weakened); e2e 8/8 + videos; markRaw non-corruption + destroy-once + enqueue-not-setProgress + {kind:'bytes'} + core-neutrality(0 DOM hits) + code-split all VERIFIED; no suppressions; fallow dead-export 14.3%→9.9%. **architecture(opus)=PASS**: WebFormatHandler placed right (platform/web, structural guard), registry reuses ch6 manifest, **CSP loader DE-duplicated** (openFoliateBook MOVED to shared book-loader.ts; parse+render both flow the one loadText→injectCSP seam). **security(opus)=FAIL (residual HIGH H1)**: `injectContentSecurityPolicy` gates on `<html>` root (`IS_HTML_DOCUMENT` regex, early-return) → an **SVG-root EPUB spine doc gets NO CSP**, loads `image/svg+xml` blob into the allow-same-origin+allow-scripts iframe, its `<script>` reads `window.parent.localStorage`(`edda.creds`) + `fetch()` egress (connect-src unrestricted). **No media-type gating anywhere** (to-publication hardcodes mediaType) → a malicious *reflowable* EPUB ships an SVG spine doc; precondition == original HIGH, delta = "use `<svg>` root". M1 Med: `<script>` before first `<head>` runs before meta-CSP. Metadata sinks CLEAN (Vue-escaped, verified); maker's e2e probe genuine but XHTML-only. → Gate 1 attempt 2: media-type-INDEPENDENT active-content sanitization (strip `<script>`/`on*`/`javascript:` from EVERY framed doc incl. SVG, parser-based not regex) + meta-CSP as defense-in-depth + extend negative e2e (SVG/pre-head/`javascript:`/egress). | no |
| 2026-06-29 14:57 EEST | gate1-implement (attempt 2) | security fix applied | maker(opus), surgical (8 files; protected reader/composable/registry untouched, mtime-verified): rewrote `content-security.ts` to **media-type-independent parser-based sanitization** — `detectRootElementName()` (prolog/comment/DOCTYPE/ns-aware, NOT a `<html` regex) classifies root: `html`/`svg`→sanitize, OPF/NCX/container/CSS/JS→**byte-identical**. Parse (correct MIME + foliate-mirroring fallbacks)→strip every `<script>` (HTML+SVG, any ns/case/**position**=closes M1), every `on*`, every `javascript:`/`data:text/html`/`vbscript:` URL (entity-decoded, so `&#106;avascript:`/`java\tscript:` caught)→re-serialize. **SVG = strip-only** (no meta-CSP) = closes H1; HTML keeps meta-CSP as defense-in-depth. CFI/render unaffected (foliate already DOMParser→serializes every doc; scripts carry no body text). +18 unit cases; updated 2 assertions in the verifier's gate2-reader-checker.test.ts that **encoded the bug** (`toContain('onerror=')`)→"active content REMOVED" (re-verify must confirm legit, not weakened); extended e2e (SVG H1 both-ways: control fires fetch→attacker.test; hardened→XSS_SVG null + ZERO attacker requests via page.on('request'); pre-head/inline/on* neutralized; happy path intact). ADR-012 rewritten (SVG "residual"→closed); delta spec generalized. No suppressions (control chars→explicit Unicode escapes). self-check GREEN: typecheck/lint/format/test 378(41 files)/build/reader e2e 3/3/validate strict. → re-verify (security + verifier; architecture PASS carries). | no |
| 2026-06-29 15:51 EEST | gate2-verify (attempt 3) | **FAIL (attempt 3)** — security HIGH-1; **BUDGET EXHAUSTED → user decision** | 2/3 PASS, 1 blocking. **verifier(sonnet)=PASS**: static 393 tests/43 files; e2e 4/4 (happy + 5-spine XSS probe + H1-SVG both-ways + H2-section both-ways); CFI/parse no-regression; foliate code-split; display_options byte-identity guard CONFIRMED (real+necessary); scope surgical (6 files); BOTH prior checker tests intact (7+4, untrimmed); +4 fresh independent tests; no suppressions/control bytes. **architecture PASS carried**. **security(opus)=FAIL (HIGH-1, ~9/10, demonstrated via foliate real path)**: H1/M1/H2(section/body/bare-script) CLOSED, but the gate still keys passthrough on **root element** — a spine item declared `application/xhtml+xml` with a forged **control-vocab root** (`<package><script>…`) → classifier passthrough (root∈set) → foliate frames by media-type, downgrades to text/html (epub.js:817-822), executes → reads `edda.creds.*` + egress. PoC = rename existing fixture `<section>`→`<package>`. Maker's "accepted residual" is NOT acceptable (real HIGH). **Definitive root cause (3rd FAIL of the class): classify by RESOURCE ROLE/PATH or media-type, NEVER root element** — passthrough only `#loadXML` targets (container/OPF/NCX/encryption/signatures/display-options/SMIL by path), ALWAYS sanitize anything framed via loadItem/loadReplaced (media-type {xhtml,html,svg}); reviewer: "breaks nothing". Attempt 4 > BUDGET=3 → orchestrator does NOT silently loop; **surfaced to user** (authorize 1 root-caused attempt / Option B separate-origin / BLOCK+triage). | no |
| 2026-06-29 15:54 EEST | decision | **user authorized 1 root-caused attempt (budget extension)** | User chose Option 1 (deliberate engineer call, not silent looping): apply the definitive root-cause fix. **Discriminator must be resource ROLE/PATH or MEDIA-TYPE, never root element** — and media-type is forgery-PROOF because foliate uses the *same* manifest media-type to decide iframing, so the attacker can't make foliate iframe-execute a doc our sanitizer skips. Plan: thread the resource name/href (and/or media-type) into `injectContentSecurityPolicy` (via book-loader.ts loadText); passthrough only #loadXML control docs by path/role (container.xml, OPF, NCX, encryption, signatures, the 2 display-options, SMIL) + non-markup; **sanitize everything foliate frames** (media-type {xhtml,html,svg}); root element only sub-selects html-vs-svg sanitizer, never whether to sanitize. → Gate 1 attempt 4. | in-progress |
| 2026-06-29 16:29 EEST | gate1-implement (attempt 4) | definitive fix: media-type discriminator | maker(opus), surgical (7 files; reader/composable/navigator + 3 checker tests untouched, mtime-verified): `injectContentSecurityPolicy(source, mediaType?)` now decides skip-vs-sanitize by **manifest media-type** — sanitize iff ∈ `FRAMED_DOCUMENT_MEDIA_TYPES {application/xhtml+xml,text/html,image/svg+xml}` (the EXACT field foliate's loadReplaced keys on, epub.js:814 incl. malformed-xhtml→text/html downgrade) → **forgery-proof** (skip-decision & frame-decision read one signal; root element only sub-selects html-vs-svg sanitizer, never whether). `book-loader.ts` pre-parses container.xml→OPF `<manifest>` into resolvedHref→media-type map (verbatim copy of foliate's `resolveURL`=zero drift), threads `mediaTypes.get(name)??null` into loadText; bootText-cached (no double getData, ADR-005 holds); pre-parse failure ⇒ foliate's #loadXML throws identically (no exploit window). Root dispatch DEMOTED to no-media-type fallback (unreachable on render path; kept for the 3 checker tests' 1-arg calls); PASSTHROUGH_CONTROL_ROOTS→CONTROL_VOCABULARY_ROOTS. **PoC CLOSED**: e2e Test B (7 spine docs incl forged-`<package>` xhtml+html) + new Test E both-ways (raw `<package>` runs+egresses→hardened null+0 egress); 5/5 e2e. unit 33(+8): 9 control-vocab roots sanitized-when-framed / byte-identical-as-control-doc; display_options guard holds. ADR-012 residual CLOSED (+ honest note: resources foliate frames WITHOUT loadText = foliate-internal limitation, not this class → security must scrutinize). self-check GREEN: typecheck/lint/format/test 401(43 files)/build/e2e 5/5/validate strict; no suppressions. → FINAL re-verify (security primary + verifier + architect on book-loader OPF-pre-parse). | no |
| 2026-06-29 16:43 EEST | gate2-verify (attempt 4) | **FAIL (attempt 4) — 3 NEW HIGHs across 2 reviewers; in-app approach leaking 4×** | 1/3 PASS. **verifier(sonnet)=PASS** functionally (405 tests/44 files; e2e 5/5 incl. forged-`<package>` Test E closed; +4 fresh tests) — BUT its probes use canonical media-types/in-`<manifest>` items, so they DON'T cover the poisoning vectors. **architecture(opus)=FAIL (HIGH)**: map CONSTRUCTION drifts from foliate — `book-loader.ts:69` `getElementsByTagNameNS('*','item')` (any `<item>`, any ns, first-wins) vs foliate's strict direct-child `<manifest>`/direct-child `<item>`/NS.OPF/by-id (epub.js:80-92,637-644,763-781) → a rogue `<item href="ch1.xhtml" media-type="text/css"/>` OUTSIDE `<manifest>` poisons our map→skip while foliate frames+executes. **security(opus)=FAIL (2 HIGH)**: **HIGH-1** (conf9) media-type-STRING forgery — foliate routes to our loadText seam by STRICT STRING equality (epub.js:778) but the browser frames by MIME ESSENCE; `media-type="text/HTML"`/`; charset=utf-8`/`Image/SVG+XML`/leading-space fails foliate's `shouldReplace`→takes the **loadBlob path which NEVER calls loadText** (epub.js:783-784)→re-blobs raw bytes→iframe executes, our CSP/sanitizer never ran (no second seam; book-loader.ts:120-123 loadBlob returns raw). **HIGH-2** (conf8) duplicate-href first-wins (book-loader.ts:74) masks a framed dup. ALL same meta-cause: an in-app sanitizer must perfectly mirror foliate's routing (loadText vs loadBlob) + browser MIME handling (essence vs strict-string) + manifest node-selection + duplicates — diverged 4 ways across 4 attempts. Architect: design *idea* sound but mirror unsound; security: "SAME signal" claim FALSE. resolveURL copy + canonical-decl closures (H1/H2/prior-HIGH-1) + control-doc byte-identity all still hold. Advisory: buildManifestMediaTypes CRAP 31.6, detectRootElementName cyclo, resolveResourceHref .href-vs-URL benign. **Past the user-authorized +1 → orchestrator surfaces to user: recommend Option B (separate-origin/credential isolation — robust regardless of foliate routing) over a 5th in-app patch.** | no |
| 2026-06-29 16:56 EEST | decision / **BLOCKED** | user chose **Option B: re-architect for origin isolation** | After 4 in-app attempts (3 closed findings, 3 still-open HIGHs across 2 reviewers — loadBlob seam bypass / MIME-essence vs strict-string / manifest-mirror drift / duplicate-href), user chose to stop in-app sanitization and isolate book content so it physically cannot reach `edda.creds.*` regardless of foliate routing. **This is a DESIGN task, OUTSIDE spec-loop scope** (skill: "consumes ready changes; never invents scope; designing = /opsx:propose"). foliate needs same-origin `contentDocument` to paginate ⇒ isolation means running the ENGINE inside a sandboxed cross-origin frame + postMessage bridge (CFI/preferences/locator events cross the bridge) — a substantial re-architecture warranting its own proposed change (+ explore: foliate-compat, perf, bridge protocol). **ch7 BLOCKED**; in-app `content-security.ts`/`book-loader.ts` CSP approach to be SUPERSEDED by the isolation change. Functional reader (ReaderView/useNavigator/components/registry/WebFormatHandler/navigation/markRaw) is sound + green and **preserved UNCOMMITTED in the working tree** (will be reused; only the isolation mechanism changes). Loop PAUSED (run config = strict 1→11; reader-dependent ch8/9/11 must not stack on a blocked reader). → hand back to user: propose/explore the origin-isolation change, then resume the loop to implement it. | blocked |
| 2026-06-29 15:40 EEST | gate1-implement (attempt 3, LAST) | H2 fix: gate inverted to fail-safe | maker(opus), surgical (6 files; reader/composable/navigator/book-loader + BOTH checker tests untouched, mtime-verified): inverted `injectContentSecurityPolicy` dispatch to **default-deny** — `root===null`→passthrough (non-markup); root∈control-vocab→passthrough; `svg`→sanitizeSvg; **ELSE→sanitizeHtml** (covers html/body/section/math/unknown/bare-script). Closes H2 (non-html/svg-root spine docs now sanitized). **Deliberate +1 deviation flagged: added 9th passthrough root `display_options`** (Apple/Kobo META-INF) — foliate `#loadXML` strict-XML-parses it & THROWS on the HTML-wrapped output, so sanitizing it would break every EPUB shipping one; it fits passthrough (parsed-not-iframed). L2: meta-refresh `javascript:`/`data:` neutralized (entity-decoded). +7 unit cases (section/body/bare-script/math sanitized; full control vocab byte-identical; meta-refresh); e2e Test B (5-doc probe incl. section-root xhtml + body-root text/html) + Test D (H2 both-ways control: raw section text/html runs+egresses→hardened null+0 egress); happy path intact. ADR-012: fail-safe dispatch + **control-root-forgery stated as ACCEPTED residual**. self-check GREEN: typecheck/lint/format/test 389(42 files)/build/reader e2e 4/4/validate strict; no suppressions/control bytes. → FINAL re-verify (security: is control-root-forgery exploitable via foliate's real path? + verifier green/no-regression; arch carries). | in-progress |
| 2026-06-29 15:11 EEST | gate2-verify (attempt 2) | **FAIL (attempt 2)** — NEW security HIGH (H2) | 2/3 PASS, 1 blocking. **verifier(sonnet,≠maker)=PASS**: static exit 0 (382 tests/42 files; +4 fresh independent tests gate2-attempt2-verifier.test.ts); all 3 e2e vectors neutralized (inline/pre-head M1/SVG H1 both-ways w/ control arm) + happy path intact; CFI round-trip + parse no-regression; foliate still code-split; checker-test edits LEGITIMATE (assert active-content-REMOVED, not weakened) — minor note: test-count delta suggests maker trimmed somewhat more from the checker file than the "2 assertions" stated, but coverage solid (18 content-security + 4 fresh + untouched markRaw/sync). architecture PASS carried (logic within approved seam). **security(opus)=FAIL (NEW HIGH H2)**: H1+M1 CLOSED ✓, but the fix's gate is **allowlist-to-SANITIZE** (`{html,svg}`→sanitize, *everything else→passthrough*) — wrong polarity. foliate loads-into-scripting-iframe by **manifest media-type** and **downgrades malformed `application/xhtml+xml`→`text/html`** (epub.js:817-822). Exploit: spine doc declared `application/xhtml+xml` with a non-html/svg root (`<section>`/`<body>`/bare `<script>`) → classifier passthrough → foliate HTML-parses+executes in same-origin sandbox → reads `edda.creds.*` + `fetch()` egress. Same impact as H1, trivial delta. L2 Low: meta-refresh URL not neutralized. → **Gate 1 attempt 3 (LAST budgeted)**: INVERT the gate to allowlist-to-**passthrough** (passthrough only `detectRootElementName===null` + EPUB control vocab {package,ncx,container,smil,encryption,signatures,metadata,manifest}; svg→sanitizeSvg; **everything else→sanitizeHtml**) + fold in L2 meta-refresh strip + probe tests for non-html/svg-root spine docs in both media-type declarations. | no |

### add-format-epub
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 11:28 EEST | assess | resumed; baseline re-confirmed GREEN | New session resumed the loop at spec 6/11. Re-ran verify-gate → static suite GREEN (typecheck/lint/format/test/build), audit clean. Preflight: GitHub reachable (foliate-js HEAD `78914aef4466eb960965702401634c2cb348e9b1` = pin candidate), npm reachable (`@readium/shared@2.2.3`), both test-epubs/ fixtures present. No `.gitmodules` yet; **no Vitest browser mode** → task 5.3 should use Playwright (`@playwright/test` installed), not add `@vitest/browser`. Komga NOT needed (headless format change). | in-progress |
| 2026-06-29 11:39 EEST | plan | plan ready | planner(sonnet, read-only): 7-step plan in `loop/artifacts/.../plan/plan.md`. submodule add foliate-js @ pin 78914ae + checkout; @readium/shared already in package.json (verify). Files: src/plugins/formats/epub/{index,manifest,bytes-source,cfi-locator,navigator}.ts + epub.parse/cfi-locator tests + e2e/epub-navigator.spec.ts. Do NOT edit core/contracts (richer surface = additive class members; flag for orchestrator). 5.3 via Playwright page.route(). Top risk: foliate API surface unknown until submodule read FIRST. e2e: Pensées open→nav→goTo→currentLocation→applyPreferences→destroy. | in-progress |
| 2026-06-29 12:24 EEST | gate1-implement | tasks 1.1–6.2,7.1,7.2,7.4 [x] (−7.3) | maker(opus): foliate-js submodule pinned (gitlink 78914ae, textual diff) + .gitmodules + vite alias; EpubFormatHandler (capabilities/sniff/open/parse/createNavigator) across index/capabilities/manifest/bytes-source/to-publication/cfi-locator/navigator + foliate-js.d.ts + README; 25 new tests (epub.parse/cfi-locator/bytes-source/to-publication) + e2e/epub-navigator.spec.ts (both fixtures). **Read real foliate API**: EPUB takes {loadText,loadBlob,getSize} loader+await init(); zip via vendored zip.js BlobReader→ranged Blob.slice; metadata messy (langmap/author variants) normalized defensively; epubcfi.fromRange/toRange jsdom-safe; spine idx=relocate.detail.section.current; bare spine CFI won't anchor→goTo by href for section starts; view.js customElements.define at eval→imported only in browser nav path. core/contracts UNTOUCHED (richer surface = additive class members). self-check GREEN: typecheck/lint/format/test 335 (37 files)/build/e2e 2 passed (Pensées+14MB)/validate strict; **code-split PROVEN** (probe build: foliate in epub/zip/epubcfi/view chunks, entry engine-free; +static no-static-import test). Triage flagged: registry wiring=ch7 (out of scope); foliate pdf.js glob→build WARN (harmless on epub path). 7.3→checker. | in-progress |
| 2026-06-29 12:35 EEST | gate2-verify | PASS | 3/3 reviewers PASS. **verifier(sonnet,≠maker)=PASS**: verify-gate exit 0 (38 files/345 tests; +10 independent checker tests in gate2-checker.test.ts written w/o maker helpers); ALL acceptance criteria re-derived (exact fixture metadata, sniff +/−, CFI round-trip identity both fixtures, Locator plain+no @readium instance leak+Object.prototype, isReactive=false, code-split via binary-grep of shell entry, `git diff HEAD -- src/core/` EMPTY); e2e 2/2 both fixtures + video/trace in gate2/playwright/; validate strict; fallow improved (dead-export 14.3%→10.1%, unused @readium flag cleared). **security(opus)=PASS** (no High): M1 Med = inherited foliate allow-same-origin+allow-scripts iframe trust boundary (foliate design; headless here, our code doesn't worsen) → hard-gate+ADR at ch7; L1 Low = navigator.ts:63 raw fontFamily interpolation (first-party). **architecture(opus)=PASS**: core untouched (grep+diff), exact contract conformance (EpubFormatHandler implements FormatHandler, EpubNavigator extends Navigator, EPUB_MANIFEST mirrors KOMGA_MANIFEST), no sig dup; NOT editing core/contracts is correct (ch5 fictional-optionality failure does NOT recur); recommends ch7 add `WebFormatHandler extends FormatHandler` + type-guard narrowing (not instanceof), promote neutral members then. Orchestrator wrote findings.md (verifier returned in-message). → Gate 3. | in-progress |
| 2026-06-29 12:44 EEST | gate3-docs | PASS | documenter(haiku): synced delta → NEW `openspec/specs/format-epub/spec.md` (7 reqs / 15 scenarios); `openspec validate --specs` 11 passed + `add-format-epub --strict` valid; 8 spec-vs-code spot-checks all consistent (capabilities/sniff/client-parse/bytes-source/CFI-adapter/Navigator/markRaw) — no divergence, no code fix needed; README EPUB section added incl. `git submodule update --init` note for fresh clones; src/plugins/formats/epub/README.md confirmed accurate (submodule update path). format:check PASS. Zero code changes. | in-progress |
| 2026-06-29 12:44 EEST | archive | archived | `openspec archive add-format-epub --skip-specs --yes` (Gate 3 already synced) → changes/archive/2026-06-29-add-format-epub. 21/22 tasks [x]; lone unchecked 7.3 = independent-review task, performed by the Gate-2 verifier (not box-ticked pre-archive), per the ch5 precedent. | done |
| 2026-06-29 12:44 EEST | commit | b48ed4b | one-spec commit on main (author remember-reach): foliate-js submodule (gitlink 160000) + .gitmodules + vite alias + src/plugins/formats/epub/* (code+tests incl. verifier's gate2-checker.test.ts) + e2e + synced format-epub spec + README + archived change + loop artifacts. Housekeeping: gitignored regenerable Playwright html-report viewer bundle (canonical webm/trace/png kept). .claude skills/agents infra deliberately left uncommitted (separate from spec work). Spec 6/11 DONE (1 attempt, all gates green). |

### _resume — origin-isolation pivot
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 19:05 EEST | assess | resumed; tree statically GREEN | New session resumed at the ch7 blocker. `verify-gate.sh` on the current (ch7-reader) tree exit 0 (typecheck/lint/format/test/build). Backlog gained a NEW change `harden-format-navigator-contract` (0/26): contract cleanup — `parse(bytes)`→`open(source:PublicationSource)`, enrich neutral `Navigator`, move `ReadingPreferences`→`core/model`, retype `useNavigator`. It explicitly does NOT do the security isolation (Non-Goals: no ADR-012/CSP/foliate change) and is entangled with the uncommitted ch7 reader (edits useNavigator/web-format-handler/book-loader — files absent from HEAD). | in-progress |
| 2026-06-29 19:05 EEST | decision | **user chose "origin-isolation first"** | Pivot (AskUserQuestion): before harden-format, design+propose the origin-isolation re-architecture (ch7's real blocker) via /opsx:explore→/opsx:propose, implement it, then harden the contract on top. Designing is OUT of pure spec-loop scope (consumes ready changes) → orchestrator runs the propose workflow, then resumes the loop to implement. Research underway: modern-web-guidance + 3 Explore agents (foliate drive-from-outside surface; ch7 reader reuse/replace map; contracts+ADR constraints). | in-progress |
| 2026-06-29 19:17 EEST | spike | **isolation mechanism PROVEN (browser, 2 origins)** | Playwright spike (`scratchpad/sandbox-spike`): (1) opaque `sandbox="allow-scripts"` frame on the APP origin → `parent.localStorage` BLOCKED (creds isolated ✓) BUT foliate's nested `allow-same-origin` spine iframe → `contentDocument`=**NULL** (pagination BREAKS ✗) because nested sandbox forces a fresh opaque origin. (2) A **separate served origin** (2nd port) → creds BLOCKED ✓ AND spine `contentDocument` ACCESSIBLE ✓ AND a real `event.origin` the bridge can validate. **⇒ Decision: the reader MUST run on a separate origin (NOT opaque-sandbox).** Foliate's same-origin `contentDocument` pagination is the constraint that rules out the no-infra option. Deployment consequence (self-hosters need a 2nd origin: port/subdomain) → surface at proposal review. → /opsx:propose `add-reader-origin-isolation`. | in-progress |
| 2026-06-29 19:26 EEST | propose | **`add-reader-origin-isolation` created + validated** | /opsx:propose → proposal/design/specs/tasks; `openspec validate --strict` ✓ (4/4 artifacts). New capability `reader-isolation`: render untrusted EPUB on a SEPARATE ORIGIN behind a MessageChannel postMessage bridge (strict `event.origin`); book bytes as a Transferable ArrayBuffer (no in-frame network, ADR-005); app-side navigator PROXY (markRaw, teardown-once); DELETE `content-security.ts` + `book-loader` CSP (ADR-012 superseded → ADR-013). Reuses ch7 reader chrome unchanged. Tasks: **spike-first** (foliate on 2nd origin, both fixtures) → frame harness → app proxy → wire → delete sanitizer → tests/e2e (XSS-probe executes-but-can't-steal) → docs → maker≠checker. **Deployment cost: self-hosters need a 2nd origin** → surfaced to user for go/no-go before implementing. | in-progress |
| 2026-06-29 21:11 EEST | plan | plan ready (attempt 1) | planner(sonnet, read-only) file-verified → `loop/artifacts/add-reader-origin-isolation/attempt-1/plan/plan.md`. **User chose IMPLEMENT.** Spike-first (REAL Vite foliate chunk on a 2nd origin, both fixtures) → frame harness → app proxy (MessageChannel, fail-closed, port-capability) → wire → DELETE 6 sanitizer files + simplify book-loader → unit/conformance/e2e (XSS-probe BLOCKED both-ways) → docs(Gate3) → checker(Gate2). Corrections folded: 5 reader components; port `event.origin===''` (validate at handshake, capability after); parse-before-transfer + `.slice(0)`; jsdom fail-closed (update `src/app/gate2-checker.test.ts`); 2nd-port playwright webServer + `dev:frame`/`build:frame`. → Gate 1. | in-progress |
| 2026-06-29 21:57 EEST | gate1-implement | tasks 1.1–6.4,8.2 [x] (−7.x docs→Gate3, −8.1 review→Gate2) | maker(opus): **SPIKE GREEN — kill-switch risk did NOT fire**: real bundled foliate on :5174 driven from :5173 over the real bridge → spine `contentDocument` non-null 62×/57× (null 0×; instrumented `HTMLIFrameElement.prototype.contentDocument` since foliate's shadow root is closed), in-frame `parent.localStorage`→SecurityError, ranged `Blob.slice` 24×/71×, both fixtures paginate (evidence JSON). Built: reader-frame entry (`vite.frame.config.ts` + `src/platform/web/reader-frame/{harness,protocol,index.html}` — zip/loader MINUS CSP; `toLocator`+`buildReadingCss` in-frame), `navigator-proxy.ts` (fail-closed throw, init `targetOrigin=READER_ORIGIN`, port-capability, markRaw-safe, transfers a buffer copy), `index.ts#createNavigator`→proxy, `navigator.ts`→interface-only (−188), `useNavigator` parse-before-transfer. DELETED `content-security.ts` + 5 sanitizer tests; simplified `book-loader`. Rewrote `e2e/reader.spec.ts` (Test A paginate via :5174 iframe; Test B XSS BLOCKED both-ways) + 2nd playwright webServer + `dev:frame`/`build:frame`. self-check GREEN: typecheck/lint/format/test 367/build(app+dist-frame)/e2e A+B/validate strict; prod build fail-closed verified. Deviations: D5-alt parse-in-app (renderer chunks frame-only, ADR-009 holds); in-flight cmds resolve-not-reject on teardown. → Gate 2 (3 concurrent checkers). | in-progress |
| 2026-06-29 22:16 EEST | gate2-verify | **PASS (3/3)** + orchestrator M1/L1 hardening | All 3 concurrent checkers PASS. **verifier(sonnet,≠maker)**: verify-gate exit 0 (377 tests, app+`dist-frame`); 10 independent tests (fail-closed, `targetOrigin` handshake, foreign-origin dropped, no-creds-seeded, markRaw non-reactive, teardown idempotent); e2e A+B + video; invariants — `git diff HEAD src/core/`=EMPTY, 6 sanitizer files GONE + 0 `injectContentSecurityPolicy` refs, foliate RENDERER code-split frame-only (`dist-frame/` never `dist/`), bytes bypass SW; `gate2-checker.test.ts` edit legitimate; fallow 91.1 (4 'unused'=frame entries fallow can't see). **architecture(opus)**: clean layer fit; `protocol.ts` SHARED app↔frame (not dup); single `openFoliateBook` (identical chunk hash, not dup); `toLocator`/`buildReadingCss` MOVED in-frame; D5-alt code-split verified; resolve-not-reject sound. **security(opus)=PASS (no HIGH)**: credential-exfil class closed at SOP level (cross-origin iframe, NO `allow-same-origin`, no creds in payload, `targetOrigin`-pinned, fail-closed); 4 prior vectors moot. Finding **M1 Medium** — app kept a window `message` listener ⇒ a book script (same-origin to reader origin) could `window.top.postMessage` a forged bridge event past the origin check (bounded: spoofed reading-position→sync@ch9, NOT creds/XSS); +L1 advisory. **Orchestrator applied M1+L1 NOW** (change purpose=robust isolation; user pref): removed the window listener (port-capability only — book can't reach the port), added reader≠app-origin fail-closed guard; flipped the 2 window-message tests (maker's + verifier's) to pin the seam CLOSED; refined the delta spec (port-only bridge + same-origin fail-closed scenarios). Re-verified GREEN: 19 targeted + full hard suite + e2e A+B + validate strict + format:check. → Gate 3 docs. | in-progress |
| 2026-06-29 22:26 EEST | gate3-docs | PASS | documenter(sonnet): synced delta → NEW `openspec/specs/reader-isolation/spec.md` (4 reqs incl. port-only bridge + same-origin fail-closed); ADR-013 added to stack.md (spike evidence) + ADR-012 marked SUPERSEDED (text retained); DESIGN.md §5.2 + §11 split (§11.1 active origin-isolation / §11.2 future plugin sandbox); architecture.md reader-frame/ dir note; README 'Reader origin' section (VITE_READER_ORIGIN, dev :5174, prod port/subdomain, fail-closed, nginx snippet); tasks 7.1-7.3 [x]; 8 spot-checks PASS. validate strict + format:check PASS; zero code changes. | in-progress |
| 2026-06-29 22:26 EEST | archive | archived | `openspec archive add-reader-origin-isolation --skip-specs --yes` → changes/archive/2026-06-29-add-reader-origin-isolation. 22/23 [x]; lone 8.1 (independent review) performed by the Gate-2 checkers, not box-ticked — ch5/ch6 precedent. | done |
| 2026-06-29 22:26 EEST | commit | ff8fec9 | one commit on main (remember-reach), 167 files +6055/−312: isolation change + the previously-blocked ch7 reader it secures (entangled) + docs + synced reader-isolation spec + archived change + ch7 delta reconcile + loop artifacts. Hygiene: reverted prior-spec screenshot churn (hardcoded-SHOTS); excluded .claude/ infra + the pending harden-format-navigator-contract proposal. **ch7 reader UNBLOCKED + SHIPPED; credential-exfil HIGH closed by construction.** | done |
| 2026-06-29 23:22 EEST | pause | **loop PAUSED by user after the ch7-isolation milestone** | User chose "pause here" after 7a shipped. Tree green; HEAD ff8fec9 on main; working tree = ONLY `.claude/` infra + the pending `harden-format-navigator-contract` proposal (both intentionally uncommitted). Un-drained backlog, ready to resume: ch7 archive (follow-up) · harden-format-navigator-contract (now UN-entangled — reader committed) · ch8 reading-preferences · ch9 offline-and-sync · ch10 extensions · ch11 visual-polish-e2e. Resume via /spec-loop (reads this STATE). | in-progress | done |

### _resume — drain backlog (harden → ch7-archive → 8–11)
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 23:26 EEST | assess | resumed; baseline GREEN | New session resumed after the ch7-isolation pause. Fresh `verify-gate.sh loop/artifacts/_baseline` exit 0 (typecheck/lint/format/test/build; audit clean; fallow flags pre-existing). HEAD ff8fec9 on main, ahead 8 (no push). Working tree = `.claude/` infra + the harden proposal (both intentionally uncommitted). Backlog: harden-format-navigator-contract (0/26), add-reader-navigation archive (25/26 — only 7.3 independent-review left, performed by Gate-2), ch8/9/10/11. Playwright runner present → per-spec video evidence OK; consolidated demo offered at end. | in-progress |
| 2026-06-29 23:29 EEST | decision | **order: harden (7b) BEFORE ch7 archive** | Orchestrator call. ch7's reader-navigation delta spec already carries FORWARD-LOOKING contract wording ("asking format-epub to **open** a `PublicationSource`") that the SHIPPED code does NOT satisfy: `src/core/contracts/index.ts:93` still `parse(bytes)`, `:99` `currentLocation()`; `useNavigator.ts:113` calls `handler.parse(bytes)` + imports `@/plugins/formats/epub/{navigator,bytes-source}` (the app→plugin coupling); `ReadingPreferences` still in the plugin. harden reshapes exactly that (`open(source)`, `currentLocator()`, retype useNavigator to neutral/WebNavigator, move ReadingPreferences→core/model). Running harden FIRST makes the code match ch7's spec ⇒ ch7 archives with NO spec-vs-code mismatch (Gate 3 won't sync unbuilt behavior). Inverts the nominal 7→7b order for the ARCHIVE bookkeeping only — ch7 CODE already shipped (ff8fec9). harden must also precede ch8/ch9 per its own proposal ("last cheap moment, before reading-preferences→applyPreferences & offline-and-sync→on('locatorChanged')"). | in-progress |

### harden-format-navigator-contract
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-29 23:41 EEST | plan | plan ready (attempt 1) | planner(sonnet, read-only), file-verified → `loop/artifacts/harden-format-navigator-contract/attempt-1/plan/plan.md`. Bottom-up 8-step (tree green at each step). **Caught the post-ADR-013 re-targeting design.md missed**: `openFoliateBook` now in `book-loader.ts` with TWO callers — `index.ts` (headless parse) + `reader-frame/harness.ts` (render) → the `parse→open` signature change is ATOMIC across both (harness `new File(...)`→`publicationSourceFromBytes`). Spike-first = `PublicationSourceReader`→zip.js Reader (size set in `init`, not returned), proven on both fixtures + ranged-read spy (14MB never fully buffered). 14 acceptance criteria; reuse e2e Test A (:5174 frame); 5 risks. Docs (tasks 6.x) deferred to Gate 3. → Gate 1. | in-progress |
| 2026-06-30 00:40 EEST | gate2-verify | **PASS (3/3)** | All 3 concurrent checkers PASS. **verifier(sonnet,≠maker)**: verify-gate exit 0 (typecheck/lint/format/test/build); **388 tests/44 files, 0 skipped** (+21 own independent tests → 409/46); fallow NET-IMPROVED (dead-export 11.4%→10.9%, `WebFormatHandler` re-export now live; no regression). Independent confirmations from a DIFFERENT angle: comment-stripped DOM-token scan (core/contracts+core/model DOM-free, HTMLElement only in platform/web), both-fixture `open()` Publication-shape parity (bytes==file source), independent ranged-read spy on the BYTES path (maxRead≪14MB, ADR-005), transfer-buffer detachability incl. subarray/byteOffset-bug-stays-fixed + `structuredClone(transfer)` neuters a plain ArrayBuffer (not SharedAB). Prior checker-test edits = pure contract renames, **all 6 origin-isolation security asserts preserved** (fail-closed unset/same-origin, targetOrigin≠'*', foreign-origin drop, port-only, idempotent teardown) — zero coverage loss. e2e 2/2 fresh-servers (:5173/:5174 cleared first): Test A open→next→ArrowRight→← Library teardown (iframe→0, no `loaded` event, 0 console errors) + Test B in-frame XSS denied (SecurityError, STOLEN=null) + video/trace. **security(opus)=PASS** (no High+; max Low = pre-existing reader-css fontFamily, triaged→ch8; byte-path = sized detachable COPY, targetOrigin pinned, no allow-same-origin, loaded-removal app-internal/no forged-msg seam, reads clamped no-OOB). **architecture(opus)=PASS**: D1 line drawn exactly (every core/** hit is comment prose, 0 DOM type usages; HTMLElement ×1 = createNavigator mount), genuine native conformance target, zip duck-type verified vs pinned 78914ae (no Reader export ⇒ `extends` impossible; init-guard+readable getter faithful), NET DE-DUP (drainStream ×1; SniffInput/NavigatorOptions/ReadingPreferences/Unsubscribe single homes), PDF stub appropriate ripple, layering improved. Triage (non-blocking): plugins-catalog.ts keeps EPUB_MANIFEST/loadEpubFormat import (intentional registry seam, pre-existing, not nav-type coupling — spec intent satisfied); 2 complexity advisories (readable getter double-cast; sourceToTransferBuffer reads-twice). Doc-lag (DESIGN.md §5.2 createNavigator/parse) → Gate 3. → Gate 3 docs. | in-progress |
| 2026-06-30 00:52 EEST | gate3-docs | PASS | documenter(sonnet, model-override for the load-bearing DESIGN.md rewrite): synced delta specs → `openspec/specs/core-domain-model/spec.md` (+ReadingPreferences vocabulary req) + `openspec/specs/format-epub/spec.md` (parse→open(source), PublicationSource neutral random-access, createNavigator→platform/web, +next/prev/seek, currentLocator, loaded dropped, +Neutral-Navigator-with-platform-factory req). Rewrote `DESIGN.md` §5.2 per D9 (removed HTMLElement+createNavigator from the NEUTRAL interface, **caught open() still wrongly taking `host: HostBridge`** + fixed, +PublicationSource, +Web-extension subsection, ADR-013 proxy=WebNavigator); `architecture.md` (platform/web + epub/ notes); plugin README (open(source) + 3 source-builders table). **8/8 spec-vs-code spot-checks PASS** (file:line). `openspec validate --strict` PASS; `openspec validate --specs` PASS; `pnpm format:check` PASS. tasks 6.1/6.2/6.3/7.3 [x]. No code change. | in-progress |
| 2026-06-30 00:52 EEST | archive | archived | `openspec archive harden-format-navigator-contract --skip-specs --yes` → `changes/archive/2026-06-29-harden-format-navigator-contract` (UTC date). 25/26 [x]; lone 7.2 (independent review) performed by the Gate-2 checkers, not box-ticked — ch5/ch6/7a precedent. Pre-commit hygiene: no foreign screenshot churn (verifier captured to harden gate2/playwright/ only), format:check GREEN tree-wide, 49 files staged, `.claude/` infra excluded. | in-progress |
| 2026-06-30 00:52 EEST | commit | cd2079f | one-spec commit on main (author remember-reach), 49 files +2414/−346: contract reshape (core/contracts+core/model+platform/web+epub plugin, navigator.ts+bytes-source.ts deleted, PDF stub conformed) + maker/verifier tests + synced format-epub & core-domain-model specs + DESIGN.md §5.2/architecture.md/README + archived change + loop artifacts. `.claude/` infra deliberately uncommitted (loop tooling, separate from spec work). **Spec 7b DONE (1 attempt, all 3 gates green).** Contract now matches ch7's `open(PublicationSource)` spec wording ⇒ ch7 archive unblocked. | done |
| 2026-06-30 00:26 EEST | gate1-implement | tasks 1.1–5.5,7.1 [x] (−6.x docs→Gate3, −7.2 review→Gate2) | maker(opus): neutral contract reshape under the origin-isolated reader. `ReadingPreferences`→core/model; `core/contracts` gains `PublicationSource`/`SniffInput`/`NavigatorOptions`/`Unsubscribe`/`publicationSourceFromBytes`, enriched `Navigator` (next/prev/seek/applyPreferences/typed on/destroy), `open(source)` replaces `parse(bytes)`, `currentLocation`→`currentLocator`; `platform/web` `WebNavigator`/`WebFormatHandler` + `publicationSourceFromFile`/`...FromStream` (HTMLElement appears ONCE). **Spike PASS + caught 2 real bugs pre-merge**: zip.js (pinned 78914ae) exports NO `Reader` base → duck-typed `PublicationSourceReader` (identical contract + replicated `readable` getter); `subarray` views broke zip.js byteOffset-ignoring DataViews → `.slice()` copies. Ranged-read spy: 14.3MB fixture, maxRead 11.9KB ≪ 14MB, totalRead 14.8KB (ADR-005 holds). Deleted `navigator.ts`+`bytes-source.ts`; dropped `loaded` event+sticky replay; retyped `useNavigator` (app→plugin nav coupling GONE); `harness.ts`/`protocol.ts` re-targeted. Transfer buffer via `new ArrayBuffer(n)`+`.set()` (guaranteed-detachable, no shared-AB type assertion). **Prior checker/test files touched (Gate 2 verify legit)**: gate2-checker, gate2-origin-isolation-checker (security asserts intact), registry.test, cfi-locator.test — mechanical contract renames. **Triage**: PDF stub conformed to new contract (design-anticipated ripple, green-tree necessity, not new feature). Self-check GREEN: typecheck/lint/format/test **388(44 files)**/build(app+dist-frame)/e2e 2/2/validate strict. e2e caveat: stale Vite dev server held deleted modules → killed :5173/:5174, fresh re-run passed (env artifact). Orchestrator sanity: navigator.ts+bytes-source.ts deleted ✓, all diffs textual, 0 control bytes, 23 files +490/−290. → Gate 2 (3 concurrent checkers). | in-progress |

### add-reader-navigation (archive — follow-up after 7b)
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-30 01:02 EEST | gate3-docs (archive-sync) | PASS | documenter(sonnet, ≠ orchestrator — independent spec-vs-code check): reader CODE already shipped (ff8fec9) + verified ×2 (7a Gate2 + harden Gate2 e2e); contract now matches (post-cd2079f `handler.open(publicationSourceFromBytes)`/`currentLocator()`). **Fixed delta validation FAIL**: the `> SUPERSEDED` blockquote sat between `### Requirement: Untrusted book content is isolated…` and its normative SHALL → parser saw no SHALL; moved SHALL paragraph first, supersession note trailing (kept as POINTER to reader-isolation, no dead in-app CSP). Synced delta → NEW `openspec/specs/reader-navigation/spec.md` (9 reqs). Also fixed `reader-isolation/spec.md` Purpose `> **Purpose:**`→`## Purpose` (validator needs H2). `validate add-reader-navigation --strict` PASS; `validate --specs` 13/13 PASS. **13 spec-vs-code spot-checks PASS** (open(PublicationSource) now true; locatorChanged→sync intake not direct write; markRaw+destroy on the WebNavigator proxy; isolation=separate-origin fail-closed; top-bar/spread/chevrons/keyboard/position-bar vs ReaderView+components). format:check PASS. No code change. | in-progress |
| 2026-06-30 01:02 EEST | archive | archived | `openspec archive add-reader-navigation --skip-specs --yes` → `changes/archive/2026-06-29-add-reader-navigation`. 25/26 [x]; lone 7.3 (independent review) performed by Gate-2 (7a+harden), per precedent. No foreign churn; format:check GREEN. → commit. | done |
| 2026-06-30 01:02 EEST | commit | 23f1f59 | docs commit on main (no src/ change): synced `reader-navigation` main spec + delta validation fix + reader-isolation Purpose fix + archived change + archive-sync notes. **ch7 fully closed.** Roadmap 1–7 + 7a + 7b all DONE; ahead 10. → ch8 add-reading-preferences. | done |

### add-reading-preferences (ch8)
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-30 01:14 EEST | plan | plan ready (attempt 1) | planner(sonnet, read-only), foundation-audited → `loop/artifacts/add-reading-preferences/attempt-1/plan/plan.md`. 9 steps on harden's foundation (consume `readerPreferencesStore` stub + the operational applyPreferences bridge; close the `useNavigator` prefs-on-open gap). **Highest-risk = Step 3 reader-css.ts SECURITY**: fontFamily/typeface allowlist + fontSize/lineHeight clamp (closes the recurring SEC note ch8 OWNS — a non-allowlisted fontFamily must NOT reach output CSS). DisplayPanel.vue per maket (4 themes/3 typefaces/size slider/Paged-Scroll/3 spacing); localStorage persistence (`edda.reading-preferences`); live apply via `structuredClone(toRaw())` (ADR-001) across the isolation bridge. 13 ACs; e2e restyles the :5174 frame + proves persistence on reopen. Orchestrator note to maker: harden's legacy prefs fields have no real consumers → prefer a clean single vocabulary over parallel legacy+new fields (architect adjudicates). → Gate 1. | in-progress |
| 2026-06-30 01:41 EEST | gate1-implement | tasks 1.1–4.2,4.4 [x] (−4.3 review→Gate2; no docs task) | maker(opus): 9 steps. **Field shape = ONE clean vocabulary** (theme/typeface/textSizePt/layout/spacing/rtl; REMOVED harden's legacy fontFamily/fontSize/scroll/lineHeight — ch8 design mandates this, no legacy-compat clause; no dual-representation). **Security (reader-css.ts as the bridge trust boundary)**: THEME_COLORS/TYPEFACE_STACKS/ALLOWED_LEGACY_FAMILIES (`Object.hasOwn` exact-match → proto keys miss), clampPt[10,40]/clampLH[1,3], numeric `Number()`-coerce (string inj→NaN→default); injection suite proves 6 vectors (`expression()`, `'; color:red }`, `url(javascript:)`, numeric `24; }body{display:none}`…) ABSENT from output. **ADR-001**: `structuredClone(toRaw())` once in the applyPreferences wrapper (on-open + live), unit-proven isReactive===false. DisplayPanel.vue (Tailwind bottom sheet per maket, 4 themes/3 typefaces/pt slider/Paged-Scroll/3 spacing) at `components/reader/` (flat); localStorage `edda.reading-preferences` persist+rehydrate; live `watch`→applyPreferences across the isolation bridge (setStyles FULL CSS each time). Self-check GREEN: typecheck/lint/format/test **445(49 files)**/build/e2e 1/1 fresh-servers (Sepia/Literata/24pt/Scroll restyle :5174 + persist-across-reload + restore)/validate strict. **Prior tests touched (field renames only, intent intact)**: gate2-independent-verifier.test (scroll→layout), navigator-proxy.test + epub-navigator.spec (fontSize→textSizePt). **DEVIATION → Gate 2 PRIMARY SCRUTINY**: task 2.1 readium-css vendoring (`--USER__*`/ReadiumCSS-before-after appearances) DELIBERATELY scoped out → direct-CSS via the existing setStyles seam; maker EDITED task 2.1 text to document this + marked 2.1–2.5 [x]. Architect must rule: acceptable realization of tasks 2.1–2.5 + CLAUDE.md "book typography is readium-css", or a real gap/spec-narrowing → FAIL. → Gate 2 (3 concurrent checkers). | in-progress |
| 2026-06-30 01:55 EEST | gate2-verify | **PASS (3/3)** | All 3 concurrent checkers PASS. **verifier(sonnet,≠maker)**: verify-gate exit 0 (typecheck/lint/format/test/build); **445 tests/49 files** + **46 own independent tests → 491/50**, 0 skipped. Injection: 10 hostile vectors to `buildReadingCss` (`expression()`, `</style><script>`, `; }html{display:none}`, `url(javascript:)`, backslash-encode, near-miss `Newsreader; font-size:9999px`, NaN/huge/negative textSizePt, non-enum typeface) ALL dropped→safe constants/clamps. Persistence: round-trip exact 5 keys; malformed/empty→defaults no-throw; `{"__proto__":…}`/`{constructor:…}`→NO prototype pollution. ADR-001: snapshot isReactive===false, structuredClone(toRaw(reactive))≠live. e2e 1/1 fresh-servers + video: Aa→Sepia/Literata/24pt/Scroll restyle the :5174 spine live + persist-across-RELOAD + restore (aria-pressed) + 0 console errors. Design fidelity HIGH vs `doc/mobile/04` (4 themes/3 typefaces/slider/Paged-Scroll/3 spacing/warm chrome). Prior-test edits = pure legacy→clean renames (intent intact). fallow no regression (reader-css.ts + store members now consumed). **security(opus)=PASS** (max LOW, nothing High+): allowlist/clamp/parse independently bypass-tested & hold; LOW-1 `reader-css.ts:130` theme lookup not `Object.hasOwn`-guarded but INERT (`__proto__`→undefined.bg→`background: undefined`, no breakout); LOW-2 missing explicit pollution test. **architecture(opus)=PASS** — **readium-css RULING: direct-CSS is an ACCEPTABLE in-pattern realization** (decisive facts: `git show HEAD:reader-css.ts` was ALREADY direct-CSS-via-setStyles; readium-css vendored NOWHERE; the load-bearing invariant half = book typography reaches the content doc inside the isolated frame not the Tailwind chrome, HONORED; rendering engine is NOT the cross-platform contract — DOM-free ReadingPreferences in core/model is). Field-shape consolidation = right call (1:1 maket vocab, no orphan consumers); flowMode DRY improvement; ADR-001 honored; panel/store/reader-css placement correct. **Process note (verifier+architect converge)**: tasks 2.2–2.5 still describe readium-css `--USER__*` while code does direct CSS → `[x]` marks MISLEADING; proposal.md/design.md over-promise readium-css. NOT a FAIL (choice sound) → **Gate-3 doc-HONESTY reconciliation** (annotate tasks 2.1–2.5 direct-CSS-v1, reconcile design/proposal, ensure MAIN spec describes what was BUILT) + readium-css-vendoring BACKLOG item. → Gate 3. | in-progress |
| 2026-06-30 02:06 EEST | gate3-docs | PASS | documenter(sonnet, model-override for the honesty reconciliation): **doc-HONESTY reconcile done** — appended transparent direct-CSS-v1/readium-css-deferred parentheticals to tasks 2.2–2.5 (original readium-css text PRESERVED), 5 deferral notes in design.md + 3 in proposal.md; marked 4.3 [x]. **Synced delta → NEW `openspec/specs/reading-preferences/spec.md` describing ONLY shipped behavior** (4 THEME_COLORS hexes, 3 TYPEFACE_STACKS, clampPt[10,40]@12pt-base, flowMode, SPACING_PRESETS line-height/margin-block, localStorage `edda.reading-preferences`, structuredClone(toRaw()) open+live) — **zero `--USER__*` language in the main spec**. 10/10 spot-checks match (file:line). Added readium-css-vendoring backlog row to triage. `openspec validate --strict` PASS; `validate --specs` 14/14 PASS; `format:check` PASS. No code change. | in-progress |
| 2026-06-30 02:06 EEST | archive | archived | `openspec archive add-reading-preferences --skip-specs --yes` → `changes/archive/2026-06-29-add-reading-preferences`. **Task status ✓ Complete** (all checked incl. 4.3 by Gate-2). No foreign churn; format:check GREEN. Verifier independent tests in new `src/gate2-verifier/` (green evidence; unconventional dir → triage). → commit. | done |
| 2026-06-30 02:06 EEST | commit | 8a99761 | one-spec commit on main (remember-reach), 54 files: DisplayPanel + persisted store + security-hardened reader-css mapping + live-apply wiring + tests (incl. verifier's gate2-verifier/) + e2e + synced `reading-preferences` spec + reconciled tasks/design/proposal + archived change + loop artifacts. `.claude/` excluded. **Spec 8/ch8 DONE (1 attempt, all 3 gates green).** ahead 11. → ch9 add-offline-and-sync (Komga UP). | done |

### add-offline-and-sync (ch9)
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-30 02:24 EEST | plan | plan ready (attempt 1) | planner(sonnet, read-only), brownfield-mapped → `loop/artifacts/add-offline-and-sync/attempt-1/plan/plan.md`. 9 steps honoring the load-bearing line (**core sync engine server-free + DOM-free; progress PER-CONNECTOR; book bytes bypass SW**). Reuses existing `furthestWins`/`OutboxEntry`/`SyncEngine`/`ProgressSyncStrategy` + `publicationSourceFromFile` ranged reads; replaces `noopSyncEngine` at ReaderView:60 only. New: Dexie `EddaDb` (downloads+outbox), OPFS book cache via a dedicated **Worker** (FileSystemSyncAccessHandle, injectable for tests), `DexieSyncEngine` (enqueue collapse-to-furthest, drain single-flight + furthest-wins + retry-on-error), `sync-scheduler` (online/visibilitychange), `downloadsStore`+`DownloadsView`, real `KomgaProgressStrategy`. **Komga progress API CONFIRMED vs LIVE**: PATCH `{page}`→400 non-Divina ⇒ fallback `{completed}`-only. 11 grep-checkable ACs; e2e = Vitest vs live Komga (furthest-wins both directions, cleanup DELETE). Risks: furthest-wins correctness, Dexie/OPFS-Worker in jsdom (maybe `fake-indexeddb` devDep → dep ladder), non-Divina 400, quota. → Gate 1. | in-progress |
| 2026-06-30 03:06 EEST | gate1-implement | tasks 1.1–7.1,8.1,8.4 [x] (25; −8.2 design-fidelity→Gate2, −8.3 review→Gate2, docs→Gate3) | maker(opus): clean **load-bearing partition**. **core (neutral)**: only added `lastSyncedAt` to SyncEngine (furthestWins/OutboxEntry already there); orchestrator greps CONFIRM `src/core/sync` server-free (komga|api/v1|Bearer|fetch=EMPTY) + `src/core` DOM-free (window|document|fetch|HTMLElement|ReadableStream|navigator=EMPTY non-test). **platform/web**: `db.ts` (Dexie4 EddaDb downloads+outbox, compound PK, v1→v2 migration), `opfs-book-cache.ts`+`opfs-write-worker.ts` (stream→OPFS via FileSystemSyncAccessHandle in a Worker, injectable; File.slice reads), `sync-engine.ts` (DexieSyncEngine), `sync-scheduler.ts` (online/visibilitychange+debounce+backoff), `connector-download.ts`, `publication-source.ts` (extracted to break a cycle). **connector**: `KomgaProgressStrategy(base,authFn,http)`+downloadDescriptor+deleteReadProgress (no platform/web import). **Furthest-wins proven** both directions + single-flight + error-retain (unit + LIVE). **Komga 400 fallback** (page→completed, both bodies asserted); integration DELETEs progress before+after, disjoint books (Pensées/Eminence) to avoid races. Dep: 1 devDep `fake-indexeddb@6.2.5`, `pnpm audit` clean (ladder step 1). Self-check GREEN: typecheck/lint/format/**test 540/540 0-skipped** (BOTH integration files RAN vs live Komga)/build(worker chunk emitted)/validate strict. Prior tests touched (behavior changed not suppressed): NavSidebar.test/LibraryView.test (registry-driven badge + real lastSyncAt), komga.test (real strategy), libraryStore (dropped fake seed). Triage(deferred per design Open Qs): Background Sync, partial-download resume, OPFS quota/LRU; LibraryBrowse.readSummary unused downloadedCount. Orchestrator sanity: 17 files +510/−116, 15 new, textual, 0 control bytes. **Gate-2 to probe**: OPFS key=filename (path-traversal if bookId has `/`/`..`? ch5 triage), download-via-fetch (respects connector origin/permission?), v1→v2 Dexie migration soundness, fake-indexeddb devDeps placement. → Gate 2 (3 concurrent). | in-progress |
| 2026-06-30 03:27 EEST | gate2-verify | **FAIL (attempt 1)** — 2 independent root-cause blockers (maker≠checker working) | 1/3 PASS, 2 FAIL. **architecture(opus)=PASS**: per-connector partition CLEAN in design-intent (core gained only `lastSyncedAt`; `ProgressSyncStrategy` engine reconciles purely on `totalProgression`, grep komga|page|completed in engine+scheduler=NONE; Komga page/completed normalized AT the connector boundary); bytes-bypass-SW intact (sw.ts untouched); OPFS Worker boundary sound (never buffers book); v1→v2 migration correct+intentional (v2 adds title index, tested); publication-source cycle-break real. Advisory: dead `downloadedCount` field, `enqueue` swallows write errors silently. **security(opus)=FAIL (HIGH H1)**: `connector-download.ts:31` raw `fetch(url,{headers})` OUTSIDE `bridge.http` + Komga `downloadDescriptor` attaches `Authorization: Basic <creds>` to a `url` taken VERBATIM from the server manifest (`resolveDownloadHref`/`resolveAcquisitionLink`, no same-origin check) ⇒ a malicious/compromised server returns an acquisition href→`attacker.tld` and the app sends the user's reader creds cross-origin (CORS-preflight the attacker allows). Defeats the ch3 origin-allowlist for the ONE credentialed request; latent-worse for OPDS (ch10). PRIMARY suspected OPFS path-traversal = NOT a finding (mitigated: `opfsKeyFor` encodeURIComponent + WHATWG guards). +L1 Low: `opfsKeyFor` `__`-delimiter not injective (`encodeURIComponent` doesn't encode `_`; `a__b`+`c`==`a`+`b__c`; near-nil today—sourceId is randomUUID). **verifier(sonnet)=FAIL (fallow regression)**: NEW circular dep `opfs-book-cache.ts:159` (Worker URL ref) ↔ `opfs-write-worker.ts:8` (imports back). ELSE all PASS: static 549 (540+9), 4/4 AC greps, 9/9 own tests (furthest-wins both dirs + single-flight + error + persist + OPFS-no-fetch), live integration RAN (400→fallback confirmed live), e2e 5/5+video+design-fidelity, prior edits legit. → **Gate 1 attempt 2** (surgical, do NOT re-touch validated core): (FIX1 H1) origin-gate the credentialed download — attach auth only when `URL(url).origin===connector base origin`, route via `originAllowed`, `redirect:'error'`, reject cross-origin manifest hrefs + a test; (FIX2 cycle) extract `createOpfsWriteHandler`+protocol types→`opfs-write-protocol.ts`, both files import it not each other; (FIX3 L1) make `opfsKeyFor` injective (encode delimiter/hash) + docstring; (FIX4 cleanup) drop dead `downloadedCount` from CatalogSummary+readSummary. | no |
| 2026-06-30 03:43 EEST | gate1-implement (attempt 2) | 4 surgical fixes applied; validated core untouched | maker(opus, ≠ attempt-1 maker): **FIX1 (H1)** — extracted `originAllowed`→leaf `origin-allowlist.ts` (imports only `@/core/contracts`, single source for bridge client + download); `downloadDescriptor` now returns `{url,headers,allowedOrigins}`; `connectorDownloadStream` THROWS (no creds sent) if `!originAllowed(url, allowedOrigins)` + `redirect:'error'`; Komga `allowedOrigins=[new URL(#base).origin]`. NEW `connector-download.test.ts` 4 cases (cross-origin→fetch NEVER called; same-origin→redirect:'error'+auth; undefined→fail-closed; fixture fallback). **FIX2 (cycle)** — NEW leaf `opfs-write-protocol.ts` (createOpfsWriteHandler + 6 types); cache imports+re-exports it, worker imports it not the cache → orchestrator-confirmed worker no longer imports cache; fallow **0 circular deps**. **FIX3 (L1)** — `opfsKeyFor` segments `encodeURIComponent(s).replace(/_/g,'%5F')` (delimiter reserved) → injective + docstring fixed + 2 regression tests (old collision pair now distinct). **FIX4** — dropped dead `downloadedCount` from LibraryBrowse CatalogSummary+readSummary. Self-check GREEN: typecheck/lint/format/**test 555/58 files** (549+6; live Komga RAN: komga.integration 10/10, offline-sync 2/2)/build/`pnpm check` 0 cycles/validate strict. Orchestrator sanity: both new modules clean leaves, no control bytes. NOT modified: furthest-wins/single-flight engine, OPFS streaming, Dexie schema/migration, Komga mapping, app wiring, src/core. → re-verify (security H1-closed + verifier cycle/static; architect PASS carries). | in-progress |
| 2026-06-30 03:50 EEST | gate2-verify (attempt 2) | **PASS** | Proportionate re-verify (both ≠ attempt-2 maker); architect PASS carried (extractions match the approved publication-source.ts pattern, no arch change). **security(opus)=PASS — H1 CLOSED**: traced path (throw@:53-58 precedes credentialed fetch@:61, no swallowing catch); full bypass matrix all fail-closed (userinfo/port/trailing-dot/IDN-punycode/data:/file:/protocol-relative/undefined+empty allowlist/redirect) — compares `new URL(x).origin` not substring, `redirect:'error'` blocks 3xx; extracted `originAllowed` git-confirmed char-identical to prior; it's the ONLY new credentialed egress (other fetches gated/same-origin); test genuinely asserts `fetch` NOT called cross-origin + undefined. L1 FIXED (opfsKeyFor injective + closes OPFS path-traversal at the single key chokepoint). Residual advisory LOW (non-exploitable `blob:`-same-origin edge → add scheme∈{http,https} → triage). **verifier(sonnet)=PASS**: static 555/58, 0 skips; **cycle CONFIRMED GONE** (no circular-deps section in fallow; opfs-write-protocol.ts clean leaf, worker imports protocol not cache); H1 test non-tautological (asserts fetchSpy.not.toHaveBeenCalled cross-origin + fail-closed undefined); NO functional regression (furthest-wins both dirs / OPFS-no-fetch / Dexie v1→v2 / Komga live round-trip+400-fallback all green, integration RAN not skipped); L1 collision test passes; the one new komga clone-group was pre-existing (attempt-1 had it). → Gate 3 docs. | in-progress |
| 2026-06-30 03:57 EEST | gate3-docs | PASS | documenter(sonnet): synced 3 NEW capability specs → `openspec/specs/{sync-engine,progress-sync-strategy,offline-storage}/spec.md`. **Added 2 AS-BUILT requirements the deltas lacked** (reflecting the Gate-2 remediation): progress-sync-strategy +`Komga 400-fallback for non-Divina EPUBs`; offline-storage +`Credentialed download is origin-gated` (H1 fix: refuse before fetch when origin∉allowedOrigins + redirect:'error'). README +"Offline reading and progress sync" section. **10/10 spot-checks** (furthestWins on totalProgression, lastSyncedAt, enqueue-collapse, drain single-flight, 400-fallback, File.slice no-fetch, compound PK+v1→v2 migration, origin-gate, noopSyncEngine gone). `openspec validate --specs` 17/17 PASS; `--strict` PASS; format:check clean. No code change. | in-progress |
| 2026-06-30 03:57 EEST | archive | archived | `openspec archive add-offline-and-sync --skip-specs --yes` → `changes/archive/2026-06-30-add-offline-and-sync`. 25/27 [x]; 2 unchecked = 8.2 design-fidelity + 8.3 review (performed by Gate-2), per precedent. No foreign churn; format:check GREEN; fake-indexeddb confirmed devDeps. → commit. | done |
| 2026-06-30 03:57 EEST | commit | 988f304 | one-spec commit on main (remember-reach), 75 files: Dexie+OPFS+sync-engine+scheduler (platform/web), KomgaProgressStrategy+download (connector), core `lastSyncedAt`, Downloads view/store/wiring, origin-allowlist + opfs-write-protocol (Gate-2 fixes) + tests + synced 3 specs + README + archived change + loop artifacts. devDep fake-indexeddb. `.claude/` excluded. **Spec 9/ch9 DONE (2 Gate-2 attempts: HIGH credential-launder + OPFS cycle → fixed at root).** ahead 12. → ch10 add-extensions-and-capability-install (Komga UP). | done |

### add-extensions-and-capability-install (ch10)
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-30 04:11 EEST | plan | plan ready (attempt 1) | planner(sonnet, read-only) → `loop/artifacts/add-extensions-and-capability-install/attempt-1/plan/plan.md`. Large 11-step change on the existing `PluginRegistry`/`resolveFormat`: id reconcile (hyphen→DOT ids+versions to maket, HOST_API 1.0→1.2), `core/sniff` tiers, neutral EventBus, `CapabilityDispatcher`, app wiring, Capability-missing native `<dialog>` (doc/web/07), Extensions screen (doc/web/06), complete OPDS `browse()`, real PDF handler (pdfjs-dist range-transport ADR-005 + markRaw). pdfjs-dist already a dep. 18 ACs; e2e install→open→retry; 5 risks. **Orchestrator guardrails**: (1) bundled-non-disableable enforced at the **REGISTRY** (refuse disable of bundled→reader can't break) + UI toggle disabled — ch7 triage "registry/UI", robust reading; (2) "install" = lazy import() NOT network code-fetch (assert fetch-not-called); (3) markRaw pdfjs proxy; (4) ADR-005 PDF ranged reads; (5) id rename surgical+grep-complete. → Gate 1. | in-progress |
| 2026-06-30 05:54 EEST | gate1-implement | tasks 1.1–7.3 [x] (−7.4 review→Gate2) | maker(opus): 11 steps. **Bundled-non-disableable AT THE REGISTRY** (`registry.disable()` throws `PluginNotDisableableError` for bundled + `isBundled()`; comment "LOAD-BEARING source of truth not just UI"; UI toggle disabled) — robust enforcement as required; tested registry/app/component/e2e. **Install = lazy import() not network** (fetch spied un-called ×4). dot ids (`connector.opds`/`format.epub`/`format.pdf`+stubs) + HOST_API 1.2.0 + maket versions; updated 11 asserting test files. core (sniff tiers / event-bus NEW / CapabilityDispatcher) **DOM-free CONFIRMED** (grep empty). CapabilityMissingModal native `<dialog>` (screen 07) + SettingsExtensions (screen 06) + presentation mappers. OPDS `browse()`/`content()`/`_opds-core` OPDS1/2 + honest progressSync + basic auth. Full `format.pdf` (manifest/handler/navigator/transport/engine). **PDF DESIGN DECISION**: renders IN-APP to `<canvas>` via pdfjs (maker rationale: "no book-authored HTML/JS runs, unlike EPUB" — NOT origin-isolated like EPUB); `markRaw` PDFDocumentProxy; `PDFDataRangeTransport`→`source.read` (ADR-005, no fetch) + `disableAutoFetch/disableStream`. Root-cause: `vite.config optimizeDeps.exclude:['pdfjs-dist']` (dep-optimizer incompat; fixed e2e cold-flake, removed interim retry). Self-check GREEN: typecheck/lint/format/**test 615/65 files**/build (PDF code-split handler 3.6KB/engine 425KB/worker 1.2MB)/**e2e 2/2 COLD fresh-servers**/**OPDS integration vs live Komga 4/4**/validate strict/audit clean. Triage: modal body shows bookId-slug not title (reader builds bookRef.title=bookId; cosmetic, reader concern); OPDS v2 progression I/O = sync-engine job (design Non-Goal). Orchestrator sanity: 30 files +1235/−139, 18 new, 0 control bytes. **Gate-2 PRIMARY scrutiny**: (a) pdfjs on the credential app-origin — `enableScripting:false`/`isEvalSupported:false` set? render-location acceptable vs ch7 isolation stance? (b) OPDS credentialed fetch — origin-gated like ch9 H1, or same credential-launder class? (c) install-no-remote-code; (d) modal "Not now" download origin. → Gate 2 (3 concurrent). | in-progress |
| 2026-06-30 06:11 EEST | gate2-verify | **FAIL (attempt 1)** — HIGH (OPDS=ch9-H1 repeat) + fallow regression | 1/3 PASS. **architecture(opus)=PASS (conditional)**: PDF-in-app/EPUB-isolated asymmetry COHERENT (pdfjs paints to canvas, parse in a Worker, Acrobat-JS gated behind `enableScripting`=default-false → lower risk class; isolating PDF would force 1.7MB pdfjs into the reader origin for marginal gain) — condition: pin pdfjs safe flags; **registry bundled-non-disableable CORRECT LAYER** confirmed (disable() throws for bundled, resolveFormat(EPUB) can never become installable, tested); core sniff/dispatch/event-bus neutral; id-rename right+complete; PDF structure mirrors EPUB + WebFormatHandler conformance + code-split. Advisory: vestigial `toDisplayConnectorId`, isFormatCapabilities 3-line dup. **security(opus)=FAIL (HIGH H1 = PRIMARY-2 ch9-H1 repeat)**: `opds/index.ts:167-183` `content()` attaches `Authorization: Basic <creds>` to `#absolute(href)` where href is the SERVER-SUPPLIED acquisition href (passes `//attacker.tld` through unchanged); `OPDS_MANIFEST.permissions.network=['*']` makes the `PermissionEnforcingHttpClient` gate a NO-OP → malicious/MITM'd/multi-tenant feed returns href→attacker.tld, app GETs it WITH creds. Reachable on BOTH main paths: reader open (`useNavigator:175 connector.content`) + modal "Not now" download (falls through `connector-download:67` to `content()` — Komga's descriptor gate doesn't cover OPDS). Defeats "server already has creds" (3rd-party feed-content influence). Fix = mirror ch9: gate `content()` (+ add OPDS `downloadDescriptor` w/ `allowedOrigins:[base origin]`) via `originAllowed`, fail-closed + bypass-matrix test. PRIMARY-1 pdfjs render = ACCEPTABLE (canvas, scripting off, no cMapUrl/network) — M1 MEDIUM advisory: `getDocument` lacks `isEvalSupported:false` (defaults true, eval-from-data on cred origin, no CSP; not High — pdfjs@6.1.200 patches CVE-2024-4367). L1 Low: FetchHttpClient redirect:'follow' (browser strips auth cross-origin). **verifier(sonnet)=FAIL (fallow)**: 7 NEW dead exports in touched `plugins-catalog.ts` (`PluginNotAvailableYetError` + 6 catalog-entry factories — exported, called only in-file). ELSE all PASS: static 615/65, 0 real skips; core DOM-free; id-rename consistent (no live hyphen-ids); **PDF code-split CONFIRMED** (main bundle = dynamic-import wrapper only; engine 416KB+worker 1.2MB separate); 9 own tests (registry-non-disableable + install-no-fetch + PDF ADR-005 ranged + markRaw + dispatcher events); e2e 2/2+video+design-fidelity 06/07; OPDS live 4/4 RAN; prior edits legit (renames + ADDED coverage). Triage: CapabilityMissingModal↔AddSourceModal 17L clone (intentional), useNavigator runOpen CRAP 224 complexity, platform/web dead type exports (ch9 not this spec). → **Gate 1 attempt 2** (surgical): (FIX1 H1) origin-gate OPDS `content()` + add OPDS `downloadDescriptor`(allowedOrigins:[base]) → both sinks fail-closed + bypass test; (FIX2 fallow) un-export the 6 internal catalog factories; resolve `PluginNotAvailableYetError` (un-export OR make the install error-handling catch it by class); (FIX3 fold-in M1) thread `isEvalSupported:false`+explicit `enableScripting:false` into pdfjs params. Carry: validated core (PDF/registry/dispatcher/OPDS-browse). | no |
| 2026-06-30 06:30 EEST | gate1-implement (attempt 2) | 3 surgical fixes; validated core untouched | maker(opus, ≠ attempt-1 maker): **FIX1 (H1)** — imported existing `originAllowed`; `#allowedOrigins()`→`[new URL(#base).origin]` (fail-closed empty/unparseable→[]=deny-all); `#assertAllowedOrigin` gates `content()` BEFORE attaching Authorization (covers reader-open + modal-download sinks) + implemented `downloadDescriptor()` w/ `allowedOrigins` (Komga parity, gates streaming path). Bypass-matrix test both sinks: same-origin→creds sent; `https://attacker.tld`+protocol-relative `//attacker.tld`→refused `/allowed origins/i`, fetch NEVER called (protocol-relative = the real escape, `#absolute` resolves it then refuses). **FIX2 (fallow)** — un-exported the 6 intra-file catalog factories; `PluginNotAvailableYetError` made LIVE+better-UX (imported into SettingsExtensions.vue, `onInstall` catches `instanceof`→"Coming soon"); fallow **0 new dead exports** vs HEAD baseline + 0 new cycle. **FIX3 (M1 fold-in)** — `isEvalSupported:false`+`enableScripting:false` threaded into pdfjs params interface + call site (no render change). Self-check GREEN: typecheck/lint/format/**test 630/66 files**/build (PDF code-split intact)/fallow 0-new-dead/0-cycle/validate strict; **OPDS live 4/4 RAN** (gate doesn't break legit same-origin Komga-OPDS fetch). Orchestrator sanity: gate+un-exports+pdfjs-flags confirmed, 0 control bytes. NOT altered: PDF handler/transport, registry enforcement, dispatcher, OPDS browse/_opds-core. → re-verify (security H1-closed+M1 + verifier fallow/static; architect PASS carries). | in-progress |
| 2026-06-30 06:32 EEST | **PAUSE (session limit)** | ch10 attempt-2 Gate-2 re-verify INTERRUPTED | **Account session limit hit (resets ~06:50 Europe/Kyiv).** Both re-verify subagents (security ab04921884, verifier a2f9fb47) were killed before emitting verdicts (returned only the limit message, ~120 tokens each) — **no Gate-2 attempt-2 verdict yet**. State is clean + recoverable: ch10 attempt-2 Gate-1 FIXES are applied in the WORKING TREE (uncommitted) + self-check GREEN (630 tests/66 files, fallow 0-new-dead/0-cycle, OPDS live 4/4 RAN, build code-split, validate strict); orchestrator sanity confirmed the OPDS origin-gate + un-exports + pdfjs flags structurally. **RESUME (after limit reset)**: re-spawn the 2 re-verify checkers (spec-loop-security: H1-closed bypass-matrix incl. unparseable-base/protocol-relative + M1; spec-loop-verifier: fallow 0-new-dead-on-plugins-catalog + static 630 + OPDS-live-still-green + no functional regression). Architect attempt-1 PASS carries (fixes don't touch structure). If both PASS → Gate 3 docs (sync ch10 delta specs → main) → archive `add-extensions-and-capability-install` → commit (HEAD currently 988f304/ch9, ahead 12; ch10 will be the next commit). Then **ch11 add-visual-polish-e2e** (last spec, no Komga) → StopConditionJudge → consolidated demo-video offer. Triage to add on resume: connectors keep introducing credentialed fetches to server-supplied URLs w/o origin-gating (ch9 Komga, ch10 OPDS) → make origin-gating a DEFAULT in the bridge/permission layer (permissions.network default to [base] not ['*'], or gate credentialed sends in PermissionEnforcingHttpClient) so it's not per-connector opt-in. | in-progress |
| 2026-06-30 09:47 EEST | assess (resume) | session resumed; tree intact, fixes present | New session resumed at the interrupted ch10 attempt-2 Gate-2 re-verify. Ground truth re-confirmed: HEAD `988f304` (ch9), ahead 12; working tree = ch10 attempt-2 (uncommitted) UNCHANGED since pause; Komga UP @ :25600 (container 27h) so OPDS live integration can run. Orchestrator sanity grep CONFIRMS all 3 fixes present: FIX1 `originAllowed`+`#assertAllowedOrigin`+`downloadDescriptor`(opds/index.ts:175/197/258), FIX3 `isEvalSupported:false`+`enableScripting:false`(document.ts:85-86), FIX2 `PluginNotAvailableYetError` imported+`instanceof`-caught(SettingsExtensions.vue:20/88) + 6 factories un-exported; 0 raw control bytes. → re-spawning the 2 re-verify checkers (security: H1 bypass-matrix both sinks incl. protocol-relative/unparseable-base + M1 pdfjs flags; verifier: static 630 + fallow 0-new-dead + OPDS live + no regression). Architect attempt-1 PASS carries (3 fixes are surgical, don't touch structure; FIX3 satisfies its lone condition). | in-progress |
| 2026-06-30 09:57 EEST | gate2-verify (attempt 2) | **PASS (3/3)** | Proportionate re-verify (both checkers ≠ attempt-2 maker); architect attempt-1 PASS carries (surgical fixes don't alter structure; FIX3 met its pdfjs-flags condition). **security(opus)=PASS** (max LOW advisory): **H1 CLOSED at root** — `opds/index.ts content()` `#assertAllowedOrigin` throws @:175 BEFORE the `Authorization` header is built (:176)/sent (:179); fail-closed (`#allowedOrigins()` → `[]` on empty/unparseable base ⇒ deny-all). Bypass matrix holds on BOTH credentialed sinks (reader `content()` + modal `downloadDescriptor`→`connectorDownloadStream` w/ `redirect:'error'`): same-origin sends creds (legit Komga-OPDS unbroken), `https://attacker.tld` / protocol-relative `//attacker.tld` (the real escape — `#absolute` resolves then refuses) / userinfo / port / trailing-dot / `data:`/`file:` all throw; origins via `new URL().origin` not substring. `opds.test.ts` tests non-tautological (assert attacker URL never contacted). **M1 resolved** (`document.ts:85-86` `isEvalSupported:false`+`enableScripting:false` into real `getDocument`; no cMapUrl/network; bytes via `source.read` ADR-005). No new credentialed egress (Kavita/Calibre/CBZ are inert stubs, never build a connector). L1 (`FetchHttpClient` redirect:'follow') unchanged, browser-mitigated → triage. **verifier(sonnet)=PASS**: static GREEN, **633 tests/66 files** (630 + 3 own OPDS origin-gate tests: allowedOrigins-pinned / cross-origin-throws-before-send / unparseable-base-fail-closed), 0 suppressions; **fallow 0 NEW dead exports** on plugins-catalog.ts (back to 2 baseline `manifestForConnector`+`epubFormatCatalogEntry`; 6 factories now module-internal; `PluginNotAvailableYetError` live via instanceof-catch), 0 new cycle; **OPDS live 4/4 RAN** vs Komga :25600 (skipIf=false, same-origin download works); **PDF code-split CONFIRMED** (entry `index-*.js` 184KB 0 pdfjs refs; engine 415KB + worker 1255KB lazy via 3.6KB wrapper); carry-forward GREEN (registry bundled-non-disableable throws `PluginNotDisableableError`; install no-fetch; dispatcher emits CAPABILITY_MISSING; CBZ→"Coming soon"); e2e 4/4 + 2 videos + design fidelity vs doc/web/06 (INSTALLED·3 bundled→·4 after PDF install) + doc/web/07 (modal strings/chips/badges). → Gate 3 docs. | in-progress |
| 2026-06-30 10:05 EEST | gate3-docs | PASS | documenter(sonnet, model-override for the 4-spec sync + as-built honesty): synced delta → 4 NEW main specs `openspec/specs/{capability-dispatch,connector-opds,extensions-registry,format-pdf}/spec.md` + UPDATED `plugin-registry` (HOST_API 1.2.0; bundled-always-on req `PluginNotDisableableError`). **Added 2 AS-BUILT requirements the deltas lacked** (ch9 precedent): connector-opds +"Credentialed download is origin-gated" (same-origin carries creds / cross-origin throws-before-send / streaming descriptor gates); format-pdf +"In-app canvas rendering with eval+scripting lockdown" (the intentional PDF/EPUB asymmetry). DESIGN.md +§11.3 (PDF in-app canvas rationale: rasterized pixels not book script ⇒ no origin isolation; eval/scripting off + no network + range transport as defense-in-depth). README +"Extensions and install-on-demand formats". `openspec validate --strict` + `--specs` 21/21 + `format:check` GREEN. **6/6 spot-checks** (PDF eval lockdown document.ts:85-86; OPDS fail-closed opds/index.ts:246-260; HOST_API contracts:241=1.2.0 + manifests ^1.0.0; registry bundled-non-disableable registry:192-194; dispatcher emits CapabilityMissing dispatch:52-53; sniff tiers 1.0/0.7/0.5 sniff:58-69). No code change. | in-progress |
| 2026-06-30 10:13 EEST | archive | archived | `openspec archive add-extensions-and-capability-install --skip-specs --yes` → `changes/archive/2026-06-30-add-extensions-and-capability-install`. 25/26 [x]; lone 7.4 (independent review) performed by the Gate-2 checkers (security+verifier+architect), not box-ticked — ch5/ch6/7a/7b/8/9 precedent. Pre-commit hygiene: NO foreign screenshot/artifact churn (ch10 e2e wrote only to its own gate dirs); `.claude/` infra excluded. | in-progress |
| 2026-06-30 10:13 EEST | commit | 04c946f | one-spec commit on main (remember-reach), 96 files: capability dispatch/sniff-tiers/event-bus (core, DOM-free) + Extensions screen 06 + Capability-missing modal 07 (app) + connector.opds (always-on, origin-gated) + format.pdf (install-on-demand, code-split, in-app canvas) + registry bundled-non-disableable + maker/verifier tests + e2e + synced 4 specs & plugin-registry + DESIGN §11.3 + README + archived change + loop artifacts. `.claude/` excluded. **Spec 10/ch10 DONE (2 Gate-2 attempts: OPDS credential-laundering HIGH + fallow regression → fixed at root; re-verify interrupted by session-limit then completed PASS 3/3).** ahead 13. → ch11 add-visual-polish-e2e (LAST spec, no Komga). | done |

### add-visual-polish-e2e (ch11 — capstone acceptance gate)
| timestamp (Europe/Kyiv) | stage | progress | comment | resolution |
|-------------------------|-------|----------|---------|------------|
| 2026-06-30 10:29 EEST | plan | plan ready (attempt 1) | planner(sonnet, read-only) → `loop/artifacts/add-visual-polish-e2e/attempt-1/plan/plan.md` (33 tasks → 20 steps, 8 phases A–H). **Visual-regression = 2 tiers**: Tier-1 `pixelmatch` app-screenshot vs cropped doc/web|doc/mobile PNG (strip ~85px browser-chrome strip; `threshold 0.1`, `maxDiffPixelRatio 0.05`) = design-intent fidelity; Tier-2 Playwright `toHaveScreenshot()` vs committed goldens (`maxDiffPixelRatio 0.005`, Chromium-only) = strict drift gate. Cross-origin `reader-mount` + volatile text (timestamps/download-count) masked in both. Determinism: seeded fixture, self-hosted fonts, disabled animations/caret, fixed viewport+deviceScaleFactor. **Correction vs run-config: Komga IS required** for task 6 (offline happy-path e2e) — proposal supersedes the old "ch11 no-Komga" note; Komga UP @ :25600. Dev deps (ladder applies): `@axe-core/playwright@^4.12.1`, `pixelmatch@^7.2.0`, `pngjs@^7.0.0`, `@types/pngjs@^3.4.4`. e2e scenario = the goals.md offline happy path (add→browse→open→paginate→prefs→offline→reconnect→furthest-wins), doubles as the demo video. **Triage**: T2 CBZ RTL reader not implemented (no loadCbzFormat) → committed `test.fixme`, non-blocking (CBZ = future format); T3 mobile themes composite → DisplayPanel subregion crop + 4 separate theme goldens (resolved in-plan). | in-progress |
| 2026-06-30 10:29 EEST | decision | **user chose "Add BottomNav.vue" (T1)** | Spec self-contradiction surfaced by the planner (AskUserQuestion): `doc/mobile/*.png` shows a bottom tab bar the app doesn't render, but the proposal says "no new components" while task 5.2 says "match mobile maket". User chose to ADD `BottomNav.vue` (responsive bottom tab bar; sidebar→`hidden md:flex`) — treated as in-scope responsive LAYOUT re-exposing existing routes, NOT new product surface (CLAUDE.md designates the maket as the layout source of truth). Gate-2 architect must accept it as in-scope responsive layout, not flag it as new-surface scope-creep. → Gate 1. | in-progress |
| 2026-06-30 11:46 EEST | gate1-implement | tasks 1.1–5.2,6.1,6.4,7.1,7.2,7.5 [x] (−6.2/6.3/7.3 Komga-flow blocked by pre-existing connector gap; −7.4 review→Gate2) | maker(opus, ran modern-web-guidance first): visual-regression harness (playwright.config + `e2e/visual/{capture-helper,tolerances,maket-seed}`; goldens at `e2e/<project>/snapshots/`) + targeted polish + new `BottomNav.vue`/`OfflineBanner.vue`. Self-check GREEN: typecheck/lint/format/**test 633(66 files)**/build; **test:visual 19/19** (Tier-1 maket-within-tolerance + Tier-2 goldens stable); **test:a11y 12/12** (axe 0 serious/critical every screen + keyboard/focus-trap); **test:responsive 18/18** (no overflow @390/768/1300 + phone makets within tol); validate strict. Dev deps `@axe-core/playwright@4.12.1`/`pixelmatch@7.2.0`/`pngjs@7.0.0`/`@types/pngjs@6.0.5` → audit clean (ladder step 0). Orchestrator sanity CONFIRMED: connector-komga + src/core UNTOUCHED; non-test source = only visual-polish files + the 2 new components; 0 control bytes. **3 honest findings the capstone SURFACED (maker stayed in scope, did NOT silently fix):** (1) **HEAD 04c946f not green** — pre-existing `typecheck` RED in ch10's `gate2-checker-extensions-install.test.ts` (KeyValueStore stub w/ bogus `keys` + unused param; the gate's typecheck.log was a timing artifact = authored-after-typecheck, the ch4→ch5 process-gap class). Maker applied the only non-ch11 edit (behavior-neutral) for a green baseline → **Gate-2 must confirm behavior-neutral + HEAD-was-red**. (2) **Komga offline-e2e (task 6) blocked by PRE-EXISTING gaps**: `resolveDownloadHref`→`/manifest` **406** (Accept `application/json` vs Komga `application/webpub+json`; throws before the `?? .../file` fallback @komga/index.ts:480) + reader origin **:5174 not in Komga CORS allowlist** (`test/komga` lists 5173/4173 only — host concern). Connect/browse/open works ("p.1–2/522"); download/reconnect can't. (3) **4 other chromium e2e red at HEAD, not ch11**: `library-browse:18` STALE assertion ("14 downloaded" — ch9 changed to "0", documented in offline-sync.spec, didn't update this older spec); `epub-navigator`×2 (`source.size` — 7b contract reshape); `reader.spec` Test A. **→ Decision-2 (stale e2e assertions = test maintenance, in-scope for the acceptance harness): orchestrator will direct a fix regardless. Decision-1 (the connector-komga 406 + CORS — fix-in-ch11 vs strict-scope-defer): SURFACED to user (AskUserQuestion) — genuine scope-vs-capstone-completeness fork on the headline acceptance criterion.** | in-progress |
| 2026-06-30 12:00 EEST | decision | **user chose "Fix in ch11 (prove it E2E)" (Decision-1)** | User authorized making the offline happy path pass end-to-end via the minimal connector-komga 406 fix + the :5174 CORS host-config (framed as bug-fixes to an EXISTING path, like BottomNav — not new surface). Decision-2 (stale e2e assertions = test maintenance) the orchestrator directs regardless. → Gate-1 continuation (same attempt 1; completing deferred 6.2/6.3/7.3 + the authorized fixes). | in-progress |
| 2026-06-30 12:48 EEST | gate1-implement (continuation) | tasks 6.2/6.3/7.3 [x] (−7.4 review→Gate2); full suite GREEN | maker(opus, fresh ≠ harness-maker): 4 fixes. **FIX1** connector-komga `resolveDownloadHref` (komga/index.ts:474-487) — manifest `#json` now sends `Accept: application/webpub+json` (was default `application/json`→406→threw before the `/file` fallback); live-curl CONFIRMED 406→200; `/file` fallback + origin-gate UNTOUCHED. **FIX2** CORS host-config — `http://localhost:5174` (ADR-013 reader origin) added to `KOMGA_CORS_ALLOWED_ORIGINS` (`test/komga/docker-compose.yml`+`.env.example`); Komga restarted (data survived); :5174 preflight now allowed. **FIX3** stale assertions (all VERIFIED genuine staleness, not regressions): library-browse "14 downloaded"→"0" + "Synced 2m ago"→"Synced just now" (ch9); epub-navigator×2→current API (`publicationSourceFromBytes`, dropped `loaded` event, `currentLocator()`); reader Test A→net-forward-progress paging (pixel-diff proof; foliate page-NUMBER readout ticks ~every 2 turns = brittle assertion not a bug). **FIX4** offline happy path GREEN vs live Komga ~7.5s (add→browse→download-OPFS→open→OFFLINE→paginate→Sepia→reconnect); demo video `loop/artifacts/add-visual-polish-e2e/attempt-1/gate1/playwright/offline-happy-path.webm`. Self-check GREEN: typecheck/lint/format/**test 633(66 files)**/build; **full playwright 79 passed/1 fixme** (the 4 previously-red specs now green; CBZ-RTL the only fixme); visual/a11y/responsive 49; validate strict; 0 control bytes; HEAD-baseline typecheck fix untouched. Orchestrator sanity: tree clean (no foreign churn), FIX1 minimal Accept-header only, src/core UNTOUCHED. **DEVIATION + ESCALATION (Gate-2 to RULE):** literal "reconnect→furthest-wins on book detail" NOT achievable for a Komga EPUB — curl-proven Komga rejects every non-`completed:true` read-progress write for a non-Divina EPUB (`{page}`/`{completed:false}`→400, only `{completed:true}`→204) ⇒ mid-book EPUB position can't round-trip through Komga's server; app reads progress only from connector (no local display fallback). Maker (in scope: FIX1=don't alter other connector behavior) asserted the ACHIEVABLE (offline retained + paging + reconnect + no errors) + escalated the mid-book-roundtrip to connector-komga (Komga R2 locator API) / offline-and-sync (local progress cache); furthest-wins POLICY already unit-tested. → Gate 2 (3 concurrent checkers; architect+verifier RULE on the deviation; architect: BottomNav + connector-fix are USER-AUTHORIZED not scope-creep). | in-progress |
| 2026-06-30 12:59 EEST | gate2-verify (attempt 1) | **PASS (3/3)** | All 3 concurrent checkers PASS. **verifier(sonnet,≠maker)**: static GREEN (typecheck/lint/format/build; **test 646/67 files** = 633 + 13 own checker tests in `gate2-checker-visual-polish.test.ts`: connector-406 Accept-override ×5 / OfflineBanner ×3 / BottomNav ×5); **full Playwright 79 passed/1 fixme** (visual 19 Tier-1-within-tol + Tier-2 goldens matched no `--update`; a11y 12 axe-0-serious/critical+focus-trap; responsive 18 no-overflow@390/768/1300; Komga offline RAN+passed ~7.5s+video; the 4 previously-red specs GREEN); **HEAD-04c946f-was-typecheck-RED CONFIRMED** (`gate2-checker-extensions-install.test.ts:265` `keys` not on KeyValueStore + unused `req`) + maker fix **behavior-neutral** (no assertion changed); **FIX1 live-Komga CONFIRMED** (manifest `Accept: application/json`→406, `application/webpub+json`→200; `#json` override correct; `/file` fallback + `downloadDescriptor.allowedOrigins` untouched); stale assertions = genuine staleness (not masked regressions; reader Test A net-progress backed by pixel-diff); deviation = acceptable (furthest-wins POLICY unit-tested in core/sync + sync-engine + gate2-checker-offline-sync); no suppressions (only CBZ-RTL fixme); fallow delta = 2 pre-existing-pattern barrel re-exports (triage). **security(opus)=PASS** (max LOW advisory): FIX1 origin-gate HOLDS (Accept-header routes through unchanged PermissionEnforcingHttpClient; `downloadDescriptor.allowedOrigins` untouched ⇒ ch9-H1 invariant intact; manifest parse reads only `links[]`, no injection/SSRF); 3 deps are devDeps w/ 0 `src/` imports (never bundled); CORS confined to `test/komga/`; no secret leak (password masked in webm, traces gitignored); BottomNav/OfflineBanner no unsafe sinks. LOW: offline-e2e console-error allowlist broad (host token) — anchor on scheme → triage. **architect(opus)=PASS**: **DEVIATION = ACCEPTABLE realization** (Komga-server-imposed 400, escalation correct, policy unit-tested, tasks annotated not silently ticked — consistent w/ ch8/ch10); **BottomNav + connector-fix NOT scope-creep** (BottomNav re-exposes 4 existing routes, no new routes, reuses SidebarLink pattern; connector fix minimal Accept-merge, `/file` + origin-gate intact); core invariant holds (`git diff HEAD src/core` empty); harness sound+DRY (Tier-2 goldens real drift guard distinct from Tier-1; principled masking; shared maket-seed/tolerances; testIgnore prevents double-runs); typecheck-baseline fix minimal+behavior-neutral. Advisory triage: useOnline composable (OfflineBanner dup of navigator.onLine idiom), Tier-1 loose tolerances by-design, console-filter breadth. → Gate 3 docs. | in-progress |
| 2026-06-30 13:09 EEST | gate3-docs | PASS | documenter(sonnet, model-override for the capstone sync + as-built honesty): synced delta → NEW main spec `openspec/specs/visual-fidelity/spec.md` (10 reqs/37 scenarios). **As-built honesty (ch9/ch10 precedent)**: replaced the delta's aspirational "Furthest-wins reconcile on reconnect" scenario with an honest "Reconnect with position intact" scoped to what the e2e asserts; added an "Achievable scope (as-built)" block + a "Documented limitation" block (Komga 400 on non-`completed:true` EPUB writes, cites `offline-happy-path.spec.ts:78-82,149-157`, notes 6.3/7.3 annotated) + a Backlog section escalating the connector-komga R2 locator API / local progress cache. README: ch11 "What works now" entry + new "Visual fidelity & acceptance gates" section (test:visual/a11y/responsive/komga commands, `komga:up`+`provision` gating, :5174 CORS note, goldens location+update, demo-video path). DESIGN.md checked — no divergence (ch10 already synced the plugin/capability screens). `openspec validate --strict` + `--specs` 22/22 + `format:check` GREEN. **10/10 spot-checks** PASS. No code change. | in-progress |
| 2026-06-30 13:09 EEST | archive | archived | `openspec archive add-visual-polish-e2e --skip-specs --yes` → `changes/archive/2026-06-30-add-visual-polish-e2e`. 32/33 [x]; lone 7.4 (independent review) performed by the Gate-2 checkers — ch5/…/ch10 precedent (6.2/6.3/7.3 [x] w/ escalation annotations). Pre-commit hygiene: REVERTED prior-spec screenshot churn (the full-e2e run re-rendered 9 prior specs' committed PNGs via the hardcoded-SHOTS antipattern — triage); `.claude/` infra excluded. | in-progress |
| 2026-06-30 13:09 EEST | commit | 79cf915 | one-spec commit on main (remember-reach), 70 files: visual-regression 2-tier harness (`playwright.config`+`e2e/visual/{capture-helper,tolerances,maket-seed}`+16 committed goldens) + a11y/responsive/komga e2e + visual polish + `BottomNav.vue`/`OfflineBanner.vue` + connector-komga 406 Accept-header fix + :5174 CORS host-config + 3 stale-e2e-assertion fixes + the ch10 HEAD-baseline typecheck fix + verifier's `gate2-checker-visual-polish.test.ts` + synced `visual-fidelity` spec + README + archived change + loop artifacts. Dev deps `@axe-core/playwright`/`pixelmatch`/`pngjs`/`@types/pngjs` (audit clean). `.claude/` excluded. **Spec 11/ch11 DONE (1 attempt + a continuation; all 3 gates green). ROADMAP 1–11 + 7a + 7b COMPLETE.** ahead 14. → StopConditionJudge. | done |
| 2026-06-30 13:15 EEST | stop-judge (run 1) | **NOT-DONE → caught a real defect** | StopConditionJudge(sonnet, ≠ all makers): backlog DRAINED (`openspec list`=`[]`, 13 archived = 11+7a+7b), specs valid (22/22), tree clean-except-`.claude/`+STATE — BUT static suite **NOT green**: `typecheck`+`build` FAIL on `src/gate2-checker-visual-polish.test.ts:125` `downloadDescriptor('bk3', MEDIA_TYPE_EPUB)` (2 args; takes 1 `BookRef`). **The maker≠checker stop-condition gate worked**: the ch11 Gate-2 verifier's OWN checker test shipped a `vue-tsc`-only error `vitest` can't see (the 3rd recurrence of the authored-after-typecheck class — ch4/ch10/ch11) → commit 79cf915 had a red typecheck. | no |
| 2026-06-30 13:15 EEST | fix + re-verify | typecheck/build GREEN | Orchestrator applied the surgical test fix (pass a valid `BookRef {sourceId,bookId,mediaType,title}` not `(bookId,mediaType)` — preserves the test's `allowedOrigins` intent; the `{links:[]}` stub still exercises the `/file` fallback). `verify-gate.sh loop/artifacts/_final` → **typecheck/lint/format/test(646)/build all PASS**, audit clean. **Amended into 79cf915** so the ch11 spec commit is green + self-contained (loop invariant: one green commit per spec). → re-run StopConditionJudge for the independent GREEN confirmation. | in-progress |
| 2026-06-30 13:19 EEST | stop-judge (run 2) | **DRAINED + GREEN (confirmed) — RUN COMPLETE** | StopConditionJudge(sonnet) re-confirmed independently: static suite ALL PASS (typecheck/lint/format/**test 646/67 files**/build; audit clean); backlog `openspec list`=`[]` (zero active); specs 22/22 valid; HEAD `79cf915`; tree clean except the deliberately-uncommitted loop tooling/evidence (`loop/STATE.md` spine, `loop/artifacts/_final/` verify logs, `.claude/` skill+agents). **The full Edda web-app roadmap (changes 1–11 + 7a origin-isolation + 7b contract-harden) is implemented, gated (code+test+security+architecture+docs, maker≠checker throughout), archived, and committed to main — ahead 15 of origin (incl. this finalize commit).** Backlog drained → per policy the loop STOPS and hands back to the user to propose the next change. Consolidated demo offered (ch11's offline-happy-path.webm already walks the headline flow end-to-end). | done |





## Triage

Specs that exhausted the retry budget, out-of-scope discoveries, or requests to suppress a rule
(which the loop refuses to do on its own).

| timestamp (Europe/Kyiv) | spec | issue | suggested next step |
|-------------------------|------|-------|---------------------|
| 2026-06-29 03:07 EEST | add-app-shell | SEC LOW-1: `sw.ts` `caches.match(request)` is unscoped (no `{cacheName: SHELL_CACHE}`). Harmless now (activate prunes other caches), but future changes adding caches could wrong-hit. | Scope the lookup when offline-and-sync (change 9) introduces additional caches. |
| 2026-06-29 03:07 EEST | add-app-shell | SEC INFO-1: no CSP on the shell (pre-existing, not a regression). Shell has no inline scripts + is same-origin, so a CSP is cheap. | Consider a CSP header/meta in a later hardening change (or change 11 polish). |
| 2026-06-29 03:07 EEST | add-app-shell | ARCH advisory: `gate2-checker.test.ts` is loop process-evidence living in the durable `src/` tree. (Build-coupling already fixed via pwa-manifest.ts extraction.) | Optional: relocate loop-checker tests out of `src/` in a future cleanup; not blocking. |
| 2026-06-29 04:45 EEST | add-library-browse | SEC INFO: `coverColor` `:style` binding is trusted fixture data now; will be server-fed by Komga/OPDS. | Allowlist `coverColor` to a CSS color grammar in change 3/10 when connectors supply it. |
| 2026-06-29 04:45 EEST | add-library-browse | SEC INFO: serialization `parse*` casts inner payload (`payload as T`) without a structural guard; test-only today. | Add a structural validator before change 9 persists/restores envelopes via OPFS/Dexie. |
| 2026-06-29 04:45 EEST | add-library-browse | ARCH advisory: `progressMapKey` (fixture) string-joins the (sourceId,bookId,mediaType) tuple that core exposes only as an object via `progressKey()`; `readSummary` duck-types `.summary` off the connector outside the contract. | Add a shared `progressKeyString()` in core + a real `Connector.catalogSummary()` when `add-connector-komga` (change 3) lands. |
| 2026-06-29 06:02 EEST | add-connector-komga | SEC L1: origin allowlist in `PermissionEnforcingHttpClient` checks only the initial URL; `fetch` default `redirect:'follow'` means a 3xx to a disallowed origin isn't re-checked. No-op today (only Komga ships, network:['*']). | Set `redirect:'manual'` + re-check `originAllowed` per `Location` before the first non-`*` connector ships. |
| 2026-06-29 06:02 EEST | add-connector-komga | ARCH forward: `_opds-core.parseOpdsEntries` uses `DOMParser`, unavailable in the Worker sandbox (DESIGN §11). Low-risk now (in-process; Komga uses only the pure `resolveAcquisitionLink`). | Revisit when `connector-opds` (change 10) formalizes `_opds-core` for the sandbox. |
| 2026-06-29 06:02 EEST | add-connector-komga | FALLOW: registry/connector forward API (registry.available/resolveConnector, KOMGA_MANIFEST, createWebPluginBridge) reads as dead — zero consumers until `main.ts` wires the registry. | `add-source-flow` (change 4) consumes these; expect the dead-code flags to clear then. (RESOLVED: ch4 wired the registry; fallow now 0 dead files.) |
| 2026-06-29 07:23 EEST | add-source-flow | ARCH/CHECKER advisory: `AddSourceModal.vue` template complexity (CRAP 88 / cyclomatic 18); `connect()` persists the SourceRecord/sidebar row before awaiting `createConnectorFor` (minor transactionality — orphan record if build throws). | Decompose the modal (extract steps) + make connect() transactional (await connector before persist) in a future cleanup; not blocking. |
| 2026-06-29 07:23 EEST | add-source-flow | NAME collision: `Resolution` exists as a generic type in `core/contracts` AND a string-union in `core/dispatch` (newly co-visible). + SEC INFO-3: `btoa` Basic-auth throws on non-Latin1 passwords (fails closed; robustness). | Rename one `Resolution`; base64-encode credentials UTF-8-safely. Future spec. |
| 2026-06-29 10:30 EEST | add-source-flow | PROCESS gap (found by ch5 verifier): spec-4's `gate2/typecheck.log` recorded `vue-tsc --noEmit` clean even though `gate2-checker-add-source.test.ts:344` already had a real type error (`['kavita-native']` not in `protocols` union). Likely the checker test was written after typecheck ran, or a vue-tsc timing artifact — a hole in that attempt's evidence. Fixed forward in ch5 (cast). | When convenient, re-run `pnpm typecheck` on main to confirm 0 errors; consider having verify-gate.sh fail loudly if a just-authored test isn't included in the typecheck pass. |
| 2026-06-29 10:30 EEST | add-book-detail | ARCH/FALLOW advisory: `BookDetailView.vue` template complexity (MI 83.4 / CRAP 26.5 / cyclo 9) from loading/error/cover/pills/progress/chapters states. | Revisit at `add-format-epub` (ch6) when TOC rows land — consider extracting a `BookCoverBlock` sub-component if it grows. |
| 2026-06-29 10:30 EEST | add-book-detail | UX/SEC-Info (ch9): Komga `coverHref` is an authed `/api/v1/books/{id}/thumbnail` (401 on a bare `<img>`); today degrades to the colour cover but shows a broken-image icon (no `@error` handler), no JS errors. Also: `bookId` rendered un-encoded in the detail URL path segment (path-only, same-origin to the user's own server). | `add-offline-and-sync` (ch9): authed cover loading + an `<img @error>` fallback to title/spine text; encode `bookId` in the path if a connector ever emits slashes. |
| 2026-06-29 10:30 EEST | add-book-detail | SEC Low (carry, dup of ch2 coverColor): `coverColor` bound to object-syntax `:style` (CSSOM-safe today, Komga doesn't supply it yet) without a CSS-color allowlist. | Allowlist `coverColor` to a colour grammar when a connector first feeds it (tracked under ch2/ch10). |
| 2026-06-29 12:35 EEST | add-format-epub | SEC MEDIUM (M1): foliate renders untrusted EPUB HTML/JS in an `allow-same-origin allow-scripts` iframe on the credential-bearing app origin (`vendor/foliate-js/paginator.js`; reached via `navigator.ts:120-133`). A malicious EPUB's `<script>` could reach `localStorage` (`edda.creds.<id>`). Headless now (no book mounts on live DOM); our code doesn't worsen it. **Escalates to HIGH at change 7.** | At ch7 (reader screen) HARD-FAIL unless an isolation strategy exists: separate credential-free origin for book content and/or strict CSP on framed docs. Write an ADR. |
| 2026-06-29 12:35 EEST | add-format-epub | SEC LOW (L1): `navigator.ts:63` interpolates `font-family: ${preferences.fontFamily}` raw into the injected stylesheet (no allowlist). First-party reading-pref config (not EPUB data), so bounded; same class as the coverColor note. | Allowlist `fontFamily` to the known typefaces (Newsreader/Literata/Sans) + clamp `fontSize`/`lineHeight` when `add-reading-preferences` (ch8) owns the prefs shape. |
| 2026-06-29 12:35 EEST | add-format-epub | ARCH forward (architect's recommendation): the richer FormatHandler/Navigator surface lives in the plugin; `core/contracts` was correctly left untouched. Change 7 will need to reach `createNavigator`/`capabilities` off the neutral `FormatHandler`. | At ch7 introduce a small web sub-interface `WebFormatHandler extends FormatHandler { capabilities; createNavigator(mount) }` and narrow via a **type guard** (NOT `instanceof EpubFormatHandler`); promote the truly-neutral members (`capabilities`, `sniff`, `Navigator.on`/`destroy`) into `core/contracts` then. Reuse `EPUB_MANIFEST`/`loadEpubFormat` — do not re-declare a second EPUB manifest in app/. |
| 2026-06-29 12:35 EEST | add-format-epub | FALLOW/ARCH advisory: `navigator.ts:104 toLocator` high complexity (11 cyclomatic / 37.1 CRAP) from defensive branching over five nullable foliate `relocate.detail` fields. Structural, tested, commented — advisory, not gate-blocking. | Consider extracting `buildHref()` / `buildLocatorFromDetail()` sub-helpers when ch7 touches the navigator. |
| 2026-06-29 12:35 EEST | add-format-epub | PROCESS nit: `e2e/epub-navigator.spec.ts:15` `SHOTS` constant hardcodes `loop/artifacts/add-format-epub/attempt-1/gate1/playwright` — a loop-artifact path baked into a committed test (Pensées screenshot landed in gate1; verifier copied it to gate2; video/trace went to the right dir). | When ch7 rewrites this e2e (navigator mounts in the real reader), derive the output path from Playwright's `testInfo.outputPath()` instead of a hardcoded gate/attempt path. |
| 2026-06-29 14:10 EEST | add-reader-navigation | PROCESS (worsened): hardcoded-SHOTS antipattern now spans MULTIPLE e2e specs — re-running the full suite regenerates prior specs' COMMITTED screenshots (`add-app-shell` gate2, `add-library-browse`/`add-book-detail` gate1). `e2e/reader.spec.ts:23` also hardcodes gate1. Orchestrator reverts these prior-spec artifact churns at each commit, but it dirties the tree every e2e run. | Centralize e2e screenshot output via `testInfo.outputPath()` (or a single per-run dir) so e2e specs never write into another spec's committed `loop/artifacts/<spec>/` tree. Worth a small dedicated cleanup change. |
| 2026-06-29 14:10 EEST | add-reader-navigation | ARCH advisory: `WebFormatHandler.createNavigator` returns `Promise<EpubNavigator>` and `NavigatorOptions.source` is `EpubBytesSource` — the generic web handler is typed to EPUB specifics (import-type only, zero runtime coupling; EPUB is the sole format this milestone). | At ch10 (PDF/CBZ) introduce a `WebNavigator` base that `EpubNavigator extends`, retype `createNavigator`, generalize the source to a `PublicationSource` — a contained type-level edit; the resolution mechanism stays. |
| 2026-06-29 14:10 EEST | add-reader-navigation | ARCH note: EPUB is bundled + always-enabled at boot. A future ch10 extensions UI that let a user *disable* EPUB would make `resolveFormat(EPUB)`→installable and break the reader. | ch10 must treat bundled+always-on formats as non-disableable in the extensions registry/UI. |
| 2026-06-29 14:10 EEST | add-reader-navigation | FALLOW advisory (verifier): `useNavigator.ts:87` CRITICAL (cyclo 23 / CRAP 137.3) from the async lifecycle (cancellation + CSP path + format resolution + orphan guard + position restore); `navigator.ts:129 toLocator` HIGH (carry from ch6); `ReaderView.vue:111 onKeydown` HIGH (simple switch). Plus unused re-exports `plugins-catalog.ts:134 epubFormatCatalogEntry`, `platform/web/index.ts:19 WebFormatHandler` (barrel), `readerPreferencesStore close/requestOpen` (ch8 stub). | Consider extracting an `openAndMount()` helper from `useNavigator` to lower complexity; the unused re-exports clear when ch8/ch10/ch9 consume them. Advisory only. |
| 2026-06-29 16:56 EEST | add-reader-navigation | **BLOCKER → NEEDS A PROPOSED CHANGE (design, not loop):** ch7 reader content-isolation. In-app sanitization rejected after 4 attempts (an `allow-same-origin allow-scripts` iframe on the credential origin means any miss = `edda.creds.*` theft; diverged from foliate's routing/MIME 4 ways). User chose **Option B: origin isolation**. Open HIGHs if in-app were ever revived (don't): loadBlob seam bypass; MIME-essence vs strict-string; manifest-mirror drift (wildcard/any-ns/first-wins vs direct-child NS.OPF by-id); duplicate-href. | **Propose/explore an origin-isolation change** (`/opsx:explore`→`/opsx:propose`): run foliate ENGINE in a sandboxed cross-origin frame; app↔frame postMessage bridge for goTo/currentLocation/applyPreferences/locatorChanged/CFI; book content+scripts confined to a credential-free origin. Resolve: same-origin pagination INSIDE the isolated frame, CFI/Locator across the bridge, perf, how `connector.content()` bytes reach the frame, SW/OPFS (ADR-005). Reuse ch7's functional reader (uncommitted in tree); REPLACE `content-security.ts`/`book-loader` CSP. Then spec-loop implements it. |
| 2026-06-29 16:56 EEST | add-reader-navigation | WORKING-TREE: ch7's full functional reader is implemented but UNCOMMITTED (blocked on isolation). Also uncommitted: prior-spec screenshot churn (hardcoded SHOTS) + `.claude/` skill/agent infra (pre-existing). | Commit the reader + isolation together once the isolation change lands. Before then, `git stash`/branch ch7 work if the tree must be clean; revert prior-spec screenshot churn at that commit. |
| 2026-06-29 22:26 EEST | add-reader-navigation | **ch7 BLOCKER RESOLVED** — origin isolation (7a, ff8fec9) shipped; reader committed + secure. Remaining: formal archive of the ch7 CHANGE. | At archive, REPLACE the ch7 delta's content-isolation requirement with a pointer to `reader-isolation` (don't carry the dead in-app parse-and-strip into the main spec); then `openspec archive add-reader-navigation`. |
| 2026-06-29 22:26 EEST | add-reader-origin-isolation | Offline: the app SW can't cache the cross-origin reader frame's OWN assets (foliate bundle). Book bytes are already offline (bridged, ADR-005); the frame's bundle loads online for now. | Give the reader origin its own minimal precache (own SW) when ch9 (offline-and-sync) lands — ADR-013 open question. |
| 2026-06-29 22:26 EEST | add-reader-origin-isolation | Advisory residuals (non-blocking, gate-2 PASSED): SEC L2 (no inbound port-message SHAPE validation — defense-in-depth, lower priority now M1 closed), L3 (`reader-css.ts` interpolates pref strings unescaped — ch8 owns prefs); ARCH (navigator-proxy in `plugins/` imports `platform/web` protocol = plugin→platform note; complexity: extract `handleEvent` / fold harness `lastLocation` repetition; frame bundle ships foliate's full format suite). | See `loop/artifacts/add-reader-origin-isolation/attempt-1/gate2/{security.md,architecture.md}`. Address opportunistically in ch8/ch9/ch10. |
| 2026-06-30 00:40 EEST | harden-format-navigator-contract | Advisory (gate-2 PASSED): SEC L1 `reader-css.ts:22` interpolates `preferences.fontFamily` into CSS (first-party, confined to the credential-free cross-origin frame — no creds reach); ARCH complexity — `PublicationSourceReader.readable` getter double-cast (faithful zip.js mirror) + `sourceToTransferBuffer` reads the whole source to transfer ("bytes read twice", documented tradeoff). | fontFamily allowlist → **ch8** (`add-reading-preferences`, owns the prefs shape) — same class as the ch2/ch6 coverColor/fontFamily notes. Complexity advisories: leave as-is (no simpler structure matches the duck-type / serializable-Publication constraint). |
| 2026-06-30 00:40 EEST | harden-format-navigator-contract | ARCH/CHECKER note: `src/app/plugins-catalog.ts` still imports `EPUB_MANIFEST`+`loadEpubFormat` from `@/plugins/formats/epub/manifest` — the intentional bundled-plugin registration seam (pre-existing since 7a). The change's actual target (the `EpubNavigator`/`EpubBytesSource` TYPE coupling in `useNavigator`) IS removed; a literal `grep @/plugins/formats/epub src/app` is non-empty only because of this manifest/loader seam. | If strict app↔plugin decoupling is ever wanted, a follow-on `plugins-catalog-neutralisation` change (registry resolves the manifest by id, no static import). Out of scope for the contract hardening; not blocking. |
| 2026-06-30 01:02 EEST | add-reader-navigation | Spec wording staleness in the synced `reader-navigation` main spec (non-blocking, flagged by the archive-sync checker, covered by an in-spec reconciliation note): (1) "Open an EPUB" parenthetical says `(openResource)` but the shipped method is `connector.content()`; (2) the lifecycle requirement lists "Publication, Navigator, and **Session**" — post-ADR-013 there is no app-side `Session` (the app holds a `WebNavigator` proxy). Behavior is correct; only the prose is stale. | Opportunistic doc cleanup: rename the parenthetical to `connector.content` and drop/clarify the `Session` mention (proxy-based lifecycle) when ch8/ch9 next touch the reader spec, or in ch11 polish. |
| 2026-06-30 EEST | add-reading-preferences | Gate-3 doc-honesty: tasks 2.1–2.5 described readium-css `--USER__*`/`ReadiumCSS-before-after`/appearances but the v1 implementation delivers equivalent **direct CSS** via the established foliate `renderer.setStyles` seam (`THEME_COLORS`, `TYPEFACE_STACKS`, `SPACING_PRESETS`, `clampPt`). readium-css is vendored nowhere in the tree. Architect ruling (gate2): acceptable realization — the load-bearing invariant half (book typography inside the isolated frame, never Tailwind chrome) is honored. | **Backlog change: vendor readium-css library + move the reader-frame seam to `--USER__*` user-settings** (publisher-CSS normalization, hyphenation, Readium alignment). Blocked on: deciding the vendoring strategy (npm vs git submodule), confirming Firefox Baseline on any readium-css features used, and verifying no cascade conflict with the existing `setStyles` override. Propose as a dedicated change; not blocking ch9–ch11. |
| 2026-06-30 02:06 EEST | add-reading-preferences | Advisory (gate-2 PASSED): SEC LOW-1 `reader-css.ts:130` theme lookup `THEME_COLORS[preferences.theme]` not `Object.hasOwn`-guarded like its `resolveFontStack`/`resolveSpacing` siblings — INERT (`theme:'__proto__'`→`undefined.bg`→`background: undefined`, no CSS breakout; only a prototype-pollution vector could weaponize it and none exists), + LOW-2 no explicit malicious-`theme`/pollution test. ARCH: `THEME_COLORS` (reader-css.ts) ↔ `THEME_OPTIONS` (DisplayPanel.vue) hardcode the same 4 bg/fg hex pairs (deliberate — panel must not import frame module across the origin boundary; can drift). | LOW-1: add `Object.hasOwn(THEME_COLORS, theme)` gate + a malicious-theme test (1-line + test). Theme-hex dup: lift to one neutral shared constant both layers import. Address in **ch11 polish** (or a hardening sweep); inert, non-blocking. |
| 2026-06-30 02:06 EEST | add-reading-preferences | PROCESS: the Gate-2 verifier wrote its independent tests to a NEW top-level dir `src/gate2-verifier/reading-preferences-gate2.test.ts` (green, part of the 491). Unconventional vs prior verifiers (src/app/, src/core/contracts/). Same class as the ch1 "loop-checker tests live in durable src/" note. | Optional: relocate Gate-2 verifier tests under the spec's own area (e.g. `src/app/` or `src/platform/web/`) or a single conventional `src/__gate2__/` in a future cleanup; not blocking. |
| 2026-06-30 03:50 EEST | add-offline-and-sync | Advisory residuals (gate-2 attempt-2 PASSED): SEC LOW (non-exploitable) — `origin-allowlist.ts`/`connector-download.ts` accept a `blob:`/non-http URL whose inner origin equals the connector base (a server can't register a client Blob, so no egress); ARCH — `DexieSyncEngine.enqueue` swallows write errors (`.catch(()=>{})`, defensible fire-and-forget since the next `locatorChanged` re-enqueues + furthest-wins self-heals, but silent); FALLOW — `StreamingConnector` type in connector-download.ts + several new platform/web re-exports read as dead until a future consumer. | SEC: add a `scheme ∈ {http,https}` check to `originAllowed` (defense-in-depth). ARCH: optionally log/telemetry the enqueue write failure. Dead exports clear when ch10/ch11 consume them. All non-blocking; address opportunistically (ch10/ch11). |
| 2026-06-30 06:32 EEST | add-extensions-and-capability-install | PATTERN (cross-cutting): connectors keep reintroducing credentialed `fetch` to SERVER-SUPPLIED URLs without origin-gating (ch9 Komga, ch10 OPDS — both caught as Gate-2 HIGHs, both fixed per-connector). Per-connector opt-in is fragile. | Make origin-gating a DEFAULT in the bridge/permission layer: `permissions.network` default to `[base]` not `['*']`, and/or gate credentialed sends in `PermissionEnforcingHttpClient` so a new connector can't forget. Propose as a hardening change. |
| 2026-06-30 11:46 EEST | add-extensions-and-capability-install | PROCESS (recurrence of the ch4→ch5 gap): ch10's commit 04c946f shipped a latent `typecheck` RED in `src/gate2-checker-extensions-install.test.ts` (KeyValueStore stub w/ bogus `keys` + unused `req`) — `verify-gate.sh`'s typecheck step passed because the Gate-2 verifier finalized the checker test AFTER that step ran. ch11 fixed it forward (behavior-neutral, Gate-2-confirmed). | Strengthen `verify-gate.sh` (or the verifier procedure) so a just-authored checker test is re-typechecked before the gate is declared green — e.g. run `typecheck` LAST, or fail loudly if `git status` shows a test file newer than the typecheck log. Recurred twice now. |
| 2026-06-30 13:09 EEST | add-visual-polish-e2e | **PRODUCT GAP (capstone-surfaced, escalated; the one real follow-up):** a mid-book EPUB reading position cannot round-trip through a Komga server — Komga's read-progress API rejects every non-`completed:true` write for a non-Divina EPUB (curl-proven `{page}`/`{completed:false}`→400), and the app reads progress only from the connector (no local display fallback), so "reconnect → furthest-wins on book detail" is unachievable for a Komga EPUB. The furthest-wins POLICY is sound + unit-tested; only the Komga-EPUB server persistence is missing. | **Propose a follow-up change**: (a) connector-komga — use Komga's R2 **locator-based** progress endpoint (if available on the server version) for EPUB position; and/or (b) offline-and-sync — a LOCAL progress cache so a downloaded book's position is displayable/resumable even when the server can't store it. Not blocking v1 (offline reading + paging + reconnect all work). |
| 2026-06-30 13:09 EEST | add-visual-polish-e2e | PROCESS (hardcoded-SHOTS antipattern STILL unresolved, now repo-wide): running the FULL e2e suite (as Gate-2 must) re-renders 9 prior specs' COMMITTED screenshots under `loop/artifacts/<spec>/.../playwright/*.png`; the orchestrator reverts this churn at every commit, but it dirties the tree each run. | A dedicated cleanup change: route ALL e2e screenshot output via Playwright `testInfo.outputPath()` (or one per-run dir) so specs never write into another spec's committed `loop/artifacts/` tree. Tracked since ch6/ch7; worth doing now the backlog is drained. |
| 2026-06-30 13:09 EEST | add-visual-polish-e2e | Advisory (gate-2 PASSED): SEC LOW — offline-e2e console-error allowlist (`offline-happy-path.spec.ts:81-82`) keys on a broad `localhost:25600` token (can't mask a product vuln; the `pageerror` JS-fault guard is unfiltered). ARCH — `OfflineBanner` re-implements the `navigator.onLine`+listener idiom (also a non-reactive predicate in `sync-scheduler.ts`); no `useOnline` composable / VueUse exists so nothing available is duplicated. PROCESS — verifier tests in top-level `src/gate2-checker-visual-polish.test.ts` (+ ch10's `gate2-checker-extensions-install.test.ts`) — same unconventional-durable-src/ class as ch1/ch8. | LOW: anchor the console regex on host scheme. ARCH: optionally extract `src/app/composables/useOnline.ts`. PROCESS: relocate the loop-checker tests under a conventional `src/__gate2__/` in the cleanup sweep. All non-blocking. |
