# tf-guard runs keyless and offline — no credentials are required to build, test,
# eval, or run it (NFR-OFFLINE-01). This file exists only to document the OPTIONAL
# swap of the deterministic eval judge for an LLM judge (see evals/rubric.ts).
#
# Copy to .env (git-ignored) ONLY if you wire an LLM judge yourself. Never commit
# real keys — the pre-commit hook scans for them.

# ANTHROPIC_API_KEY=
