// Domain schemas are added per capability slice (db/schema/<domain>.ts) and
// re-exported here. Empty until the first slice — loop-first: no domain code
// before its spec + failing tests exist.
export {};
