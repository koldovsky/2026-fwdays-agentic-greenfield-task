// Deterministic demo seed. Real seed data is added once domains exist (the
// Phase 5 seed helper: deterministic IDs, idempotent upserts, re-pinned
// baseline state). Stub for now so `npm run db:seed` is wired from Phase 0.
console.log("db:seed — no domains to seed yet (added per slice / Phase 5).");
