Selectable filter pill — the History screen's horizontal tag-filter row.

```jsx
<FilterChip selected>All</FilterChip>
<FilterChip dot="#F5A300">Design</FilterChip>
<FilterChip dot="#7BC57F">Admin</FilterChip>
```

Fills amber when `selected`. Pass `dot` for a leading color dot matching a tag.
