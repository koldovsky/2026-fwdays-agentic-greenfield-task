Surface container with warm soft shadow. Wraps entries, stat blocks, and settings groups.

```jsx
<Card padding={20}>…</Card>
<Card glow>{/* live running timer */}</Card>
```

`elevation` 0–3 sets shadow depth. `glow` swaps in the amber running-timer ring (use only for an active timer).
