Round avatar — image or amber-gradient initials.

```jsx
<Avatar name="Maya Okonkwo" size={56} />
<Avatar src="/me.jpg" name="Maya" />
```

Derives up to two initials from `name`. Falls back to a 🐝 when empty.
