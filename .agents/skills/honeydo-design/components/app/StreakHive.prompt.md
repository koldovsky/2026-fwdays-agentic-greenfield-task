Streak display — honeycomb cells fill as the streak grows; a bee lands on the newest day.

```jsx
<StreakHive filled={5} total={7} current label="5-day streak · keep it warm" />
```

`filled` amber cells, the rest dashed outlines. `current` puts a 🐝 on the latest filled cell.
