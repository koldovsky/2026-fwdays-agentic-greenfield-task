Donut + legend for language share across repos. `pct` values are 0–1; segments fall back to a warm palette when no `color` given.

```jsx
<LanguageDonut total={5} languages={[{name:'TypeScript',pct:.4},{name:'Go',pct:.3}]} />
```
