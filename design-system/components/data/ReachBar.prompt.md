One horizontal magnitude bar with a right-aligned serif value. Stack several (sharing one `max`) to compare stars / forks / watchers / followers.

```jsx
<ReachBar label="Stars" value={5600} valueFmt="5.6k" max={5600} />
```
