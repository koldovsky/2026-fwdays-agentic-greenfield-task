Vertical bar chart for a small set of counts (e.g. 90-day commits / PRs / issues / releases). Bars scale to the max value.

```jsx
<ActivityChart data={[{label:'Commits',value:140},{label:'PRs',value:2}]} />
```
