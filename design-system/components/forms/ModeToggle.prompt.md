Segmented control for 2–3 mutually exclusive modes (e.g. Explore vs Compare, or the Landing/Stats/Battle nav).

```jsx
<ModeToggle options={['Explore','Compare']} value={mode} onChange={setMode} />
```
