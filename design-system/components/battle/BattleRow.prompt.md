One metric row in the battle table: A value | centered label | B value. The winning side is colored + semibold; a draw leaves both muted. Stack inside a bordered, rounded container; reveal rows with a staggered fade-up.

```jsx
<BattleRow label="Total stars" aValue="5.6k" bValue="812" winner="a" />
```
