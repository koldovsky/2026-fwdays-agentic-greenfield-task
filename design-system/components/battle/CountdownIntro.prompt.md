Big serif countdown number that pops in on each tick (3 → 2 → 1 → "FIGHT") before the battle reveal begins. Drive `count` from a timer; the `key={count}` re-triggers the pop.

```jsx
<CountdownIntro count={2} />
```
