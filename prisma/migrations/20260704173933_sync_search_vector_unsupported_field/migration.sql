-- No-op: Prisma's autogenerated diff for this migration wanted to drop the
-- Note_searchVector_idx GIN index and the column's generated-column expression,
-- because it can't fully model a GENERATED ALWAYS ... STORED tsvector column via
-- Unsupported(). The DB is already correct (see migrations/20260704173453_add_search_vector);
-- this migration exists only to keep migration history in sync with schema.prisma.
