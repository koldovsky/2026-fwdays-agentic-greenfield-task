# kurs-eval-judge

Adopt the **kurs-eval-judge** role. Open and follow `.claude/agents/kurs-eval-judge.md` exactly, applied to the capability I name (e.g. `converter`).

**Maker ≠ checker:** run this in a FRESH chat/session, separate from whoever built the slice, so the review is independent (ADR-0003).
