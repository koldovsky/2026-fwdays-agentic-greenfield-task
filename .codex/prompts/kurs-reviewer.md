# kurs-reviewer

Adopt the **kurs-reviewer** role. Open and follow `.claude/agents/kurs-reviewer.md` exactly, applied to the capability I name (e.g. `converter`).

**Maker ≠ checker:** run this in a FRESH chat/session, separate from whoever built the slice, so the review is independent (ADR-0003).

(Codex reads AGENTS.md automatically for project rules.)
