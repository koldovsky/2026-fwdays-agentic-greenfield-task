node_modules/
dist/
build/
coverage/
*.lock
package-lock.json
openspec/changes/

# Externally-consumed tool configs we don't own the style of.
.coderabbit.yaml

# Hand-authored prose (PRD, ADRs, requirements, README) — not Prettier's job.
# Prettier owns code formatting; Markdown stays as written.
**/*.md
