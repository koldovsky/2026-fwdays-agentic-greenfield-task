# qa-011-normalize-objects.md

## Контекст

dev-010 виконано — `src/siebel/workspaces.js` містить `normalizeObjects`.  
Функція критична: Siebel повертає поле `Object` у трьох варіантах, і неправильна обробка призведе до пропуску Business Service або падіння застосунку.

---

## Що зробити

Створити `tests/normalizeObjects.test.js` з вичерпним покриттям усіх трьох варіантів поля `Object`.

### Тест-кейси

**Варіант 1 — поле відсутнє:**
```js
it('повертає [] якщо Object відсутнє (undefined)', ...)
it('повертає [] якщо Object є null', ...)
```

**Варіант 2 — одиночний об'єкт:**
```js
it('загортає одиночний об\'єкт у масив', ...)
it('зберігає всі поля об\'єкту при загортанні', ...)
```

**Варіант 3 — масив:**
```js
it('повертає масив без змін', ...)
it('повертає порожній масив без змін', ...)
```

**Граничні випадки:**
```js
it('повертає [] для порожнього рядка (falsy)', ...)
it('повертає [] для 0 (falsy)', ...)
```

---

## Критерії готовності (Definition of Done)

- [x] `tests/normalizeObjects.test.js` існує
- [x] Покрито всі три варіанти: `undefined`, одиночний об'єкт, масив
- [x] `npx jest tests/normalizeObjects.test.js` — всі тести зелені (10/10)
- [x] Тести не залежать від мережі або файлової системи

---

## Приклади / Референси

- `docs/SPEC.md` → секція "⚠️ Критична особливість: поле Object"
- `src/siebel/workspaces.js` → реалізація `normalizeObjects`

---

## Статус

[x] TODO → [x] IN PROGRESS → [x] DONE
