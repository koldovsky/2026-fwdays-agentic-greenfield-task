// Public API for the paywall widget (add-payments-emulator task 2.1,
// FR-PAYWALL-01/02) — other layers import ONLY this barrel.
export { Paywall, type PaywallProps, type PaywallReason } from "./ui/Paywall";
