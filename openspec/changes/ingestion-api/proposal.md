## Why

Платформа збирає дані про конверсії з Google Таблиць користувачів через Apps Script, але наразі відсутній REST API ендпоінт для прийому цих даних. Без `POST /api/conversions` система не може отримувати записи конверсій, а аналітичний дашборд залишатиметься порожнім. Це четвертий крок за планом реалізації — базова інфраструктура (БД, автентифікація, білінг) вже готова.

## What Changes

- Створюється новий API Route Handler `POST /api/conversions` для приймання масивів записів конверсій від Apps Script.
- Реалізується автентифікація запитів через заголовок `X-API-Key` (пошук користувача за хешем ключа в таблиці `users.api_key_hash`).
- Перевіряється статус підписки перед прийомом даних — запити відхиляються з `402 Payment Required` або `403 Forbidden`, якщо підписка неактивна.
- Дані конверсій валідуються та записуються до існуючої таблиці `conversions` (схема вже створена у `core-database`).
- Створюється Cron-задача для щоденного автоматичного очищення записів конверсій, старших за 14 місяців.
- Забезпечується час відповіді API ≤ 200 мс на p95 (NFR-PERF-01).

## Capabilities

### New Capabilities
- `ingestion-api`: REST API ендпоінт прийому конверсій з Google Таблиць, автентифікація за API-ключем, перевірка підписки, валідація та збереження записів, автоматичне очищення застарілих даних.

### Modified Capabilities
_(немає змін у вимогах існуючих спеків)_

## Impact

- **Нові файли**: `app/api/conversions/route.ts` (API handler), `app/api/conversions/cleanup/route.ts` (Cron-ендпоінт очищення), `vercel.json` (конфігурація Vercel Cron).
- **Існуючі залежності**: таблиці `users` (поле `api_key_hash` для автентифікації), `subscriptions` (перевірка статусу), `conversions` (запис даних) — все вже реалізовано у `core-database`.
- **Зовнішні системи**: Google Apps Script (клієнт API), Vercel Cron (планувальник очищення).
- **API контракт**: `POST /api/conversions` стає публічним ендпоінтом, що використовується зовнішніми Apps Script.
