## 1. Кодова валідація та інспекція

- [ ] 1.1 Перевірити відповідність структури даних та формату повідомлень у `lib/notifications.ts` вимогам `FR-NOTIF-01` та `FR-NOTIF-03`.
- [ ] 1.2 Провести інспекцію шаблонів повідомлень користувачам та адміну на повну відповідність `BC-BRAND-01` (відсутність знаків оклику та спокійний тон).
- [ ] 1.3 Перевірити відповідність мовних вимог `NFR-I18N-01` (всі повідомлення мають бути виключно українською мовою).
- [ ] 1.4 Перевірити обробку помилок та лімітів Telegram API у `lib/notifications.ts` відповідно до `FR-NOTIF-07` (асинхронність, неблокуюча поведінка, логування помилок `403` та `429`).

## 2. Перевірка інтеграції тригерів сповіщень

- [ ] 2.1 Перевірити правильність виклику сповіщень адміністратора та користувачів у платіжному вебхуці (`app/api/billing/webhook/route.ts`).
- [ ] 2.2 Перевірити інтеграцію сповіщень про невдале списання та призупинення сервісу в Cron-задачі автосписання (`scripts/billing-cron.ts`).
- [ ] 2.3 Перевірити виклики сповіщень адміністратора в ендпоінтах зміни статусів підписки (скасування, пауза, поновлення).

## 3. Створення тестового скрипта та запуск верифікації

- [ ] 3.1 Створити скрипт `scripts/test-notifications.ts` для симуляції відправки всіх видів сповіщень (адміністратора та користувачів) з логуванням вихідного тексту в консоль.
- [ ] 3.2 Запустити тестовий скрипт `scripts/test-notifications.ts` та візуально перевірити форматування повідомлень (включаючи відображення дати/часу за київським часом у форматі `ДД.ММ.РРРР ГГ:ХХ`), точність підстановки даних, тон та відсутність знаків оклику, а також симулювати помилки API.
- [ ] 3.3 Перевірити успішність компіляції проекту за допомогою `npx tsc --noEmit` та відсутність помилок лінтера через `npm run lint`.
