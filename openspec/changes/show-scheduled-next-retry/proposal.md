# Show next cron retry on scheduled bookings

Users with `ready` or `waiting` jobs cannot tell when the system will try again. Surface the next cron run time in Calgary (America/Edmonton), matching `deploy/colibri-schedule.cron`.
