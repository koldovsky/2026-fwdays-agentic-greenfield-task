## 1. Failing tests first (red)

- [x] 1.1 Write unit tests for the optional-note rule in `lib/watering/validation.ts` (the slice's one validation decision, design D2): ACCEPT a typical note ("полив дощовою водою"), an exactly-500-character note, an omitted note, and a whitespace-only note (the last two normalize to `null`, NOT `""`); REJECT a 501-character note (trimmed length) with a field-keyed Ukrainian message — never truncated, never silently accepted (FR-WATER-02). Confirm they FAIL.
- [x] 1.2 Write unit tests for the watering date rule in `lib/watering/validation.ts` (via the shared `lib/dates.ts`, design D3): an omitted/empty date defaults to today in Europe/Kiev under a fixed clock; a valid past/today `YYYY-MM-DD` is accepted; a non-`YYYY-MM-DD` string, an impossible date ("2026-13-40", "2026-02-30"), and a date after today in Europe/Kiev are each rejected with a field-keyed Ukrainian message (SC-1, SC-2). Confirm they FAIL.
- [x] 1.3 Write unit tests for the `formData -> WateringInput` mapper + issue -> Ukrainian field-message mapping in `lib/watering/validation.ts`: a failing submit echoes the raw submitted strings under `values` (date + note) for uncontrolled-form repopulation, and never throws (FR-SHELL-03). Confirm they FAIL.
- [x] 1.4 Write unit tests for `lib/watering/queries.ts` + `lib/watering/service.ts` against a real in-memory SQLite (`DATABASE_URL=:memory:`, migrations applied, db handle INJECTED as the first param like growth): `insertWatering` returns a row with id + plantId + wateredOn + note (note `null` when omitted); `listWaterings(db, plantId)` returns that plant's rows ordered by `wateredOn DESC, id DESC` (assert the SC-3 same-date tie-break with two rows sharing a date); `getWatering(id)` returns the row or a not-found signal; `updateWatering` changes only the given row's date + note; `deleteWatering` removes one row and is a no-op not-found for a missing id; inserting against a non-existent plantId is rejected by the FK. Confirm they FAIL.
- [x] 1.5 Write a REAL-SQLite cascade + smoke-flow test (`tests/integration/`, `:memory:` or temp file, design D4, SC-5): create plant A and plant B; log waterings on plant A (incl. two on the same date, one with a note, one without) -> they appear ordered date desc then id desc -> edit one watering's date + note -> reflected -> delete ONE watering after confirm -> only that row is gone, plant A's other waterings + plant A's measurements + plant B's data untouched; then delete plant A -> plant A's waterings (and measurements) cascade-removed while plant B's data is intact (FR-WATER-01..05, SC-3, SC-5, NFR-DATA-01/02). Confirm it FAILS.
- [x] 1.6 Write tests for the server actions `lib/watering/actions.ts`: each returns the shared `ActionResult`; on a validation failure returns `{ ok: false, fieldErrors, values }` (echoes submitted strings, never throws); on success returns `{ ok: true }` and triggers `revalidatePath` for the plant detail; logging against a non-existent plantId returns a not-found result (not a thrown 500); an edit/delete of a missing watering id returns a not-found result; a malformed id (float/0/negative/NaN) resolves to a friendly not-found, never a write (design D5). Confirm they FAIL.
- [x] 1.7 Write component tests for the add/edit watering form island `components/watering/WateringForm.tsx`: renders a native `<input type="date">` defaulting to today and an OPTIONAL note field; every field has an accessible label; renders `FormErrorBanner` + a `FieldError` per field; on a `{ ok:false }` result repopulates uncontrolled date + note inputs from `result.values`; submitting with the note empty is allowed (note optional); an over-length note surfaces an inline note `FieldError` (FR-WATER-01/02, NFR-A11Y-04, NFR-USA-02, SC-1, SC-6). Confirm they FAIL.
- [x] 1.8 Write component tests for the delete-with-confirm control `components/watering/DeleteWateringButton.tsx`: the control requires an explicit confirmation step before invoking the delete action; CANCEL at the confirm step leaves the row intact and fires no action; CONFIRM invokes the action once; the control is keyboard-operable with an accessible label (FR-WATER-05, SC-5, SC-6, NFR-USA-02). Confirm they FAIL. (Explicitly its own task — prior slices initially missed the delete confirm/cancel component coverage.)
- [x] 1.9 Write component tests for the waterings section `components/watering/WateringsSection.tsx` (+ `WateringRow`): renders the list ordered date desc (most recent first) showing `DD.MM.YYYY` and the note (or a clear "no note" affordance), renders a clear empty state when there are none, and shows the add form; the waterings section renders in isolation without colliding with the measurements section (design R7) (FR-WATER-03, NFR-A11Y-03, SC-3). Confirm they FAIL.

## 2. Implement to green

- [x] 2.1 Define `db/schema/watering.ts` (`id` PK autoincrement, `plantId` integer NOT NULL `references(() => plants.id, { onDelete: "cascade" })`, `wateredOn` text NOT NULL ISO date, `note` text NULLABLE, `createdAt` text default `CURRENT_TIMESTAMP`) with `Watering` / `NewWatering` types, and re-export from `db/schema/index.ts` (design D1, D4).
- [x] 2.2 Generate the migration with `npm run db:generate`; commit the SQL + meta snapshot under `db/migrations/`. Confirm `db/client.ts` auto-applies it on startup and `PRAGMA foreign_keys = ON` enforces the cascade (design D7).
- [x] 2.3 Implement `lib/watering/validation.ts`: the optional-note rule (trim; empty/whitespace-only -> `null`; reject > `NOTE_MAX_LEN = 500` trimmed chars), the date rule (via `@/lib/dates`, default to `todayInKiev()`, reject malformed/impossible/future per SC-2), the `formData -> WateringInput` mapper, and the issue -> Ukrainian field-message mapping; echo submitted `values` on failure; never throw (design D2, D3). Green for 1.1, 1.2, 1.3.
- [x] 2.4 Implement `lib/watering/queries.ts` (db INJECTED as first param: `listWaterings`, `getWatering`, `insertWatering`, `updateWatering`, `deleteWatering`; list `ORDER BY wateredOn DESC, id DESC` for SC-3) and `lib/watering/service.ts` (orchestration + not-found signal + plant-exists/FK handling) (design D4, D5). Green for 1.4.
- [x] 2.5 Implement `lib/watering/actions.ts` server actions (`createWateringAction`, `updateWateringAction`, `deleteWateringAction`): defense-in-depth integer-id guard -> validate -> service -> `revalidatePath('/plants/<plantId>')`; return the shared `ActionResult`; catch + translate validation / FK / SQLite driver errors -> Ukrainian message (`uk.errors.generic` for whole-form); echo `values` on failure; never throw raw (design D5, error-handling strategy). Green for 1.6.
- [x] 2.6 Extend `lib/i18n/uk.ts` with a `watering` copy block (section heading, watering-date label + hint, note label + optional hint, add/save/cancel, list/row labels, no-note affordance, empty-state, delete-confirm prompt + confirm/cancel, not-found, and `fieldErrors` for date: invalid/future, and for note: too-long) — Ukrainian text, English identifiers (NFR-LOC-01).
- [x] 2.7 Implement the add/edit watering form island `components/watering/WateringForm.tsx` (client) over the actions: `useActionState`, `FormErrorBanner` + per-field `FieldError`, `defaultValue` repopulation from `result.values`, a native `<input type="date">` defaulting to today, an OPTIONAL note field, accessible labels (design D6, slice-2/3 pattern). Green for 1.7.
- [x] 2.8 Implement the delete-with-confirm control `components/watering/DeleteWateringButton.tsx` (explicit confirmation step before the action fires; cancel leaves the row intact and fires no action; keyboard-operable, accessible label) (design D6, SC-5). Green for 1.8.
- [x] 2.9 Implement `components/watering/WateringRow.tsx` (shows `DD.MM.YYYY` + the note or a "no note" affordance, with inline edit toggle reusing `WateringForm` prefilled + the delete-with-confirm control) and `components/watering/WateringsSection.tsx` (list ordered date desc then id desc, empty state, add form) (design D6, SC-3). Green for 1.9.
- [x] 2.10 Add the waterings section to `app/plants/[id]/page.tsx`, ALONGSIDE the existing measurements section: server-side `listWaterings(db, plantId)`, render the list ordered date desc then id desc (`DD.MM.YYYY` + note) or the empty state, plus the add form and per-row edit + delete-with-confirm controls; namespace field ids/scope so the two sections do not collide (FR-WATER-03, NFR-A11Y-03, SC-3, design D6, R7). Green for the section-on-page part of 1.9.
- [x] 2.11 Confirm all tests from group 1 now pass (green), and re-run the existing growth + plants suites to confirm the new sibling section did not regress them.

## 3. Accessibility (Phase 6, deferred — plain bullets)

> DEFERRED to Phase 6 (cross-cutting QA: axe light+dark + vision-verify across ALL
> capabilities). The rendered-result a11y gates below run once, against the whole
> app, in Phase 6 rather than per-slice — honestly deferred, not skipped. The
> jsdom-level a11y for this slice (accessible labels on the date + note fields, the
> `FieldError` association convention, the keyboard-operable delete confirm) is
> covered by the slice's component tests (1.7, 1.8, 1.9).

- (Phase 6) Run `npm run check:a11y` (axe) on the waterings section, add/edit form, and delete-confirm control in BOTH light and dark themes; resolve any violations (NFR-A11Y-01).
- (Phase 6) Verify WCAG 2.1 AA contrast for the waterings list, form, and delete-confirm control in both themes (NFR-A11Y-02).
- (Phase 6) Verify keyboard-only operation: visible focus, accessible names on the date + note fields and the delete control, the native date picker reachable, and that `FieldError` messages are programmatically associated with their fields (NFR-A11Y-04, SC-6).
- (Phase 6) Verify the waterings section + form (alongside the measurements section) at a 360 px viewport: content visible/operable with no horizontal overflow (NFR-COMPAT-01).

## 4. Validation, docs, and archive

- [x] 4.1 Run `npm run lint` — zero errors.
- [x] 4.2 Run `npm run test:run` — all unit tests pass (incl. the integration cascade/smoke flow 1.5 and the re-run growth + plants suites from 2.11).
- [x] 4.3 Run `npm run build` — production build succeeds.
- [x] 4.4 Run `npx openspec validate add-watering --strict` — no errors.
- [x] 4.5 Run `npx openspec validate --all --strict` — no errors.
- [ ] 4.6 Manual real-DB smoke test: apply the migration with `npm run db:migrate` against the real SQLite file (or rely on `db/client.ts` auto-migrate), then `npm run dev` and:
  1. open an existing plant's detail `/plants/<id>` — confirm the waterings section shows the helpful Ukrainian empty state (no waterings yet) and sits alongside the measurements section without layout/label collision;
  2. log a watering with NO date and NO note — confirm it is accepted, dated today, has no note, and appears in the list as `DD.MM.YYYY`;
  3. log a second watering on the SAME date WITH a note — confirm both appear, the more recently created one is listed first (SC-3 tie-break), and its note is shown;
  4. log a third watering on an EARLIER date — confirm the list is ordered date descending (newest date first);
  5. attempt to log a date in the FUTURE and a note longer than 500 characters in turn — confirm EACH is rejected inline next to the offending field (no row written), never a raw 500, and the typed values stay on the screen; confirm a whitespace-only note is accepted and stored as no note (NULL);
  6. edit a watering to a new valid date + a changed note — confirm the list reflects the change; then clear the note and save — confirm the note is removed (NULL); then edit it to a future date — confirm the original row is unchanged;
  7. reload the app — confirm the waterings are still present (persistence);
  8. delete ONE watering via the explicit confirm step, first CANCEL (row intact), then CONFIRM — confirm only that row is gone and the plant + its other waterings + its measurements are untouched (single-row delete, no cascade);
  9. delete the PARENT plant via its confirm step — confirm its waterings (and measurements) are cascade-removed while another plant's data remains (FK cascade);
  10. open `/plants/<bad-id>` — confirm the friendly not-found state (no waterings section error).
- [ ] 4.7 Update `docs/current-state.md` (date/time in Europe/Kiev, current phase, watering moved planned -> implemented, third DB slice + second plant child landed, two children now cascade from the plant). README is the course/homework README with no watering/DB section — N/A there; the DB/watering notes live in `docs/current-state.md`.
- [ ] 4.8 Only after the smoke test (4.6) passes, archive MANUALLY (this project archives change folders by hand to avoid the CLI conflicting with the pre-authored baseline specs): move `openspec/changes/add-watering/` to `openspec/changes/archive/YYYY-MM-DD-add-watering/` (date in Europe/Kiev). Do NOT run `npx openspec archive`.
