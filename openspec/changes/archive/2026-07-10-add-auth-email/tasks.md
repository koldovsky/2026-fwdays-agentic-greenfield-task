<!-- Recorded retroactively: slice 001 is already built and verified, so every task is checked. -->

## 1. Data model and migration
- [x] 1.1 Create `users` (email CITEXT UNIQUE, password_hash, timezone, coach_language) and `user_sessions` (opaque token, expires_at) tables
- [x] 1.2 Alembic migration `0001_auth_email`; enable the `citext` extension; hand-review the autogenerated SQL

## 2. Auth mechanics
- [x] 2.1 bcrypt password hashing (cost 12) in `app/core/security.py`
- [x] 2.2 Session cookie (`HttpOnly; SameSite=None; Path=/`, env-driven Secure) and CSRF double-submit in `app/api/deps.py`
- [x] 2.3 `CurrentUser` dependency: resolve session cookie -> non-expired session -> owning user

## 3. Endpoints and services
- [x] 3.1 `POST /api/auth/register` (409 on duplicate email)
- [x] 3.2 `POST /api/auth/login` (sets cookie, issues CSRF token)
- [x] 3.3 `POST /api/auth/logout` (deletes session, clears cookie)
- [x] 3.4 `GET /api/auth/me` (protected; 401 without a valid session)
- [x] 3.5 `app/services/auth.py` and the user_id-scoped `app/repos/` layer

## 4. Frontend
- [x] 4.1 Auth screen (login/register) per DESIGN §7.1
- [x] 4.2 `src/api.ts` sends credentials and attaches the CSRF header on mutations

## 5. Config and tests
- [x] 5.1 Auth config in `app/config.py` (cookie Secure, SameSite, session TTL, CSRF, session secret)
- [x] 5.2 8 acceptance tests + password-hash check in `backend/tests/test_auth.py`, run against real Postgres
- [x] 5.3 `scripts/verify.*` green (ruff, mypy, alembic, pytest, frontend build)
