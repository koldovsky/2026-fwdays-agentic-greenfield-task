<!-- Authored before code (spec-first): every task is unchecked. The pure grounding validator and its
     unit tests (section 2) go RED first; the data model + migration, service, provider client, and
     routes (sections 1, 3-6) follow to green. This slice ships ONE Alembic migration (the two new coach
     tables) and NO frontend (the coach drawer is FR-SHELL-02, a later slice). No live LLM call runs in
     the committed gate — the eval cases are deterministic fixtures (NFR-COST-01). -->

## 1. Data model + migration (schema change ⇒ Alembic migration, TC-STACK-01)
- [ ] 1.1 `app/models/coach.py`: `CoachMessage` (`user_id` FK, `role` TEXT `user|coach`, `content` TEXT NOT NULL, `language` TEXT NOT NULL, `created_at`, INDEX `(user_id, created_at)`) and `CoachInsight` (`user_id` FK, `week_start` DATE NOT NULL, `payload` JSONB NOT NULL, `UNIQUE(user_id, week_start)`), per architecture §2.1 — register both in `app/models/__init__.py` (append only)
- [ ] 1.2 One Alembic migration creating `coach_messages` and `coach_insights` with hand-reviewed SQL (the `(user_id, created_at)` index and the `UNIQUE(user_id, week_start)` constraint); **do NOT re-add `users.coach_language`** — it already exists (slice 001, migration `0001_auth_email`); confirm `alembic upgrade head` applies cleanly
- [ ] 1.3 `app/repos/coach.py`: user_id-scoped reads/writes — append a `coach_messages` turn, read the recent thread (newest-first), upsert `coach_insights` for `(user_id, week_start)`, read the cached insight. **Every method takes `user_id` and scopes every query by it** (FR-AUTH-07); no method without `user_id`

## 2. Grounding validator — pure, framework-free, TEST-FIRST (FR-COACH-02, §4.4)
- [ ] 2.1 Write RED unit tests for `app/core/grounding.py` **first**, each `@trace FR-COACH-02`: an out-of-snapshot unit-bearing number (e.g. `180 min`) is a **violation** (E-9); a derived rendering of a snapshot value is **allowed** (`focus.deep_share` `0.43` -> `43%`; `focus.deep_minutes` `262` -> `4:22`); the local-time leaf `consistency.median_start_local` `09:40` is **allowed** as its `HH:MM` literal while a **date component** of `window.start` is **excluded** from numeric extraction (determinism); a **bare digit 0-9 without units** absent from the snapshot (e.g. "your 8 deep blocks") is **exempt** (the extractor emits `8`, the exemption clears it) — and a **spelled-out** number ("eight") yields no digit token; a number present in the user's current chat message is allowed; a **non-clean share** ≈ `0.4295` rendered `43%` is **allowed** (`round(v*100)=43`, both-rounding — not a false-positive); a **null / low-confidence** `consistency` leaf (< 3 active days, §3.2) contributes no number and is never cited as `0` — fixture-driven, no DB, no network
- [ ] 2.2 Implement `app/core/grounding.py` (pure, no FastAPI/SQLAlchemy imports, like `app/core/snapshot.py`) — build the allowed-number set **deterministically from the real `SnapshotResponse` leaves** so two implementers derive the identical set: (1a) **every numeric leaf** (`window.days`; `volume.*_min` + `daily_avg_30d_min` + each `per_day[].min`; `consistency.score/regularity/start_stability`; `focus.deep_count/deep_minutes/deep_share`; `switching.per_day[].switches/interruptions/switch_load` + `baseline_mean`; `streaks.current/longest`; each `baselines.*.value/.delta`; `top_categories[].id/week_min`; `per_category_per_day[].id` + `.per_day[].min`) at **fixed granularities** — raw `v` + `round(v)` + `round(v, 1)`; (1b) **share-typed** (`focus.deep_share`, `baselines.focus_share.value`) also as **percent** at both granularities — `round(v*100)` + `round(v*100, 1)`; (1c) **minute-typed** (`*_min`, `deep_minutes`, `daily_avg_30d_min`, every `per_day[].min`, **and `top_categories[].week_min`**) also as **`h:mm`** computed from **both** `floor(v)` **and** `round(v)` minutes; (1d) `consistency.median_start_local` (non-null) as its **`HH:MM`** literal; (1e) **date components excluded**; plus numbers in the user's message. **Where a rounding choice exists include BOTH the floored and the rounded rendering** (deterministic, no false-positive); a **null** leaf contributes nothing (the "when non-null" guards, architecture §3.2). Then (2) extract output numeric tokens (ints, decimals, percents, `h:mm`); (3) exempt bare integers 0-9 without units (**§4.4 item 3**); (4) any other number = violation, per §4.4
- [ ] 2.3 The validator returns a structured result (is-grounded + the offending tokens) so the service can log the violation (NFR-REL-01) and the eval suite can reuse the exact same function (§4.4: "the same validator is the core criterion of the eval suite")

## 3. Request assembly + memory (FR-COACH-04, §4.1/§4.3)
- [ ] 3.1 Build the snapshot by **reusing** `app/services/stats.py::StatsService.get_snapshot(user_id=…, window=None)` (-> pure `build_snapshot(...)`) — the same artifact `GET /api/stats/snapshot` returns; do not re-derive any number
- [ ] 3.2 Assemble the request newest-first to the §4.3 budget: system (~400 tok) + snapshot (~600 tok) + most recent turns up to **~2,000 tokens** (approx `len(chars)/4`), **hard cap 20 turns**, older turns dropped, no summarization; RED tests `@trace FR-COACH-04`: request contains snapshot + history only (**no raw session/pause row**); **the 20-turn cap** trims a >20-turn thread newest-first to 20; **the token-budget limb** — ≤20 turns whose combined size exceeds ~2,000 tokens drops the oldest turns newest-first until it fits (the exact crossing turn need not be pinned)
- [ ] 3.3 The prompt serializes only the snapshot and the stored turns — assert no `sessions`/`pause_segments` field is present (FR-COACH-04 grounding of "raw session rows never sent")

## 4. Provider client + fallback ladder (FR-COACH-07, §4.5, TC-LLM-01)
- [ ] 4.1 `app/services/coach.py` LLM client: primary model **`gemma-4-31b-it`** via Google AI Studio with structured (JSON-Schema) output, requesting the §4.2 shape; read the key ONLY from `settings.google_ai_api_key` (TC-STACK-02) — never hardcode/log/print it
- [ ] 4.2 The **one** unified ladder (encode identically to FR-COACH-02/07): (1) primary `gemma-4-31b-it` -> grounding-validate; (2) on a **grounding violation**, one corrective retry on `gemma-4-31b-it`; (3) on a **second grounding failure / transport error / schema-invalid JSON**, a **single** `Gemini 3 Flash` attempt (**no** further corrective retry); (4) then the defined fallback card. **Encode the model ids exactly as TC-LLM-01 states them** and design so a provider error for **any** model id degrades gracefully (a wrong id never crashes the app, NFR-REL-01)
- [ ] 4.5 **Fallback card shape (B4/MINOR-1):** a §4.2-conforming payload — `quiet: true`, **exactly one** short **non-numeric** observation, **empty** `recommendations`, **zero numeric content**, plus the reserved **fallback marker** set to `true`. That marker is one **server-set** boolean **present on every payload** (`false` on normal cards, `true` only here) and is **excluded from the model-facing structured-output JSON-Schema** (a model can never forge a fallback card); exact copy/HTTP status stay implementation detail. RED test: the fallback card validates against the §4.2 schema, carries no numeric token, and its marker is `true` while a normal card's is `false`
- [ ] 4.3 Missing/empty `settings.google_ai_api_key` -> return the fallback card without crashing (no call attempted); log every failure (NFR-REL-01); RED tests `@trace FR-COACH-07`: malformed JSON on both models -> fallback card, no crash, no fabricated number (E-8); missing key -> fallback card; grounding-violation-twice -> fallback card
- [ ] 4.4 Grounding orchestration: validate every model output with `app/core/grounding.py`; on a violation do **one** corrective-reminder retry before the fallback ladder (§4.4); the fabricated first output is never returned

## 5. Coach service modes (FR-COACH-01, FR-COACH-03, FR-COACH-05, FR-COACH-06)
- [ ] 5.1 Insight mode = **read-through cache** (B2): on a cache **hit** return the `coach_insights` row for `(user_id, current-week Monday)` with **no LLM call**; on a **miss** build the current-week snapshot, one structured call, validate+ground, produce a **2-4 observation / 1-2 recommendation** card or a **quiet** card (`quiet: true` + one observation) when nothing warrants advice, then **upsert `coach_insights` with `week_start = snapshot.window.start` (the user-TZ Monday, M2) ONLY for a grounded, non-fallback card (incl. a quiet card)**; a generation that degrades to the FR-COACH-07 **fallback card** is **returned but NOT cached** (it self-heals — the next same-week request re-attempts generation; symmetric with chat's both-or-neither, M3). The structured-output contract **constrains the counts** (§4.2); an out-of-range count (given `quiet`) is **schema-invalid** -> the §4 ladder (B3). v1 does **not** auto-regenerate mid-week. RED tests `@trace FR-COACH-01`: 2-4 + 1-2 shape; quiet path; **cache hit -> no LLM call**; **out-of-range counts -> degrade, never shown as-is**; **degraded fallback insight -> returned but not upserted, next same-week request re-attempts**; empty-snapshot graceful card (E-1)
- [ ] 5.2 Chat mode: assemble snapshot + history, one structured call, validate+ground, return the §4.2 reply; a **success** is **any grounded, non-fallback reply from either model** (primary `gemma-4-31b-it`, incl. after the one corrective retry, **or** the `Gemini 3 Flash` fallback model) -> persist the `user` turn and the `coach` reply to `coach_messages`; only when it degrades to the **terminal fallback card** persist **neither** turn (no dangling user turn, no fabricated coach turn, M3); RED tests `@trace FR-COACH-03`: both turns stored on any grounded reply (incl. a **corrective-retry success**); **fallback-card chat stores neither turn**; per-user isolation (user A never sees B's thread, FR-AUTH-07)
- [ ] 5.3 Output shape: the **model-facing** JSON-Schema the LLM call enforces is **exactly** the four §4.2 fields (`{language, quiet, observations[], recommendations[]}`); the **server-returned** payload is those four **plus** the reserved server-set boolean fallback marker (MINOR-1), and the **fallback card conforms to §4.2 too** (B4); no emoji, **server-enforced** by a post-generation check — emoji-bearing output is non-conforming and degrades to an emoji-free card (M5, NFR-DES-01); RED tests `@trace FR-COACH-05`: insight and chat both conform; the model-facing schema **excludes** the marker (a model cannot forge a fallback); fallback card conforms + carries no number + marker `true`; a **stubbed emoji-emitting model** -> returned `text` fields are emoji-free
- [ ] 5.4 Language: request `users.coach_language` (`en`/`uk`, default `en`); the payload's `language` equals it and content is in it; RED tests `@trace FR-COACH-06`: `uk` -> `language: "uk"`, default -> `"en"`

## 6. API router (both behind `CurrentUser`, user_id-scoped) + schemas
- [ ] 6.1 `app/api/coach.py`: `POST /api/coach/insight` (generate/return the current-week card) and `POST /api/coach/chat` (one grounded turn), both taking `CurrentUser` + the reused CSRF guard (`app/api/deps.py::require_csrf`); register the router by appending to `app/main.py` (entry-point allowlist)
- [ ] 6.2 Pydantic request/response models mirroring §4.2 field-for-field; a happy-path contract test per endpoint (`200`) with the LLM client **stubbed** (no live call), and a per-user isolation test (FR-AUTH-07)
- [ ] 6.4 **Chat input validation (M4/MINOR-2):** the `/api/coach/chat` request model rejects an **empty/whitespace-only** `user_message` with **422** **and** an **over-long** one (above a **defensive max length** ≈ 2,000 chars ≈ ~500 tok by §4.3's `len/4`) with **422** too — symmetric, rejected-not-clipped (same spirit as slice-004's `_MAX_SESSION_*` caps) — because the message is concatenated into the prompt and its numbers widen the grounding allowed set; RED tests `@trace FR-COACH-03`: empty message -> 422, no LLM call; over-long message -> 422, no LLM call
- [ ] 6.3 **Seam test (real shape, not a mock):** one test that the coach's snapshot input matches the shipped `SnapshotResponse` (`app/schemas/stats.py`) field-for-field (or loads the slice-004 snapshot fixture), so producer/consumer drift fails a test rather than the integrated coach

## 7. Eval suite (deterministic, offline, free — NFR-COST-01)
- [ ] 7.1 `evals/rubrics/coach-output.md` (mirror `evals/rubrics/trajectory-quality.md`): **grounding = CRITICAL** (any fabricated number -> `pass=false`, `score<=49`) plus a **scored** subjective `insight_usefulness`; the rubric covers both, but only the **programmatic** dimensions feed the committed ratchet (M6)
- [ ] 7.2 `evals/cases/coach/*` fixture cases `{snapshot, history?, user_message?, coach_output}` — include an **E-9** case whose `coach_output` plants a number absent from its `snapshot` (the validator MUST catch it) and an **E-1** empty-snapshot case; **no live LLM call** in the committed gate
- [ ] 7.3 Commit `docs/qa/eval/baseline.json` + `docs/qa/eval/latest.json` tracking **ONLY programmatic dimensions** (deterministic + free, M6): `grounding_pass_rate` (=1.0), `grounding_violations` (=0, under `lower_is_better`), `format_conformance` (=1.0), `language_correct` (=1.0, checks the `language` **field**), `no_emoji` (=1.0); `latest` >= `baseline` so `check-eval-ratchet` passes (a no-op until this baseline lands); the E-9 case runs through the same `app/core/grounding.py` validator. **Do NOT** put the subjective `insight_usefulness` in the ratchet
- [ ] 7.4 The subjective `insight_usefulness` is graded by the **eval-judge (LLM)** as a **separate, non-gating** pass in `/run-slice` (not in the committed ratchet), so the CI ratchet stays deterministic and free (NFR-COST-01, M6)

## 8. Verify (green)
- [ ] 8.1 `scripts/verify.*` fully green: ruff, mypy, `alembic upgrade head` (the one new coach migration applies), pytest with `RUN_DB_TESTS=1`, and the frontend build (unchanged — no frontend here)
- [ ] 8.2 `openspec validate add-coach --strict` and `python scripts/check-specs` pass; `python scripts/check-eval-ratchet` passes; `check-traceability` shows FR-COACH-01..07 GAP -> COVERED via the `@trace` docstrings

## 9. Review and done (maker != checker != judge)
- [ ] 9.1 Independent `/code-review` + `/security-review` (per-user isolation on every coach read/write; the prompt leaks no raw session row; the API key is never logged/committed; no emoji in output) plus CodeRabbit on the PR
- [ ] 9.2 Judge scores the change against the acceptance scenarios and marks the slice done; then `openspec archive add-coach`
