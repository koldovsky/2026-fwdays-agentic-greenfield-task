## 1. Failing tests first (red)

- [x] 1.1 Write exhaustive unit tests for the height-parse rule in `lib/growth/validation.ts` (the load-bearing pure function, design D2): ACCEPT "12.5", "12,5", "12.50", "12,50" (all -> 12.5), and a whole number "42" (-> 42); REJECT blank/whitespace-only, "tall"/"abc" (non-numeric), "-3" (negative), "0"/"0.0"/"0,0" (zero, must be > 0), "1000.1"/"999999999" (over the 1000 cm bound), "12.55"/"12,555" (more than one decimal place), and "1,000"/"12.5.5"/"1 200,5" (grouping/multiple separator -> treated as non-numeric). Each rejection maps to a field-keyed Ukrainian message (FR-GROWTH-05). Confirm they FAIL.
- [x] 1.2 Write unit tests for the date rule in `lib/growth/validation.ts` (via the shared `lib/dates.ts`, design D3): an omitted date defaults to today in Europe/Kiev under a fixed clock; a valid past/today `YYYY-MM-DD` is accepted; a non-`YYYY-MM-DD` string, an impossible date ("2026-13-40", "2026-02-30"), and a date after today in Europe/Kiev are each rejected with a field-keyed Ukrainian message (SC-1, SC-2). Confirm they FAIL.
- [x] 1.3 Write unit tests for the shared date helpers after the move to `lib/dates.ts` (design D3, R6): `formatAcquiredDate('2026-03-09')` -> '09.03.2026', null/malformed -> empty; `todayInKiev()` returns the local Kiev calendar `YYYY-MM-DD` under a fixed clock (no UTC midnight off-by-one); `isAfterToday` compares ISO strings correctly. Confirm the existing plant date tests still pass after re-pointing imports. Confirm the new/moved tests FAIL.
- [x] 1.4 Write unit tests for `lib/growth/queries.ts` + `lib/growth/service.ts` against a real in-memory SQLite (`DATABASE_URL=:memory:`, migrations applied, db handle INJECTED as the first param like plants): `insertMeasurement` returns a row with id + plantId + heightCm + measuredOn; `listMeasurements(db, plantId)` returns that plant's rows ordered by `measuredOn DESC, id DESC` (assert the SC-3 same-date tie-break with two rows sharing a date); `getMeasurement(id)` returns the row or a not-found signal; `updateMeasurement` changes only the given row's fields; `deleteMeasurement` removes one row and is a no-op not-found for a missing id; inserting against a non-existent plantId is rejected by the FK. Confirm they FAIL.
- [x] 1.5 Write a REAL-SQLite cascade + smoke-flow test (`tests/integration/`, `:memory:` or temp file, design D4, SC-5): create plant A and plant B; log measurements on plant A (incl. two on the same date) -> they appear ordered date desc then id desc -> edit one measurement's height + date -> reflected -> delete ONE measurement after confirm -> only that row is gone, plant A's other measurements + plant B's data untouched; then delete plant A -> plant A's measurements cascade-removed while plant B's data is intact (FR-GROWTH-01..04, SC-3, SC-5, NFR-DATA-01/02). Confirm it FAILS.
- [x] 1.6 Write tests for the server actions `lib/growth/actions.ts`: each returns the shared `ActionResult`; on a validation failure returns `{ ok: false, fieldErrors, values }` (echoes submitted strings, never throws); on success returns `{ ok: true }` and triggers `revalidatePath` for the plant detail; logging against a non-existent plantId returns a not-found result (not a thrown 500); an edit/delete of a missing measurement id returns a not-found result; a malformed id (float/0/negative/NaN) resolves to a friendly not-found, never a write (design D5). Confirm they FAIL.
- [x] 1.7 Write component tests for the measurements section + add/edit form island and the delete-with-confirm control: renders the list ordered date desc (most recent first) showing height in cm + `DD.MM.YYYY`, and a clear empty state when there are none; renders `FormErrorBanner` + a `FieldError` per field; on a `{ ok:false }` result repopulates uncontrolled inputs from `result.values`; every field has an accessible label and the date field is a native `<input type="date">` defaulting to today; the delete control requires an explicit confirmation step before invoking the action (FR-GROWTH-02, NFR-A11Y-03, NFR-USA-02, SC-3, SC-6). Confirm they FAIL.

## 2. Implement to green

- [x] 2.1 Promote the shared date helpers: MOVE `todayInKiev` / `isAfterToday` / `formatAcquiredDate` from `lib/plants/date.ts` to a new `lib/dates.ts`; move the date tests alongside; re-point the plants imports (`lib/plants/validation.ts` and any others) to `@/lib/dates`; delete the now-empty `lib/plants/date.ts`. Re-run the plant date + validation tests green (design D3, R6). Green for 1.3.
- [x] 2.2 Define `db/schema/growth.ts` (`id` PK autoincrement, `plantId` integer NOT NULL `references(() => plants.id, { onDelete: "cascade" })`, `heightCm` real NOT NULL, `measuredOn` text NOT NULL ISO date, `createdAt` text default `CURRENT_TIMESTAMP`) with `Measurement` / `NewMeasurement` types, and re-export from `db/schema/index.ts` (design D1, D4).
- [x] 2.3 Generate the migration with `npm run db:generate`; commit the SQL + meta snapshot under `db/migrations/`. Confirm `db/client.ts` auto-applies it on startup and `PRAGMA foreign_keys = ON` enforces the cascade (design D7).
- [x] 2.4 Implement the height-parse rule in `lib/growth/validation.ts`: `parseHeightCm(raw)` (trim; reject blank; at most one dot-OR-comma separator; reject grouping/multiple separator as non-numeric; comma -> dot; `^\d+(\.\d+)?$`; at most one decimal place; `> 0`; `<= HEIGHT_MAX_CM = 1000`), the date rule (via `lib/dates.ts`, default to `todayInKiev()`), the `formData -> GrowthInput` mapper, and issue -> Ukrainian field-message mapping; echo submitted `values` on failure; never throw (design D2, D3). Green for 1.1, 1.2.
- [x] 2.5 Implement `lib/growth/queries.ts` (db INJECTED as first param: `listMeasurements`, `getMeasurement`, `insertMeasurement`, `updateMeasurement`, `deleteMeasurement`; list `ORDER BY measuredOn DESC, id DESC` for SC-3) and `lib/growth/service.ts` (orchestration + not-found signal + plant-exists/FK handling) (design D4, D5). Green for 1.4.
- [x] 2.6 Implement `lib/growth/actions.ts` server actions (`createMeasurementAction`, `updateMeasurementAction`, `deleteMeasurementAction`): defense-in-depth integer-id guard -> validate -> service -> `revalidatePath('/plants/<plantId>')`; return the shared `ActionResult`; catch + translate validation / FK / SQLite driver errors -> Ukrainian message (`uk.errors.generic` for whole-form); echo `values` on failure; never throw raw (design D5, error-handling strategy). Green for 1.6.
- [x] 2.7 Extend `lib/i18n/uk.ts` with a `growth` copy block (section heading, height label + hint, measurement-date label + hint, add/save/cancel, list/row labels, empty-state, delete-confirm prompt + confirm/cancel, not-found, and `fieldErrors` for height: required/non-numeric/negative-or-zero/too-large/too-precise/ambiguous, and for date: invalid/future) — Ukrainian text, English identifiers (NFR-LOC-01).
- [x] 2.8 Implement the add/edit measurement form island (client) over the actions: `useActionState`, `FormErrorBanner` + per-field `FieldError`, `defaultValue` repopulation from `result.values`, a height text input, a native `<input type="date">` defaulting to today, accessible labels (design D6, slice-2 D3/D4). Green for the form part of 1.7.
- [x] 2.9 Implement the delete-with-confirm control for a measurement (explicit confirmation step before the action fires; cancel leaves the row intact) (design D6, SC-5). Green for the delete part of 1.7.
- [x] 2.10 Add the measurements section to `app/plants/[id]/page.tsx`: server-side `listMeasurements(db, plantId)`, render the list ordered date desc then id desc (height in cm + `DD.MM.YYYY`) or the empty state, plus the add form and per-row edit + delete-with-confirm controls (FR-GROWTH-02, NFR-A11Y-03, SC-3, design D6). Green for the list part of 1.7.
- [x] 2.11 Confirm all tests from group 1 now pass (green).

## 3. Accessibility (Phase 6, deferred — plain bullets)

> DEFERRED to Phase 6 (cross-cutting QA: axe light+dark + vision-verify across ALL
> capabilities). The rendered-result a11y gates below run once, against the whole
> app, in Phase 6 rather than per-slice — honestly deferred, not skipped. The
> jsdom-level a11y for this slice (accessible labels on the height + date fields,
> the `FieldError` association convention, the keyboard-operable delete confirm) is
> covered by the slice's component tests (1.7).

- (Phase 6) Run `npm run check:a11y` (axe) on the measurements section, add/edit form, and delete-confirm control in BOTH light and dark themes; resolve any violations (NFR-A11Y-01).
- (Phase 6) Verify WCAG 2.1 AA contrast for the measurements list, form, and delete-confirm control in both themes (NFR-A11Y-02).
- (Phase 6) Verify keyboard-only operation: visible focus, accessible names on the height + date fields and the delete control, the native date picker reachable, and that `FieldError` messages are programmatically associated with their fields (NFR-A11Y-04, SC-6).
- (Phase 6) Verify the measurements section + form at a 360 px viewport: content visible/operable with no horizontal overflow (NFR-COMPAT-01).

## 4. Validation, docs, and archive

- [x] 4.1 Run `npm run lint` — zero errors.
- [x] 4.2 Run `npm run test:run` — all unit tests pass (incl. the integration cascade/smoke flow 1.5 and the re-run plant date tests after the 2.1 move).
- [x] 4.3 Run `npm run build` — production build succeeds.
- [x] 4.4 Run `npx openspec validate add-growth --strict` — no errors.
- [x] 4.5 Run `npx openspec validate --all --strict` — no errors.
- [x] 4.6 Manual real-DB smoke test: apply the migration with `npm run db:migrate` against the real SQLite file (or rely on `db/client.ts` auto-migrate), then `npm run dev` and:
  1. open an existing plant's detail `/plants/<id>` — confirm the measurements section shows the helpful Ukrainian empty state (no measurements yet);
  2. log a measurement with height "12,5" (decimal comma) and NO date — confirm it is accepted, stored as 12.5 cm, dated today, and appears in the list as `DD.MM.YYYY`;
  3. log a second measurement on the SAME date — confirm both appear and the more recently created one is listed first (SC-3 tie-break);
  4. log a third measurement on an EARLIER date — confirm the list is ordered date descending (newest date first);
  5. attempt to log height "tall", "-3", "0", "1000.1", "12.55", and "1,000" in turn, and a date in the FUTURE — confirm EACH is rejected inline next to the offending field (no row written), never a raw 500, and the typed values stay on the screen;
  6. edit a measurement to a new valid height + a valid past date — confirm the list reflects the change; then edit it to an invalid height — confirm the original row is unchanged;
  7. reload the app — confirm the measurements are still present (persistence);
  8. delete ONE measurement via the explicit confirm step, first CANCEL (row intact), then CONFIRM — confirm only that row is gone and the plant + its other measurements are untouched (single-row delete, no cascade);
  9. delete the PARENT plant via its confirm step — confirm its measurements are cascade-removed while another plant's measurements remain (FK cascade);
  10. open `/plants/<bad-id>` — confirm the friendly not-found state (no measurements section error).
- [x] 4.7 Update `docs/current-state.md` (date/time in Europe/Kiev, current phase, growth moved planned -> implemented, second DB slice + first plant child landed, shared `lib/dates.ts` promotion noted). README is the course/homework README with no growth/DB section — N/A there; the DB/growth notes live in `docs/current-state.md`.
- [x] 4.8 Only after the smoke test (4.6) passes, archive MANUALLY (this project archives change folders by hand to avoid the CLI conflicting with the pre-authored baseline specs): move `openspec/changes/add-growth/` to `openspec/changes/archive/YYYY-MM-DD-add-growth/` (date in Europe/Kiev). Do NOT run `npx openspec archive`.
