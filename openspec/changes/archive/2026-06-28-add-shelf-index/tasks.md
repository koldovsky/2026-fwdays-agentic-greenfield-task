# Tasks — Add shelf index (C11)

- [x] Reuse existing `groupBooksByTag` query (C8) — no changes needed.
- [x] Write the failing shelf-component test (`components/shelf.test.tsx`).
- [x] Implement `components/BookCardLink.tsx` (DS `BookCard` wrapped in `next/link`).
- [x] Implement `components/TagShelf.tsx` (tag heading + grid of cards).
- [x] Replace `app/page.tsx` with the tag-grouped shelf index + empty state + Add-book action.
- [x] Run `npx vitest run components/shelf.test.tsx` — both suites green.
