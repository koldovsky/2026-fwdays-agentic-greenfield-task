<!-- Authored before code (spec-first): every task is unchecked. The test-first task 5.1 goes RED
     before any implementation makes it green. -->

## 1. Data model and migration
- [ ] 1.1 Add the `categories` model (`user_id` FK, `name`, `color` hex, `description` nullable, `archived_at`) per architecture §2.1
- [ ] 1.2 Alembic migration `0002_categories`: create the table with the partial unique index `UNIQUE(user_id, name) WHERE archived_at IS NULL`; hand-review the autogenerated SQL

## 2. Repository (user_id-scoped, reuses slice 001)
- [ ] 2.1 `CategoryRepo` in `app/repos/` — every method takes `user_id` and scopes every query by it (FR-AUTH-07 pattern)
- [ ] 2.2 Create / list-active / update methods; map a duplicate active name to a `409` rather than a raw DB constraint error
- [ ] 2.3 Delete = archive: set `archived_at` when the category has sessions; a category with no sessions may be hard-deleted (see design Open question 1 on sessions-table sequencing)

## 3. API (behind `CurrentUser`)
- [ ] 3.1 `POST /api/categories` — create; `201`, `409` on a duplicate active name — FR-CAT-01
- [ ] 3.2 `GET /api/categories` — the user's active (non-archived) categories (the read surface the screen needs)
- [ ] 3.3 `PATCH /api/categories/{id}` — edit name/color/description; `200`, `409` on duplicate-rename, `404` cross-user — FR-CAT-02
- [ ] 3.4 `DELETE /api/categories/{id}` — archive vs hard-delete; `204`, `404` cross-user — FR-CAT-03
- [ ] 3.5 Pydantic request/response models (name required, color hex, description optional)

## 4. Frontend — Categories screen (DESIGN §7.4)
- [ ] 4.1 Responsive 3-column grid of category cards: color swatch header, name, description
- [ ] 4.2 "+ New category" form using a native color input (name + color + description)
- [ ] 4.3 Hover edit / delete actions; delete = archive (with the confirm affordance per DESIGN)
- [ ] 4.4 All HTTP through `src/api.ts` (credentials + CSRF header on mutations); no emoji, SVG icons only (NFR-DES-01)

## 5. Tests and verify (test-first)
- [ ] 5.1 Write RED acceptance tests in `backend/tests/test_categories.py` **before** implementation, each with an `@trace FR-CAT-0x` docstring
- [ ] 5.2 Cover: create (happy · per-user · duplicate), edit (happy · duplicate-rename · cross-user), delete (archive · hard-delete · archived-excluded-from-list · cross-user)
- [ ] 5.3 `scripts/verify.*` fully green: ruff, mypy, `alembic upgrade head`, pytest with `RUN_DB_TESTS=1`, and frontend `npm run build`

## 6. Review and done (maker != checker != judge)
- [ ] 6.1 Independent `/code-review` + `/security-review` (isolation holds, no secrets committed) plus CodeRabbit on the PR
- [ ] 6.2 Judge scores the change against the acceptance scenarios and marks the slice done; then `openspec archive add-categories`
