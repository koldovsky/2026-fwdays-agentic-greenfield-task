# add-timer-sessions

Timer + sessions (slice 003): the active timer (start / pause / continue / stop+save / discard), the
saved-session log with manual add / edit / delete, and the 5-second undo for discard / edit / delete.
