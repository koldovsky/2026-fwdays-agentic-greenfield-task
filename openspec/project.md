# Project — agentic-crash-course

> OpenSpec project context. Created by `/project-factory:init` (fallback layout —
> no `openspec` CLI was available to run `npx openspec init`). Install the CLI at
> the Phase 1 stack decision and re-run `npx openspec init` to reconcile.

## Purpose

TBD — set during Phase 1 (requirements). This repo is governed by **Project
Factory**: requirements → OpenSpec specs → capability slices → test-first build
→ adversarial review → evals → QA proof → UAT, behind deterministic gates.

## Stack

TBD — the stack is **not chosen at init** (init is stack-agnostic). It is
declared/scaffolded at Phase 1, recorded as an ADR under `docs/adr/`, and the
placeholders in `AGENTS.md` and this file are filled then.

## Conventions

See `AGENTS.md` (root) for cross-cutting rules and `docs/context-architecture.md`
for the static/dynamic context split.

## Specs

Baseline capability specs live under `openspec/specs/<capability>/`. Per-change
proposals live under `openspec/changes/<change-id>/`. None exist yet — Phase 2
authors the baseline.
